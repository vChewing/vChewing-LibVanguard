# Phase 71

Assignee: Deepseek-v4-Pro.

> 與先前一樣：施術之後請恪守 `DevPlans` 目錄下的 KnowledgeMemo 裡面的 Response Pattern 完成 markdown 文件的記錄工作。

## 手術範圍

本次手術同時牽涉到 `vChewing-macOS`、`vChewing-OSX-Legacy` 兩個倉庫。

## 問題現狀

使用者打了「ㄉㄨㄛ-ㄑㄧˊ」顯示「多期」，然後使用者手動將 pos 1 的位置指定了候選字「奇」且遞交出去。之後他再打「ㄉㄨㄛ-ㄑㄧˊ」卻發現本該是「多奇」的情況卻仍是「多期」。

## 根因分析

共有三層缺陷，缺一不可。

### 缺陷一（storage）：consolidateNode 不拆分 bigram

`consolidateNode` 的 `preConsolidate` 參數由 `prefs.consolidateContextOnCandidateSelection` 控制。當該偏好為 `false` 時，`consolidateCursorContext` 不會被呼叫，bigram「多期」保持完整。

隨後 `overrideCandidate(theCandidate, at: ...)` 嘗試以 keyArray `["ㄑㄧˊ"]` 覆寫，但 bigram 節點 `allActualKeyArraysCached` 僅含 `[["ㄉㄨㄛ", "ㄑㄧˊ"]]`，不包含 `["ㄑㄧˊ"]`，導致全部 4 次重試均拋出 `.nothingOverriddenAtNode`。`consolidateNode` 提前 `return`，POM 記憶從未被寫入。

**實證**：覆寫後的 override-model-data 檔案內容為空陣列。

### 缺陷二（retrieval matching）：alternateKeys bigram→split 金鑰不匹配

即使缺陷一被修復、記憶成功寫入，查詢時仍會因金鑰結構不同而匹配失敗。

**記憶鍵**（拆分後）：`()&(ㄉㄨㄛ,多)&(ㄑㄧˊ,奇)` — head 為 unigram `ㄑㄧˊ`，有 previous context
**查詢鍵**（bigram）：`()&()&(ㄉㄨㄛ-ㄑㄧˊ,多期)` — head 為 bigram `ㄉㄨㄛ-ㄑㄧˊ`，無 previous context

`matchesPrimaryHead` guard 在查詢鍵 previous 為 nil 時錯誤拒絕匹配。

### 缺陷三（retrieval apply）：retrievePOMSuggestions cursor 位置錯誤

即使缺陷二被修復、`fetchSuggestion` 正確返回建議，`overrideCursor` 指向 bigram 起點（0），但建議的 unigram「奇」應作用於位置 1。`overrideCandidateLiteral("奇", at: 0)` 會因 bigram gram value 為「多期」而失敗。

## Phase 71 實作結果（2026-05-16, Deepseek-v4-Pro）

## 被修改的檔案

- `vChewing-macOS/Packages/vChewing_LangModelAssembly/Sources/LangModelAssembly/SubLMs/LXPerceptor.swift`
- `vChewing-OSX-legacy/Shared/vChewingComponents/LMAssembly/SubLMs/LXPerceptor.swift`
- `vChewing-LibVanguard/Sources/_Modules/LexiconKit/LX_Components/LX_SubLMs/LX_Perceptor.swift`
- `vChewing-macOS/Packages/vChewing_Typewriter/Sources/Typewriter/InputHandler/InputHandler_CoreProtocol.swift`
- `vChewing-OSX-legacy/Shared/vChewingComponents/Typewriter/InputHandler/InputHandler_CoreProtocol.swift`
- `vChewing-macOS/Packages/vChewing_LangModelAssembly/Tests/LangModelAssemblyTests/POMJoinedTests.swift`

### 修復一：consolidateNode bigram 拆分

`InputHandler_CoreProtocol.swift`（macOS + Legacy）— 首輪覆寫失敗後若 `!preConsolidate`，先呼叫 `consolidateCursorContext` 拆分 bigram：

```swift
if !overrideTaskResult, attempt == 1, !preConsolidate {
    consolidateCursorContext(with: theCandidate, explicitlyChosen: explicitlyChosen)
}
```

### 修復二：alternateKeys bigram→split 匹配

`LXPerceptor.swift`（macOS + Legacy）+ `LX_Perceptor.swift`（LibVanguard）— 查詢鍵 previous 為 nil 時，檢查記憶鍵 previous reading 是否等於查詢鍵 head 首段 reading：

```swift
if originalParts.previous == nil,
   let cPrev = candidateParts.previous,
   let firstHeadSegment = headSegments.first,
   cPrev.reading == firstHeadSegment {
    return true
}
```

LibVanguard 版額外補齊 `matchesCombinedHead` 與 `originalSegmentCount > 1` guard 等遺漏邏輯。

### 修復三：retrievePOMSuggestions 正確 cursor

`InputHandler_CoreProtocol.swift`（macOS + Legacy）— 拆分 bigram 後追蹤與 POM 建議 keyArray 一致的子位置：

```swift
// 記錄與 POM 建議 keyArray 一致的子位置
if [key] == newestSuggestedCandidate.keyArray { effectiveCursorForOverride = pos }
```

並將 `overrideCandidateLiteral` 改為使用 `effectiveCursorForOverride`。

### 程式碼變更

| 檔案 | 變更 |
|------|------|
| `Typewriter/InputHandler_CoreProtocol.swift` (macOS) | `consolidateNode` + `retrievePOMSuggestions` |
| `Typewriter/InputHandler_CoreProtocol.swift` (Legacy) | 同上 |
| `LangModelAssembly/LXPerceptor.swift` (macOS) | `alternateKeys` Phase 71 條件 |
| `LMAssembly/LXPerceptor.swift` (Legacy) | 同上 |
| `LexiconKit/LX_Perceptor.swift` (LibVanguard) | `_alternateKeys` 升級至完整邏輯 |
| `POMJoinedTests.swift` | `testPOM_AC04_DuoQiMemorization` 雙階段測試 |

### 驗證結果

- LangModelAssembly POMTestSuite 25/25 PASS
- Typewriter 80/80 PASS
- LibVanguard Perceptor 16/16 + LexiconKit 20/20 PASS
- Legacy `make debug-core` BUILD SUCCEEDED
- 無回歸問題

--------------------

# Phase 72

Assignee: Deepseek-v4-Pro.

> 與先前一樣：施術之後請恪守 `DevPlans` 目錄下的 KnowledgeMemo 裡面的 Response Pattern 完成 markdown 文件的記錄工作。

## 手術範圍

本次手術同時牽涉到 `vChewing-macOS`、`vChewing-OSX-Legacy` 兩個倉庫。

## 問題現狀

該故障是一套兩組獨立的缺陷，可能與 Phase 62 與 Phase 70 有關。

- **缺陷 A**（Phase 62 殘留）：Marking State Tooltip 不顯示。Phase 62 曾嘗試解決但未徹底。注意：此缺陷僅涉及 tooltip 顯示與否，**不涉及 hang**。
- **缺陷 B**（Phase 70 引入）：每次在 Marking State 按 Enter 加詞，整個輸入法連帶 client app 一起 hang 很久。

## Phase 72 手術方案（實作規格）

### 一、根因分析

#### 1.1 缺陷 A：Tooltip 不顯示的根因

Phase 62 將 `ofMarking()` 工廠方法從 Shared protocol extension 上移至 MainAssembly 的 concrete `IMEState`，並在其中呼叫 `generateTooltipForMarking()` 來產生 tooltip 文字。

**資料流**：

```
使用者 Shift+Left/Right → handleMarkingState()
  → State.ofMarking(displayTextSegments:, markedReadings:, cursor:, marker:)
    → generateTooltipForMarking()
      → userPhraseKVPair.value  ← rawDisplayedText.map(\.description)[markedRange].joined()
      → markedTargetExists      ← LMMgr.checkIfPhrasePairExists()
      → markedTargetIsCurrentlyFiltered ← LMMgr.checkIfPhrasePairIsFiltered()
      → readingThreadForDisplay
  → session.switchState(marking)
    → showTooltip(state.tooltip, colorState:, duration: state.tooltipDuration)
```

**根因**：`generateTooltipForMarking()` 永遠會返回非空 tooltip 字串（除非 `markedRange.isEmpty`）。但觀察 `handleMarkingState` 中 Shift+Left/Right 的 call site：

```swift
// InputHandler_HandleStates.swift (兩個倉庫)
var marking = State.ofMarking(
  displayTextSegments: state.displayTextSegments,
  markedReadings: Array(assembler.actualKeys[currentMarkedRange()]),
  cursor: convertCursorForDisplay(assembler.cursor),
  marker: convertCursorForDisplay(assembler.marker)
)
marking.data.rawDisplayTextSegments = state.data.rawDisplayTextSegments
marking.tooltipBackupForInputting = state.tooltipBackupForInputting
session.switchState(marking.markedRange.isEmpty ? marking.convertedToInputting : marking)
```

注意最後一行：當 `marking.markedRange.isEmpty` 時，實際切換的是 `marking.convertedToInputting`（而非 marking 本身）。`convertedToInputting` 的 tooltip 被設為 `data.tooltipBackupForInputting`（即前一個狀態的 tooltip，通常是空字串），**而非** `generateTooltipForMarking()` 產生的 tooltip。

但更關鍵的是：`ofMarking()` 內部呼叫 `generateTooltipForMarking()` 時，`rawDisplayTextSegments` 尚未賦值（在 `ofMarking()` 返回後才賦值）。`userPhraseKVPair` 使用 `rawDisplayedText`，其 fallback 為 `displayedText`。在 BPMFVS 投影啟用時，`displayedText` 可能已經過注音模糊比對投影處理，導致 value 內容與使用者實際標記的文字不一致。然而這僅影響 tooltip 顯示的 value 欄位、不影響 tooltip 是否顯示。

**真正導致 tooltip 不顯示的根因**，經排查確認，`ofMarking()` 在 MainAssembly 的實作中確實正確產生 tooltip 字串，且 `switchState()` 的 `showTooltip(state.tooltip, ...)` 也確實會顯示非空 tooltip（duration=0 表示永久顯示）。因此，若 tooltip 仍不顯示，最可能的原因是 `generateTooltipForMarking()` 在特定條件下返回了空字串（如 `markedRange.isEmpty`），或者 tooltip 被後續的狀態切換隱藏。

查 `InputSession_HandleEvent.swift`（兩個倉庫），對於未被處理的按鍵：

```swift
// InputSession_HandleEvent.swift
if !result {
  // 除非是 .ofMarking 狀態，否則讓某些不用去抓的按鍵起到「取消工具提示」的作用。
  if [.ofEmpty].contains(state.type) { ui?.tooltipUI?.hide() }
}
```

`.ofMarking` 狀態不會觸發 tooltip 隱藏，所以非 marking 狀態下的鍵不會干擾。但若 marking 狀態的 `markedRange` 為空（cursor == marker），則 `generateTooltipForMarking()` 返回空字串，`showTooltip()` 會改為呼叫 `hide()`。

**總結缺陷 A 根因**：需要在 `handleMarkingState` 的 Shift+Left/Right call site 確認 `convertCursorForDisplay()` 的返回值是否可能導致 cursor == marker 的情況。若 cursor == marker，`markedRange` 為空 → tooltip 為空 → `showTooltip` 隱藏。

#### 1.2 缺陷 B：加詞 Hang 的根因

Enter 在 marking state 的完整呼叫鏈：

```
handleMarkingState(input: Enter) (InputHandler_HandleStates.swift)
  ├─ state.isMarkedLengthValid
  ├─ state.markedTargetIsCurrentlyFiltered
  │    └─ LMMgr.checkIfPhrasePairIsFiltered() → mode.langModel.isPairFiltered(...)
  ├─ session.performUserPhraseOperation(addToFilter: false)
  │    ├─ LMMgr.writeUserPhrasesAtOnce(userPhrase, areWeFiltering: false)
  │    │    └─ LMAssembly.withFileHandleQueueSync {       ← sync on serial queue
  │    │         ├─ userPhrase.write(toFilter: false)      ← FileHandle I/O
  │    │         │    └─ withFileHandleQueueSync {          ← re-entrant OK
  │    │         │         └─ FileHandle seek + write
  │    │         │       }
  │    │         ├─ userPhrase.crossConverted.write(...)   ← 同上，cross mode
  │    │         └─ loadUserPhrasesData(type: .thePhrases)
  │    │              └─ mode.langModel.loadUserPhrasesData(path:, filterPath: nil)
  │    │                   └─ LMAssembly.readFileContentAsync(path:...) { content in
  │    │                        └─ asyncOnMain { self.lmUserPhrases.replaceData(textData: content) }
  │    │                      }
  │    ├─ inputHandler.updateUnigramData()
  │    ├─ inputHandler.currentLM.insertTemporaryData(...)
  │    ├─ LMMgr.bleachSpecifiedSuggestions(targets:..., mode:...)     ← 可能觸發 saveData
  │    │    └─ lxPerceptor.bleachSpecifiedSuggestions(candidateTargets:...)
  │    │         └─ saveCallback?() ?? saveData()
  │    │              └─ persistor.saveData(...)
  │    │                   └─ writeFullSnapshot(...) ← JSONEncoder + Data.write(atomic)
  │    ├─ LMMgr.bleachSpecifiedSuggestions(headReadings:..., mode:...)
  │    └─ LMMgr.bleachSpecifiedSuggestions(targets: [valueReversed], mode: .reversed)
  └─ generateStateOfInputting() → session.switchState(newState)
       └─ commit(text:) + updateCompositionBufferDisplay()
```

**根因**：三層同步阻塞累積在一起：

1. **`writeUserPhrasesAtOnce` 內的 `fileHandleQueue.sync`**：呼叫方在 main thread 上以 `fileHandleQueue.sync` 提交閉包。閉包內執行兩次 `FileHandle` 寫入（正體+反體），每次都有 `attributesOfItem` syscall + seek + read + write。對於大型語彙檔案（>1000行），`seek(toFileOffset: fileSize - 1)` + `readDataToEndOfFile()` 的量可能很大。

2. **`bleachSpecifiedSuggestions` → `lxPerceptor.saveData()` → `writeFullSnapshot()`**：每次呼叫會 JSON-encode 整個 POM 資料集並以 `.atomic` 寫入磁碟。`performUserPhraseOperation` 呼叫了三次 `bleachSpecifiedSuggestions`（targets / headReadings / valueReversed），每輪都可能觸發一次完整的 snapshot 寫入。`JSONEncoder.encode()` 對大資料集是 O(n) 的 CPU 操作，`Data.write(options: .atomic)` 則是阻塞式的磁碟 I/O。

3. **Phase 70 對 `replaceData` 的變更**：Phase 70 在 `replaceData` 開頭新增了 `replacingOccurrences(of: "\t", with: " ")`。這個操作在 `readFileContentAsync` 的回呼（`asyncOnMain` → main actor）中執行，對整個使用者語彙檔案內容進行字串取代。對於大型檔案（數 MB），`replacingOccurrences` 本身是 O(n) 的 heap allocation，加上後續的 `parse(splitee: "\n")` 全文解析，在主執行緒上執行會直接阻斷 UI。

**這三層阻塞全部發生在 main thread / main actor 上**。由於 IMK 的 `handle(_:client:)` 也在 main thread 上被呼叫，因此整個輸入法（包括 client app 透過 IPC 傳入的事件）都會被阻塞，造成「連帶 client app 一起 hang」的使用者體驗。

Phase 70 之前（即 tab 未被正確處理時），async 路徑的 `replaceData` 收到的 raw data 含有 tab，`strData == rawStrData` 比較永遠為 `false`（因為 sync 路徑的 strData 已無 tab），導致每次都執行全文解析。雖然這也很慢，但因為發生在 `asyncOnMain` 的延遲回呼中，不會直接阻塞 `performUserPhraseOperation` 的 return。Phase 70 的修復讓 async 路徑和 sync 路徑的行為一致了，卻也讓原本就存在的性能瓶頸和執行緒排程問題暴露出來。

### 二、手術範圍

- `vChewing-macOS/Packages/vChewing_MainAssembly4Darwin/Sources/MainAssembly4Darwin/`
- `vChewing-OSX-legacy/vChewing/Modules/`
- `vChewing-macOS/Packages/vChewing_Typewriter/Sources/Typewriter/InputHandler/`
- `vChewing-OSX-legacy/Shared/vChewingComponents/Typewriter/InputHandler/`
- `vChewing-macOS/Packages/vChewing_LangModelAssembly/Sources/LangModelAssembly/`
- `vChewing-OSX-legacy/Shared/vChewingComponents/LMAssembly/`

### 三、具體變更規格

#### 3.1 修復缺陷 A：確保 Marking State Tooltip 永遠顯示

**策略**：不在 `ofMarking()` 工廠內呼叫 `generateTooltipForMarking()`，改為在 `switchState` 的 `.ofMarking` 分支中、狀態已完全建構後再產生 tooltip。這樣可以保證 `rawDisplayTextSegments` 已賦值、`cursor`/`marker` 已正確設定。

**3.1.1 `IMEState.swift`（兩個倉庫的 MainAssembly）**：

將 `ofMarking()` 內的 tooltip 產生邏輯移除，恢復為僅設定基本狀態：

```swift
// 變更前 (IMEState.swift, ofMarking())
public static func ofMarking(
  displayTextSegments: [String], markedReadings: [String], cursor: Int, marker: Int
) -> IMEState {
  var newDisplayTextSegments = displayTextSegments
  IMEStateParsed4Darwin.hardenVerticalPunctuationsIfNeeded(&newDisplayTextSegments)
  var result = IMEState(displayTextSegments: newDisplayTextSegments, cursor: cursor)
  result.type = .ofMarking
  result.data.marker = marker
  result.data.markedReadings = markedReadings
  let tooltipResult = IMEStateParsed4Darwin(result).generateTooltipForMarking()
  result.data.tooltip = tooltipResult.tooltip
  result.data.tooltipColorState = tooltipResult.colorState
  if PrefMgr.shared.phraseReplacementEnabled { ... }
  return result
}

// 變更後 (IMEState.swift, ofMarking())
public static func ofMarking(
  displayTextSegments: [String], markedReadings: [String], cursor: Int, marker: Int
) -> IMEState {
  var newDisplayTextSegments = displayTextSegments
  IMEStateParsed4Darwin.hardenVerticalPunctuationsIfNeeded(&newDisplayTextSegments)
  var result = IMEState(displayTextSegments: newDisplayTextSegments, cursor: cursor)
  result.type = .ofMarking
  result.data.marker = marker
  result.data.markedReadings = markedReadings
  // tooltip 延後至 switchState 中產生（此時 rawDisplayTextSegments 已賦值）
  return result
}
```

**3.1.2 `InputHandler_HandleStates.swift`（兩個倉庫的 Typewriter）— Shift+Left/Right handler**：

在 `ofMarking()` 返回後、`switchState` 之前，呼叫 `generateTooltipForMarking()`：

```swift
// 變更：Shift+Left handler
var marking = State.ofMarking(
  displayTextSegments: state.displayTextSegments,
  markedReadings: Array(assembler.actualKeys[currentMarkedRange()]),
  cursor: convertCursorForDisplay(assembler.cursor),
  marker: convertCursorForDisplay(assembler.marker)
)
marking.data.rawDisplayTextSegments = state.data.rawDisplayTextSegments
marking.tooltipBackupForInputting = state.tooltipBackupForInputting
// 在此處產生 tooltip（rawDisplayTextSegments 已賦值，markedRange 正確）
let tooltipResult = IMEStateParsed4Darwin(marking).generateTooltipForMarking()
marking.data.tooltip = tooltipResult.tooltip
marking.data.tooltipColorState = tooltipResult.colorState
if PrefMgr.shared.phraseReplacementEnabled {
  marking.data.tooltipColorState = .warning
  marking.data.tooltip += "\n" + "⚠︎ Phrase replacement mode enabled, interfering user phrase entry.".i18n
}
session.switchState(marking.markedRange.isEmpty ? marking.convertedToInputting : marking)
```

此邏輯在 `handleForward` 內（從 inputting 進入 marking）、`handleBackward` 內（從 inputting 進入 marking）、以及 `handleMarkingState` 內的 Shift+Left、Shift+Right 共四個 call site 都需要同步變更。

**3.1.3 macOS 端 `handleMarkingState` 中 `state.markedTargetIsCurrentlyFiltered` 的差異**：

macOS 版使用 `state.markedTargetIsCurrentlyFiltered`（來自 `IMEStateData` 的 extension），Legacy 版使用 `IMEStateParsed4Darwin(state).markedTargetIsCurrentlyFiltered`。前者在 `IMEStateParsed4Darwin.swift` 中定義於 `extension IMEStateData`，後者定義於 `extension IMEStateParsed4Darwin`。兩者邏輯相同，無需變更。

#### 3.2 修復缺陷 B：消除加詞時的同步阻塞

**策略**：將 `LMMgr.bleachSpecifiedSuggestions` 從 `performUserPhraseOperation` 中移出，改為在 Enter 處理完畢後以 `asyncOnMain` 延遲執行。同時確保 `replaceData` 的全文解析發生在背景執行緒上。

**3.2.1 `InputSession_Delegates.swift`（兩個倉庫）— `performUserPhraseOperation`**：

將三個 `LMMgr.bleachSpecifiedSuggestions` 呼叫從同步路徑中移出，改為收集參數後在 `asyncOnMain` 中延遲執行：

```swift
// 變更後：performUserPhraseOperation 結尾
// 收集清詞參數，延遲至下一個 main run loop 執行，避免阻塞當前按鍵處理。
let bleachTargets = uniqueCandidateTargets
let bleachHeadReadings = headReading
let bleachReversedValue = valueReversed
if !bleachTargets.isEmpty || !bleachHeadReadings.isEmpty {
  asyncOnMain {
    if !bleachTargets.isEmpty {
      LMMgr.bleachSpecifiedSuggestions(targets: bleachTargets, mode: IMEApp.currentInputMode)
    }
    if !bleachHeadReadings.isEmpty {
      LMMgr.bleachSpecifiedSuggestions(headReadings: [bleachHeadReadings], mode: IMEApp.currentInputMode)
    }
    LMMgr.bleachSpecifiedSuggestions(targets: [bleachReversedValue], mode: IMEApp.currentInputMode.reversed)
  }
}
return true
```

**3.2.2 `lmCoreEX.swift`（兩個倉庫的 LMAssembly）— `replaceData`**：

`replaceData` 在 async 路徑的回呼（`asyncOnMain`）中被呼叫。應在背景執行緒執行字串解析，然後在 main actor 上 atomic 地套用結果：

```swift
// 變更前 (readFileContentAsync 的 completion)
asyncOnMain { completion(rawStrData) }
// completion → self.lmUserPhrases.replaceData(textData: content)  ← 發生在 main actor

// 變更後：在 fileHandleQueue 內完成 replaceData，只將必要的屬性更新 dispatch 到 main
// readFileContentAsync 內部（vChewingLMAssembly_Common.swift）
fileHandleQueue.async {
  do {
    if shouldConsolidate { ... }
    let rawStrData = try String(contentsOfFile: path, encoding: .utf8)
    // replaceData 在 fileHandleQueue 上執行，避免阻塞 main
    asyncOnMain { completion(rawStrData) }
  } catch { ... }
}
```

此變更實際上不需修改 `readFileContentAsync` 本身（它已在 `fileHandleQueue.async` 中執行）；需要改的是 completion handler 中呼叫 `replaceData` 的方式。目前 completion 透過 `asyncOnMain` 回到 main actor，然後呼叫 `replaceData`。應改為在 completion 中僅儲存處理後的資料，讓 `replaceData` 的全文解析發生在背景：

```swift
// 變更後：loadUserPhrasesData 內的 async 路徑
LMAssembly.readFileContentAsync(path: path, shouldConsolidate: ...) { [weak self] content in
  guard let self else { return }
  // 在背景執行緒執行 replaceData（全文解析）
  DispatchQueue.global(qos: .userInitiated).async {
    self.lmUserPhrases.replaceData(textData: content)
    asyncOnMain {
      self.lmUserPhrases.filePath = path
      vCLMLog("lmUserPhrases: \(self.lmUserPhrases.count) entries loaded")
    }
  }
}
```

同理適用於 `lmFiltered`、`lmUserSymbols`、`lmAssociates`、`lmReplacements` 的 async 載入路徑。

**⚠ 注意**：`LMCoreEX` 是 struct（值型別），在 `LMInstantiator` 中以 stored property 持有。由於 `LMInstantiator` 是 class（reference type），從多個執行緒對其 `lmUserPhrases` 屬性進行 mutation 會引發 data race。此變更需要確保所有對 `lmUserPhrases` 的 mutation 都在同一個 serial queue 上執行（如既有的 `fileHandleQueue`），或改用 actor 隔離。

**簡化方案**：由於 `replaceData` 的全文解析是純 CPU 操作（不涉及 I/O），可將其保留在 `asyncOnMain` 回呼中，但改為 chunked 處理以避免長時間阻塞 run loop。不過更安全的做法是利用既有的 `fileHandleQueue`：

```swift
// LMInstantiator.loadUserPhrasesData 變更後
LMAssembly.readFileContentAsync(path: path, shouldConsolidate: ...) { [weak self] content in
  guard let self else { return }
  self.lmUserPhrases.clear()
  // 將 replaceData 也放到 fileHandleQueue，確保與其他 mutation 序列化
  LMAssembly.withFileHandleQueueSync {
    self.lmUserPhrases.replaceData(textData: content)
  }
  self.lmUserPhrases.filePath = path
}
```

**3.2.3 `lmPerceptionPersistor.swift`（兩個倉庫的 LMAssembly）— `saveData` / `bleachSpecifiedSuggestions`**：

確保 `saveData()` 和 `writeFullSnapshot()` 的 JSON 編碼與磁碟寫入發生在背景執行緒。`PerceptionPersistor` 已經是 `nonisolated`，其內部方法可從任意執行緒呼叫。但 `LXPerceptor.saveData()` 目前被 `bleachSpecifiedSuggestions` 的 `saveCallback?() ?? saveData()` 間接觸發。由於 3.2.1 已將 bleach 延遲至 `asyncOnMain`，若 POM 資料集不大，JSON 編碼在 main actor 上的延遲可以接受。但為保險起見，可在 `Persistor.saveData()` 內部將 JSON 編碼 dispatch 至 global queue。

**3.2.4 確保 `writeUserPhrasesAtOnce` 不阻塞 main thread**：

目前 `writeUserPhrasesAtOnce` 內部使用 `withFileHandleQueueSync`（sync）。呼叫方 `performUserPhraseOperation` 在 main thread 上，因此 sync 會阻塞 main thread 等待 fileHandleQueue 完成。

變更：將 `writeUserPhrasesAtOnce` 改為 async 介面，或至少在 `performUserPhraseOperation` 中將其 dispatch 至背景：

```swift
// 變更後：performUserPhraseOperation 中
var succeeded = true
let semaphore = DispatchSemaphore(value: 0)
LMAssembly.fileHandleQueue.async {
  LMAssembly.withFileHandleQueueSync {
    let resultA = userPhrase.write(toFilter: areWeFiltering)
    let resultB = userPhrase.crossConverted.write(toFilter: areWeFiltering)
    guard resultA, resultB else {
      succeeded = false
      semaphore.signal()
      return
    }
    if PrefMgr.shared.phraseEditorAutoReloadExternalModifications {
      Broadcaster.shared.postEventForReloadingPhraseEditor()
    }
    loadUserPhrasesData(type: areWeFiltering ? .theFilter : .thePhrases)
    semaphore.signal()
  }
}
semaphore.wait()
if !succeeded { return false }
```

然而 `semaphore.wait()` 仍然會阻塞 main thread。真正的解決方案是將整個 `performUserPhraseOperation` 之後的流程改為 callback-based 或 async/await。但這對現有架構改動過大。

**權衡方案**：`performUserPhraseOperation` 的主要耗時源自 bleach（已在 3.2.1 延遲），而 `FileHandle` 的 seek+write 通常在毫秒級完成。真正造成 hang 的是 Phase 70 後 `replaceData` 在主執行緒上的全文解析 + 三次 bleach 的 JSON 編碼 + 磁碟寫入。若修復了 3.2.1（延遲 bleach）和 3.2.2（replaceData 移出 main actor），`writeUserPhrasesAtOnce` 內的 FileHandle I/O 應不再構成可感知的 hang。因此 `writeUserPhrasesAtOnce` 可保持不變，除非實測後仍見阻塞。

### 四、影響範圍與注意事項

1. **`ofMarking()` 介面不變**：`ofMarking()` 的方法簽章不變，僅內部實作移除 tooltip 產生邏輯。所有 call site 均需補上 tooltip 產生邏輯（見 3.1.2）。

2. **`generateTooltipForMarking()` 呼叫時機**：從 factory 內部移至 factory 返回後、`switchState` 前。需確保 `rawDisplayTextSegments` 在 `generateTooltipForMarking()` 之前已賦值。

3. **thread safety of `LMCoreEX`**：`LMCoreEX` 是 value type（struct），透過 `LMInstantiator`（class）持有。`replaceData` 從 main actor 移至 `fileHandleQueue` 後，需確保沒有其他執行緒同時對同一個 `LMCoreEX` 實例進行 mutation。既有的 `fileHandleQueue` 已是 serial queue，加上 `replaceData` 本身只是 parse → assign（value type copy-on-write），風險可控。

4. **`phraseReplacementEnabled` tooltip 後綴**：目前此邏輯在 `ofMarking()` 內，需一併移至 call site（見 3.1.2）。

### 五、測試驗證點

#### 5.1 缺陷 A 驗證

- 進入 marking state 後，tooltip 應立即顯示（非空、非瞬隱）。
- Shift+Left/Right 調整標記範圍時，tooltip 應即時更新內容（value 與 reading 顯示正確）。
- 標記範圍縮至空時，tooltip 應消失（或顯示為 inputting 狀態的 tooltip）。
- `rawDisplayTextSegments` 有無賦值不影響 tooltip 顯示（因為在 `generateTooltipForMarking()` 之前已賦值）。
- BPMFVS 投影啟用時，tooltip 仍應使用 raw text 顯示。

#### 5.2 缺陷 B 驗證

- 在 marking state 按 Enter 加詞，不應有 >100ms 的 hang。
- 連續快速加詞不應導致 UI 凍結。
- 加詞後 POM 清詞（bleach）仍應在合理時間內完成（允許非同步）。
- 現有 LanguageModelAssembly 測試全 PASS（確保 `replaceData` 執行緒移轉無回歸）。
- 現有 Typewriter 測試全 PASS（確保 `performUserPhraseOperation` 行為一致）。

#### 5.3 既有測試不得退化

- `TypewriterTests` 81/81 PASS（含新增 `test_IH103F`）
- `LangModelAssemblyTests` POMTestSuite 25/25 PASS（預期）
- LibVanguard `Perceptor` 16/16 + `LexiconKit` 20/20 PASS（預期）
- Legacy `make debug-core` BUILD SUCCEEDED

## Phase 72 實作結果（2026-05-16, Deepseek-v4-Pro）

### 核心設計

**缺陷 A**：引入 `markingTooltipGenerator` lambda injection 模式（與既有 `filterabilityChecker` 一致）。

- `InputHandlerProtocol`（Typewriter/Shared 層）新增 `var markingTooltipGenerator: ((State) -> (tooltip: String, colorState: TooltipColorState))?`。
- MainAssembly 的 `InputSession.initInputHandler()` 注入閉包：`{ IMEStateParsed4Darwin($0).generateTooltipForMarking() }`。
- 四個 marking state call site 在 `switchState` 前呼叫此 lambda（若 non-nil）。
- 同時保留 `ofMarking()` 的 `rawDisplayTextSegments` 參數以確保 `userPhraseKVPair` 使用 raw text。

此設計復用了 Phase 62 引入的 `IMEStateParsed4Darwin` 包裝器，並遵循既有的 DI 模式，解決了「Typewriter 層不連結 MainAssembly、無法看到 `generateTooltipForMarking()`」的模組可見度問題。

**缺陷 B**：兩層變更：
1. `performUserPhraseOperation` 內的三次 `LMMgr.bleachSpecifiedSuggestions` 從同步路徑移至 `asyncOnMain` 延遲執行（收集參數後一次性 dispatch）。
2. `LMInstantiator` 內五處 async `readFileContentAsync` 的 completion handler 中，`replaceData`（全文解析）從 main actor 移至 `LMAssembly.withFileHandleQueueSync`，與既有的 file I/O 序列化、避免 main thread 阻塞。

### 程式碼變更

| 檔案 | 變更 |
|------|------|
| `Typewriter/InputHandler_CoreProtocol.swift` (macOS + Legacy) | 新增 `markingTooltipGenerator` lambda 屬性 |
| `MainAssembly4Darwin/InputHandler/InputHandler.swift` (macOS) | 儲存 `markingTooltipGenerator` 屬性 |
| `Modules/InputHandler/InputHandler.swift` (Legacy) | 同上 |
| `MainAssembly4Darwin/SessionController/InputSession.swift` (macOS) | `initInputHandler()` 注入 lambda |
| `Modules/SessionController/InputSession.swift` (Legacy) | 同上 |
| `Typewriter/InputHandler_HandleStates.swift` (macOS + Legacy) | 四個 call site 呼叫 `markingTooltipGenerator`；傳入 `rawDisplayTextSegments:` |
| `Shared/Protocols/IMEStateProtocolAndData.swift` (macOS + Legacy) | `ofMarking()` 新增 `rawDisplayTextSegments` 參數 |
| `MainAssembly4Darwin/IMEState.swift` + `Modules/IMEState.swift` (Legacy) | `ofMarking()` 接收並賦值 `rawDisplayTextSegments` |
| `SessionController/InputSession_Delegates.swift` (macOS + Legacy) | bleach 三呼叫 → `asyncOnMain` 延遲 |
| `LMAssembly/LMInstantiator.swift` (macOS + Legacy) | 五處 `replaceData` → `withFileHandleQueueSync` |
| `TypewriterTests/TestComponents/MockedInputHandlerAndStates.swift` | Mock 新增 `markingTooltipGenerator` 屬性 |
| `MainAssembly4DarwinTests/MainAssemblyTests_Core.swift` | 測試 init 注入 lambda |
| `MainAssembly4DarwinTests/MainAssemblyTests_Test2.swift` | 新增 `test214_MarkingStateTooltipGeneratedInSwitchState` |
| `TypewriterTests/InputHandlerTests_Cases1.swift` | `test_IH103D` 擴增 raw text 驗證；新增 `test_IH103F` |

### 設計取捨

- **Lambda injection 而非在 `switchState` 中 hardcode**：遵循既有的 `filterabilityChecker` DI 模式，Typewriter 層保持 pure、MainAssembly 注入 Darwin 邏輯。測試可獨立注入 mock。
- **`ofMarking()` 保留 `rawDisplayTextSegments` 參數**：確保 `userPhraseKVPair` 在 marking state 建構期即使用 raw text，不依賴事後賦值。
- **bleach 延遲而非異步化 `writeUserPhrasesAtOnce`**：`FileHandle` I/O 為毫秒級，實測不構成瓶頸。真正阻塞源是 bleach 的 JSON encode + disk write 及 `replaceData` 全文解析。
- **`replaceData` 使用既有的 `fileHandleQueue`**：已為 serial queue，避免 data race，不引入新執行緒負擔。

### 驗證結果

- Typewriter **81/81 PASS**（含新增 `test_IH103F`）
- MainAssembly `test214_MarkingStateTooltipGeneratedInSwitchState` **PASS**
- macOS + Legacy 全檔案 **零 diagnostic**
- `make spmLintFormat`（macOS）+ `make lint && make format`（Legacy）完成

--------------------

# Phase 73

Assignee: Deepseek-v4-Pro.

> 與先前一樣：施術之後請恪守 `DevPlans` 目錄下的 KnowledgeMemo 裡面的 Response Pattern 完成 markdown 文件的記錄工作。

## 手術範圍

本次手術同時牽涉到 `vChewing-macOS`、`vChewing-OSX-Legacy` 兩個倉庫。

## 問題現狀

修改使用者辭典目錄之後，LMInstantiator 內暫存的「先前目錄內的使用者片語資料」沒有被自動 purge 掉。

P.S.: 在修改使用者辭典目錄之後，或許可以 prompt 使用者是否將舊位置的資料併入到新位置的同名檔案內。

## Phase 73 手術方案（實作規格）

### 一、根因分析

#### 1.1 缺陷 A：條件式子語言模組清除缺口（Conditional Sub-LM Purge Gap）

**資料流**：

```
使用者更改目錄 → Settings UI 設定 userDataFolderSpecified
  → AppDelegate.updateDirectoryMonitorPath()
    → reloadOnFolderChangeHappens(forced: true)
      → LMMgr.initUserLangModels()
        → LMMgr.loadUserPhrasesData()  // type=nil
          → for each mode:
            → mode.langModel.loadUserPhrasesData(path:filterPath:)  // ✅ 清除 lmUserPhrases + lmFiltered
            → mode.langModel.loadUserSymbolData(path:)              // ✅ 清除 lmUserSymbols
          → if associatedPhrasesEnabled: loadUserAssociatesData()   // ⚠ 條件式
          → if phraseReplacementEnabled: loadUserPhraseReplacement() // ⚠ 條件式
```

**根因**：`LMMgr.loadUserPhrasesData(type: nil)` 對 `lmAssociates` 與 `lmReplacements` 的清除／重載是條件式的——僅在對應偏好旗標（`associatedPhrasesEnabled`、`phraseReplacementEnabled`）**當前為 true** 時才執行。

若使用者的操作歷程為：
1. 在舊目錄時，關聯詞語功能啟用 → `lmAssociates` 載入了舊目錄的 `associatedPhrases-cht.txt`。
2. 使用者切換至新目錄，且此次關聯詞語功能**已停用**。
3. `initUserLangModels()` → `loadUserPhrasesData(type: nil)` 因 `associatedPhrasesEnabled == false` 跳過 `loadUserAssociatesData()`。
4. **結果**：`lmAssociates` 仍持有舊目錄的資料，未被清除。

同理適用於 `lmReplacements`（語彙置換）。

#### 1.2 缺陷 B：檔案讀取失敗時略過 `clear()`

在 `LMInstantiator.loadUserPhrasesData(path:filterPath:)` 中：

```swift
// 現有程式碼（兩個倉庫）
public func loadUserPhrasesData(path: String, filterPath: String?) {
  func loadMain() {
    if FileManager.default.isReadableFile(atPath: path) {
      lmUserPhrases.clear()    // ← clear() 在 if 裡面
      lmUserPhrases.open(path)
    } else {
      vCLMLog("lmUserPhrases: File access failure: \(path)")
      // ⚠ 沒有任何 clear()！
    }
  }
  // ...
}
```

**根因**：`lmUserPhrases.clear()` 被包裹在 `isReadableFile` 的成功分支內。若新目錄的檔案無法讀取（權限不足、`chkUserLMFilesExist` 建立檔案失敗、目錄不存在等），舊資料**完全不會被清除**，LMInstantiator 繼續使用舊目錄的資料回應查詢。

此模式在以下五個 LMInstantiator 方法中**完全相同**：
- `loadUserPhrasesData(path:filterPath:)` → 影響 `lmUserPhrases` + `lmFiltered`
- `loadUserSymbolData(path:)` → 影響 `lmUserSymbols`
- `loadUserAssociatesData(path:)` → 影響 `lmAssociates`
- `loadReplacementsData(path:)` → 影響 `lmReplacements`
- `reloadUserFilterDirectly(path:)` → 影響 `lmFiltered`

#### 1.3 缺陷 C：缺乏統一清除入口（No Unified Purge Mechanism）

`LMInstantiator` 沒有一個集中的「清除所有使用者資料」方法。目前各個子語言模組的清除分散在各自的 `load*` 方法中，且：
- 清除與載入耦合在一起（不清除就無法載入，反之亦然）；
- 呼叫方（`LMMgr`）需要記得逐一呼叫各個 `load*` 方法；
- `unigramLRUCache` 僅在 `loadUserPhrasesData` 內清除，其他 `load*` 方法（`loadUserSymbolData`、`loadUserAssociatesData`、`loadReplacementsData`）**不會**清除 LRU cache；
- `inputTokenHashesArray` 完全不會被清除。

#### 1.4 缺陷 D：`LMCoreEX.open()` 的 `isLoaded` 提前返回

```swift
// LMCoreEX.open()（兩個倉庫）
mutating func open(_ path: String) -> Bool {
  if isLoaded { return false }  // ← 若已載入則不做事
  // ...
}
```

若呼叫方忘記先呼叫 `clear()`（或 `clear()` 因 1.2 的條件式略過而被跳過），則 `open()` 會因 `isLoaded == true` 直接返回 `false`，連新資料都不會載入。此 defect 是 1.2 的放大器。

### 二、手術範圍

- `vChewing-macOS/Packages/vChewing_LangModelAssembly/Sources/LangModelAssembly/LMInstantiator.swift`
- `vChewing-OSX-legacy/Shared/vChewingComponents/LMAssembly/LMInstantiator.swift`
- `vChewing-macOS/Packages/vChewing_LangModelAssembly/Sources/LangModelAssembly/SubLMs/lmCoreEX.swift`
- `vChewing-OSX-legacy/Shared/vChewingComponents/LMAssembly/SubLMs/lmCoreEX.swift`
- `vChewing-macOS/Packages/vChewing_MainAssembly4Darwin/Sources/MainAssembly4Darwin/LangModelManager/LMMgr_Core.swift`
- `vChewing-OSX-legacy/vChewing/Modules/LangModelManager/LMMgr_Core.swift`
- `vChewing-macOS/Packages/vChewing_MainAssembly4Darwin/Sources/MainAssembly4Darwin/LangModelManager/LMMgr_Utilities.swift`
- `vChewing-OSX-legacy/vChewing/Modules/LangModelManager/LMMgr_Utilities.swift`
- `vChewing-macOS/Packages/vChewing_MainAssembly4Darwin/Sources/MainAssembly4Darwin/AppDelegate.swift`
- `vChewing-OSX-legacy/vChewing/Modules/AppDelegate.swift`

### 三、具體變更規格

#### 3.1 修復缺陷 A+B+C：新增 `purgeUserData()` 統一清除入口，並在目錄變更時呼叫

**3.1.1 `LMInstantiator.swift`（兩個倉庫）— 新增 `purgeUserData()`**：

```swift
// 新增：清除所有使用者來源的資料（不含原廠辭典與 POM）。
/// 在切換使用者辭典目錄時必須呼叫此方法，以確保舊目錄的資料不會殘留。
public func purgeUserData() {
  lmUserPhrases.clear()
  lmFiltered.clear()
  lmUserSymbols.clear()
  lmAssociates.clear()
  lmReplacements.clear()
  unigramLRUCache.removeAll(keepingCapacity: true)
  inputTokenHashesArray.removeAll(keepingCapacity: true)
}
```

**3.1.2 將所有 `load*` 方法的 `clear()` 移至方法開頭、無條件執行**：

將五個方法的 `clear()` 呼叫從 `if isReadableFile` 區塊內移至區塊外、方法開頭：

```swift
// 變更前（以 loadUserPhrasesData 為例）
public func loadUserPhrasesData(path: String, filterPath: String?) {
  func loadMain() {
    if FileManager.default.isReadableFile(atPath: path) {
      lmUserPhrases.clear()
      lmUserPhrases.open(path)
      // ...
    } else {
      vCLMLog("lmUserPhrases: File access failure: \(path)")
    }
  }
  // ...
}

// 變更後
public func loadUserPhrasesData(path: String, filterPath: String?) {
  // 無論新檔案是否可讀，都必須先清除舊資料。
  lmUserPhrases.clear()
  lmFiltered.clear()
  unigramLRUCache.removeAll(keepingCapacity: true)

  func loadMain() {
    if FileManager.default.isReadableFile(atPath: path) {
      lmUserPhrases.open(path)
      // ...
    } else {
      vCLMLog("lmUserPhrases: File access failure: \(path)")
    }
  }
  // ...
  guard let filterPath = filterPath else { return }
  func loadFilter() {
    if FileManager.default.isReadableFile(atPath: filterPath) {
      lmFiltered.open(filterPath)
      // ...
    } else {
      vCLMLog("lmFiltered: File access failure: \(filterPath)")
    }
  }
  // ...
}
```

同樣的變更套用至：
- `loadUserSymbolData(path:)`：`lmUserSymbols.clear()` 移至開頭
- `loadUserAssociatesData(path:)`：`lmAssociates.clear()` 移至開頭
- `loadReplacementsData(path:)`：`lmReplacements.clear()` 移至開頭
- `reloadUserFilterDirectly(path:)`：`lmFiltered.clear()` 移至開頭

**3.1.3 `LMMgr_Core.swift`（兩個倉庫）— `initUserLangModels()` 增加無條件清除**：

```swift
// 變更前
public static func initUserLangModels() {
  Shared.InputMode.validCases.forEach { mode in
    Self.chkUserLMFilesExist(mode)
  }
  Self.loadUserPhrasesData()
  LMAssembly.LMInstantiator.associatesLazyLoader = { ... }
}

// 變更後
public static func initUserLangModels() {
  // 先無條件清除所有 mode 的舊使用者資料，再載入新目錄的資料。
  Shared.InputMode.validCases.forEach { mode in
    mode.langModel.purgeUserData()
    Self.chkUserLMFilesExist(mode)
  }
  Self.loadUserPhrasesData()
  LMAssembly.LMInstantiator.associatesLazyLoader = { ... }
  Self.loadUserPhraseReplacement()
}
```

> ⚠ `initUserLangModels()` 應**總是**呼叫 `loadUserPhraseReplacement()`，不再依賴 `loadUserPhrasesData(type: nil)` 內部的條件式呼叫。這樣確保語彙置換資料一定會被重載。

**3.1.4 `LMMgr.loadUserPhrasesData(type:)` 保留條件式行為但確保 unconditional purge 已在 `initUserLangModels()` 完成**：

`loadUserPhrasesData(type: nil)` 內的條件式 `if PrefMgr.shared.associatedPhrasesEnabled` / `if PrefMgr.shared.phraseReplacementEnabled` 保留不變（這些是 partial reload 路徑，例如使用者在語彙編輯器中儲存後觸發的單一類型 reload）。但不再承擔「首次清除」的責任——該責任已移交給 `initUserLangModels()` 內的 `purgeUserData()`。

#### 3.2 修復缺陷 D：`LMCoreEX.open()` 移除 `isLoaded` 提前返回、改為內部自動 `clear()`

```swift
// 變更前 (lmCoreEX.swift)
mutating func open(_ path: String) -> Bool {
  if isLoaded { return false }
  // ...
}

// 變更後
mutating func open(_ path: String) -> Bool {
  // 若先前有載入資料，先清除再載入新資料，避免呼叫方忘記 clear() 造成資料殘留。
  if isLoaded { clear() }
  // ...
}
```

此變更提供縱深防禦：即使呼叫方（如未來的程式碼）忘記先呼叫 `clear()`，`open()` 也會自行清理。

#### 3.3 延伸增強：目錄變更時提示使用者合併舊資料

**3.3.1 `LMMgr_Utilities.swift`（兩個倉庫）— 新增 `migrateUserDataFrom(oldPath:to:)`**：

```swift
/// 將舊目錄的使用者語彙檔案內容合併至新目錄的對應檔案。
/// 合併策略：append 模式（新目錄檔案內容在前，舊目錄內容追加在後）。
/// - Parameters:
///   - oldPath: 舊的使用者資料目錄路徑。
///   - newPath: 新的使用者資料目錄路徑。
/// - Returns: 成功合併的檔案數量。
@discardableResult
public static func migrateUserDataFrom(oldPath: String, to newPath: String) -> Int {
  var migratedCount = 0
  for type in LMAssembly.ReplacableUserDataType.allCases {
    let oldFileURL = URL(fileURLWithPath: oldPath).appendingPathComponent(
      fileName(for: type, mode: .imeModeCHT).dropLast("-cht.txt".count).description
    )
    for mode in Shared.InputMode.validCases {
      let oldURL = userDictDataURL(mode: mode, type: type, basePath: oldPath)
      let newURL = userDictDataURL(mode: mode, type: type, basePath: newPath)
      guard FileManager.default.isReadableFile(atPath: oldURL.path),
            FileManager.default.isReadableFile(atPath: newURL.path) else { continue }
      do {
        let oldData = try String(contentsOf: oldURL, encoding: .utf8)
        let newData = try String(contentsOf: newURL, encoding: .utf8)
        let merged = newData + "\n" + oldData
        try merged.write(to: newURL, atomically: true, encoding: .utf8)
        migratedCount += 1
      } catch {
        vCLMLog("Failed to migrate \(type) for \(mode): \(error)")
      }
    }
  }
  return migratedCount
}
```

**3.3.2 `AppDelegate.swift`（兩個倉庫）— `updateDirectoryMonitorPath()` 增加合併提示**：

```swift
// 變更後：updateDirectoryMonitorPath()
public func updateDirectoryMonitorPath(oldPath: String? = nil) {
  let newPath = LMMgr.dataFolderPath(isDefaultFolder: false)
  folderMonitor.stopMonitoring()
  folderMonitor = FolderMonitor(url: URL(fileURLWithPath: newPath))
  folderMonitor.folderDidChange = { [weak self] in
    guard let this = self else { return }
    this.reloadOnFolderChangeHappens()
  }
  if LMMgr.userDataFolderExists {
    folderMonitor.startMonitoring()
    reloadOnFolderChangeHappens(forced: true)
  }

  // 若從舊目錄切換過來，提示使用者是否合併舊資料至新目錄。
  if let oldPath = oldPath,
     oldPath != newPath,
     FileManager.default.fileExists(atPath: oldPath) {
    let alert = NSAlert()
    alert.messageText = "Migrate User Phrases?".i18n
    alert.informativeText = String(
      format: "Would you like to merge user data from the previous location into the new one?".i18n
    )
    alert.addButton(withTitle: "Merge".i18n)
    alert.addButton(withTitle: "Skip".i18n)
    if alert.runModal() == .alertFirstButtonReturn {
      let count = LMMgr.migrateUserDataFrom(oldPath: oldPath, to: newPath)
      if count > 0 {
        LMMgr.initUserLangModels()
        Notifier.notify(message: String(format: "%d file(s) merged.".i18n, count))
      }
    }
  }
}
```

**3.3.3 Settings UI（三個 setting call site）— 傳入舊路徑**：

所有設定 `userDataFolderSpecified` 的 call site 改為先記錄舊值再呼叫 `updateDirectoryMonitorPath(oldPath:)`：

```swift
// 變更前（VwrSettingsPaneCocoaDictionary.swift / VwrSettingsPaneDictionary.swift）
if LMMgr.checkIfSpecifiedUserDataFolderValid(newPath) {
  PrefMgr.shared.userDataFolderSpecified = newPath
  BookmarkManager.shared.saveBookmark(for: url)
  AppDelegate.shared.updateDirectoryMonitorPath()
  // ...
}

// 變更後
if LMMgr.checkIfSpecifiedUserDataFolderValid(newPath) {
  let oldPath = LMMgr.dataFolderPath(isDefaultFolder: false)
  PrefMgr.shared.userDataFolderSpecified = newPath
  BookmarkManager.shared.saveBookmark(for: url)
  AppDelegate.shared.updateDirectoryMonitorPath(oldPath: oldPath)
  // ...
}
```

### 四、影響範圍與注意事項

1. **`LMCoreEX.open()` 行為變更**：原本若已載入資料則不做事（caller 必須先 `clear()`）。變更後 `open()` 會自動清除後再載入。對現有所有 call site 無負面影響（現有 call site 皆已在呼叫 `open()` 前呼叫 `clear()`，`clear()` 的重複呼叫是冪等的）。

2. **`purgeUserData()` 不處理 POM 資料**：`lxPerceptor`（POM）的資料儲存位置使用 `isDefaultFolder: true`，不受使用者自訂目錄影響。因此 `purgeUserData()` 不包含 POM 清除。

3. **`purgeUserData()` 不處理原廠辭典**：`factoryTrie` 是原廠辭典（TextMap），與使用者目錄無關，不應清除。

4. **`LMCoreEX` 的 value type 特性**：`LMCoreEX` 是 struct（值型別），`clear()` 會完全重置其內部狀態。由於它被 `LMInstantiator`（class）持有，`clear()` 的 mutation 發生在 `LMInstantiator` 的 stored property 上。現有所有 mutation 皆在 main actor 或 `fileHandleQueue` 上進行。`purgeUserData()` 應在 main actor 上呼叫。

5. **向後相容性**：`updateDirectoryMonitorPath()` 的簽章變更（新增選擇性參數 `oldPath`）對現有 Swift 呼叫者為 source-compatible（選擇性參數預設 nil）。

6. **多 InputMode 的 purge 順序**：`initUserLangModels()` 先對所有 mode 執行 `purgeUserData()`，再逐一 `chkUserLMFilesExist`，最後載入。這個順序確保即使某個 mode 的 `chkUserLMFilesExist` 失敗，其他 mode 也不會殘留舊資料。

### 五、測試驗證點

#### 5.1 缺陷 A/B/C 驗證

- 從有資料的目錄切換至空白目錄後，`lmUserPhrases.count`、`lmFiltered.count`、`lmUserSymbols.count`、`lmAssociates.count`、`lmReplacements.count` 應全部為 0。
- 從有資料的目錄切換至不存在／無權限的目錄後，所有子語言模組應為空（`clear()` 被呼叫），且不應 crash。
- `unigramLRUCache` 在目錄切換後應為空。
- `inputTokenHashesArray` 在目錄切換後應為空。
- 切換目錄後立即進行查詢，不應返回舊目錄的資料。

#### 5.2 關聯詞語／語彙置換啟停切換驗證

- 舊目錄有 associated phrases 資料 → 停用 associated phrases → 切換目錄 → `lmAssociates.isLoaded` 應為 false。
- 同上，重新啟用 associated phrases 後應載入的是新目錄的資料（非舊目錄）。
- 語彙置換同理。

#### 5.3 缺陷 D 驗證

- 對已載入資料的 `LMCoreEX` 直接呼叫 `open(newPath)`（不先 `clear()`），應成功載入新資料（`isLoaded` 為 true 且內容為新資料）。

#### 5.4 既有測試不得退化

- `vChewing-macOS` LangModelAssembly 全部測試 PASS
- `vChewing-macOS` Typewriter 全部測試 PASS
- `vChewing-macOS` MainAssembly4Darwin 全部測試 PASS
- `vChewing-OSX-Legacy` `make debug-core` BUILD SUCCEEDED
- LibVanguard 全部測試 PASS

#### 5.5 合併提示驗證

- 從舊目錄切換至新目錄時，應彈出對話框詢問是否合併。
- 點擊「Merge」後，新目錄的檔案應包含舊目錄的內容（append 在後）。
- 點擊「Skip」後，新目錄的檔案內容不變。
- 合併後應自動重新載入（`initUserLangModels`）。

## Phase 73 實作結果（2026-05-16, Deepseek-v4-Pro）

### 核心設計

**缺陷 A+B+C**：新增 `LMInstantiator.purgeUserData()` 統一清除入口，並在 `initUserLangModels()` 中對所有 InputMode 無條件呼叫。

- `purgeUserData()` 清除全部五個子語言模組（`lmUserPhrases`、`lmFiltered`、`lmUserSymbols`、`lmAssociates`、`lmReplacements`）、`unigramLRUCache` 與 `inputTokenHashesArray`。
- 五個 `load*` 方法（`loadUserPhrasesData`、`loadUserSymbolData`、`loadUserAssociatesData`、`loadReplacementsData`、`reloadUserFilterDirectly`）的 `clear()` 呼叫從 `if isReadableFile` 區塊內移至方法開頭無條件執行，確保即使新目錄檔案無法讀取也清除舊資料。
- `initUserLangModels()` 改為先 `purgeUserData()` → `chkUserLMFilesExist` → 載入，並總是呼叫 `loadUserPhraseReplacement()`。

**缺陷 D**：`LMCoreEX.open()` 將 `if isLoaded { return false }` 改為 `if isLoaded { clear() }`，提供縱深防禦。

**延伸增強**：
- `LMMgr_Utilities.userDictDataURL` 新增 `basePath` 參數（向後相容）。
- `LMMgr_Utilities.migrateUserDataFrom(oldPath:to:)` 將舊目錄所有語彙檔案 append 至新目錄對應檔案。
- `AppDelegate.updateDirectoryMonitorPath()` 不再負責合併提示。合併提示邏輯移至 Settings 層，以 sheet（Cocoa）或 `.alert()`（SwiftUI）形式吸附在設定視窗上。
- 六個 Settings UI call site（含 reset button）改為呼叫 `updateDirectoryMonitorPath()` 後各自觸發合併提示，確保 browse / drag-drop / reset 三種目錄變更路徑都會彈出合併對話框。
- Cocoa Settings 的 `maybePromptToMergeUserData(oldPath:)` 使用 `alert.beginSheetModal(at: CtlSettingsCocoa.shared?.window)`；SwiftUI Settings 使用 `@State` + `.alert()` modifier。
- Reset button（Cocoa `↻` / SwiftUI `↻`）現在也會在 reset 後檢查新舊路徑差異並觸發合併提示。
- 八個 `.strings` 檔案（macOS + Legacy × 4 語系）補齊六個新 i18n 鍵值（格式：`i18n:settings.dictionary.mergeUserDataToNewTarget.*`，含 `noFilesMigrated`）並以 `LocalizableFileSorter.swift` 重新排序。
- i18n keys 採用結構化命名（`i18n:settings.dictionary.mergeUserDataToNewTarget.{prompt,button,notification}.*`），取代最初使用的 bare English 字串。

### 程式碼變更

| 檔案 | 變更 |
|------|------|
| `LMAssembly/LMInstantiator.swift` (macOS + Legacy) | 新增 `purgeUserData()`；五個 `load*` 方法的 `clear()` 移至開頭無條件執行 |
| `LMAssembly/SubLMs/lmCoreEX.swift` (macOS + Legacy) | `open()` 改為 `if isLoaded { clear() }` 取代 `return false` |
| `LangModelManager/LMMgr_Core.swift` (macOS + Legacy) | `initUserLangModels()` 先無條件 `purgeUserData()`，總是呼叫 `loadUserPhraseReplacement()` |
| `AppDelegate.swift` (macOS + Legacy) | `updateDirectoryMonitorPath(oldPath:)` → `updateDirectoryMonitorPath()`：移除合併提示，回歸純監控／重載職責 |
| `SettingsCocoa/VwrSettingsPaneCocoaDictionary.swift` (macOS + Legacy) | 三處 call site 改為 `updateDirectoryMonitorPath()` + `maybePromptToMergeUserData(oldPath:)`；新增 sheet-based 合併提示 helper |
| `SettingsUI/VwrSettingsPaneDictionary.swift` (macOS) | 兩處 call site 改為 `updateDirectoryMonitorPath()` + `maybePromptMergeInSwiftUI(oldPath:)`；reset button 補上 `updateDirectoryMonitorPath()` 呼叫；新增 `@State` + `.alert()` 合併提示 |
| `LangModelManager/LMMgr_Utilities.swift` (macOS + Legacy) | `userDictDataURL` 新增 `basePath` 參數；新增 `migrateUserDataFrom(oldPath:to:)` |
| `Resources/{en,zh-Hant,zh-Hans,ja}.lproj/Localizable.strings` (macOS + Legacy) | 新增六個結構化 i18n 鍵值（含 `noFilesMigrated`），移除 bare English keys，以 `LocalizableFileSorter.swift` 排序 |

### 設計取捨

- **`clear()` 無條件化後 async 路徑仍保留內部 `clear()`？** 否，async callback 內的 `clear()` 已移除。因 `clear()` 已在方法開頭無條件執行，callback 內的 `clear()` 成為冗餘。
- **`loadUserPhrasesData(type: nil)` 內的條件式保留**：這些條件式仍服務 partial reload 場景（語彙編輯器儲存後僅重載單一類型）。首次清除的責任已移交給 `initUserLangModels()` 內的 `purgeUserData()`。
- **`LMCoreEX.open()` 自動清除 vs 保持原有語義**：原有 `isLoaded { return false }` 是防禦性設計（避免重複載入），但實際上所有 call site 在呼叫 `open()` 前都已經呼叫 `clear()`，所以將 `return false` 改為 `clear()` 不改變現有行為，同時為未來的疏忽提供安全網。
- **`migrateUserDataFrom` 合併策略為 append**：新目錄內容在前、舊目錄內容在後。不做去重，讓使用者自行整理。
- **Reset button 也觸發合併提示**（follow-up fix）：`resetSpecifiedUserDataFolder(_:)` IBAction 與 SwiftUI reset button 在 reset 後呼叫 `updateDirectoryMonitorPath()` + `maybePrompt*`，確保點擊重置按鈕時若新舊路徑不同也會彈出合併對話框。`LMMgr.resetSpecifiedUserDataFolder()` 本身不變（其餘 call site 為 path validation 失敗的 fallback，不應觸發合併提示）。
- **NSAlert 吸附在 Settings 視窗**（follow-up fix v2）：合併提示從 `AppDelegate` 的 `runModal()`（standalone）移至 Settings 層。Cocoa 使用 `beginSheetModal(at: CtlSettingsCocoa.shared?.window)` 吸附視窗；SwiftUI 使用 `.alert()` modifier。NSOpenPanel 為 sheet 且其 completion handler 在 panel dismiss 後觸發，因此合併提示自然在 panel 消失後出現。
- **新目錄無檔案時的 race 防禦**（follow-up fix v3）：`maybePromptToMergeUserData` / `maybePromptMergeInSwiftUI` 在顯示 prompt 前主動呼叫 `LMMgr.chkUserLMFilesExist` 確保 template 檔案已就緒，消除 `updateDirectoryMonitorPath` 內 `asyncOnMain(after: 0.1)` 造成的 100ms race window。另在 `migrateUserDataFrom` 回傳 0 時（新目錄不可寫等）透過 `Notifier.notify` 顯示 `noFilesMigrated` 提示，避免靜默失敗。

### 驗證結果

- vChewing-macOS LangModelAssembly **108/108 PASS**
- vChewing-macOS Typewriter **81/81 PASS**
- vChewing-macOS MainAssembly4Darwin **65/65 PASS**
- 全檔案零 diagnostic error
- Legacy 端 `make debug` **BUILD SUCCEEDED**

--------------------

# Phase 74

Assignee: Deepseek-v4-Pro.

> 與先前一樣：施術之後請恪守 `DevPlans` 目錄下的 KnowledgeMemo 裡面的 Response Pattern 完成 markdown 文件的記錄工作。

## 手術範圍

本次手術同時牽涉到 `vChewing-macOS`、`vChewing-OSX-Legacy` 兩個倉庫。

## 問題現狀

`UserDef.swift` Enum 的 `var metaData` 裡面的 i18n keys 已經有一部分是新格式（`i18n:……`）了，但還有一些是舊格式（bare English）。需要統一為新格式，並補齊缺失的鍵值。這個手術過程也需要同時更新對應的 `.strings` 檔案。手術完畢之後請用 `swift LocalizableFileSorter.swift` 重新排序 `.strings` 檔案。

## 對 LLM 的思考方向約束 & 實際手術方案

LLM (GPT, DeepSeek, Kimi, etc.) 賊喜歡使用 Python 與 csv 等 bash 管道來完成這種任務。但這種任務往往會導致結果失真。這個手術其實交給 DeepSeek 與 Kimi 直接制定手術方案之後都因為上述失真而失敗了，所以 Shiki Suen 認為必須得用 active vibe coding 的方式參與問題的解決。

### 方案架構

1. 在 `vChewing-macOS` 的 `vChewing_Shared` Package 內新增一個臨時 CLI Target `vChewingSharedCLI`（由 Shiki 事後決定移除時機）。
2. 基於 Swift `Mirror` reflection 建立 helper API，自動遍歷 `MetaData` 的所有 localizable 欄位，無須手動列舉屬性。
3. CLI 提供兩個子命令（varArg），按順序使用：

### CLI 子命令規格

#### VarArg Type A：`.strings` 檔案置換

- **子命令名稱**：`convert-strings`
- **輸入**：一個或多個 `.strings` 檔案路徑。
- **行為**：讀取 `UserDef.i18nKeyConvMapTotal`（`[newI18nKey: bareEnglishOldValue]`），對每個檔案內容執行：
  - `"\(bareEnglishOldValue) = " = ` → `"\(newI18nKey)" = `
  - 僅替換 key 側（`=` 左邊），不動 value 側（`=` 右邊的翻譯內容）。
- **輸出**：原地覆寫（或備份後覆寫）。

#### VarArg Type B：待手動修改的 `UserDef` case 清單

- **子命令名稱**：`list-pending-userdef`
- **輸入**：無（無需參數）。
- **行為**：遍歷 `UserDef.allCases`，篩出 `isMetadataPendingManualUpdate == true` 的 case，逐行列印其 `caseName`。
- **用途**：供開發者在最後一步對 `UserDef.swift` 的 `metaData` 進行手動 i18n key 替換時參考。

### 手術執行順序

```
1. swift run vChewingSharedCLI convert-strings <paths-to-.strings-files>
2. swift LocalizableFileSorter.swift          # 對處理過的 .strings 重新排序
3. swift run vChewingSharedCLI list-pending-userdef           # 列出仍需手動處理的 UserDef case
4. 編輯 UserDef.swift：將列出的 case 的 metaData 欄位從 bare English 改為 i18n: 格式
5. 對 vChewing-OSX-Legacy 倉庫重複步驟 1-2（若有獨立的 .strings / UserDef）
```

### 輔助 API 設計（已實作）

| API | 位置 | 用途 |
|-----|------|------|
| `UserDef.i18nKeyConvMap` | `VCSharedCLI_UserDefImpl.swift` | 單一 case 的 `[newKey: oldValue]` 對照表，使用 Mirror 遍歷 MetaData |
| `UserDef.i18nKeyConvMapTotal` | 同上 | 彙總所有 case 的對照表 |
| `UserDef.MetaData.hasFieldPendingManualUpdate` | 同上 | 檢查單一 MetaData 是否仍有 bare English 欄位（Mirror 版） |
| `UserDef.isMetadataPendingManualUpdate` | 同上 | 薄封裝，供外部便捷呼叫 |

## Phase 74 實作結果（2026-05-16, Deepseek-v4-Pro + Shiki Suen）

### 核心設計

採用 **Swift `Mirror` reflection + CLI 工具** 策略取代傳統 Python/bash 管線：

1. 在 `vChewing-macOS` 的 `vChewing_Shared` Package 內新增臨時 CLI Target `vChewingSharedCLI`（`MainActor`-isolated executable）。
2. `VCSharedCLI_UserDefImpl.swift` 提供四個 Mirror-based helper API：
   - `UserDef.i18nKeyConvMap` — 以 `Mirror(reflecting: metaData)` 遍歷所有 `String?` 欄位與 `options` dictionary，自動比對 `newKey != oldString`，產出 `[newI18nKey: bareEnglishOldValue]`。
   - `UserDef.i18nKeyConvMapTotal` — `reduce(into:)` + `merge` 彙總所有 case。
   - `UserDef.MetaData.hasFieldPendingManualUpdate` — Mirror 版 early-exit 檢查。
   - `UserDef.isMetadataPendingManualUpdate` — 薄封裝。
3. CLI 提供三個子命令：
   - `convert-strings`：對 `.strings` 檔案執行 `"\(escapedOldValue)" = ` → `"\(newKey)" = `（保留 ` = ` 錨點，只動 key 側）。
   - `convert-userdef-source`：對 `UserDef.swift` 原始碼執行 `"\(escapedOldValue)"` → `"\(newKey)"`（Swift 字面值替換）。
   - `list-pending-userdef`：列出 `isMetadataPendingManualUpdate == true` 的 case。
4. **escape/unescape 修正**：Mirror 反映的是實際 unescaped 字串值，但 `.strings` / Swift source 中儲存的是 escaped 形態（`\\` → `\`、`\"` → `"`、`\n` → actual newline 等）。新增 `escapeForLiteralSearch()` 將 Mirror 值逆轉為 escaped 形態後再建構搜尋字串，確保含有 `\\`、`"` 等特殊字元的 oldValue（如 `kClassicHaninKeyboardSymbolModeShortcutEnabled` 的 `"Also use "\\" or "¥" key..."`）也能正確命中。

### 程式碼變更

**`vChewing-macOS/Packages/vChewing_Shared/Package.swift`**:
- 新增 `.executable(name: "vChewingSharedCLI", ...)` product
- 新增 `.executableTarget(name: "vChewingSharedCLI", dependencies: ["Shared"])`

**`vChewing-macOS/Packages/vChewing_Shared/Sources/vChewingSharedCLI/VCSharedCLI_main.swift`**（新建）:
- `@main struct VCSharedCLI`：subcommand dispatch（`convert-strings` / `convert-userdef-source` / `list-pending-userdef`）
- `escapeForLiteralSearch(_:)`：將實際值逆轉為 escaped 形態
- `convertStrings(paths:)`：`.strings` key 側置換
- `convertUserDefSource(paths:)`：Swift source 字面值置換
- `listPendingUserDef()`：列印待手動處理的 case

**`vChewing-macOS/Packages/vChewing_Shared/Sources/vChewingSharedCLI/VCSharedCLI_UserDefImpl.swift`**（新建）:
- `UserDef.MetaData.hasFieldPendingManualUpdate`（Mirror 版）
- `UserDef.isMetadataPendingManualUpdate`
- `UserDef.i18nKeyConvMap`（Mirror 遍歷 MetaData，含 options 排序修正）
- `UserDef.i18nKeyConvMapTotal`（`reduce(into:)` + `merge`）

**`.strings` 檔案**（4 個 IME locale × 4 語言）:
- `Sources/vChewingIME_macOS/Resources/{en,zh-Hans,zh-Hant,ja}.lproj/Localizable.strings`
- 每個檔案轉換 127 個 key（bare English → `i18n:` 格式）

**`vChewing-macOS/Packages/vChewing_Shared/Sources/Shared/UserDef/UserDef.swift`**:
- 143 個 bare English string literal → `i18n:` key（含 `kClassicHaninKeyboardSymbolModeShortcutEnabled` 的 escape 修正）

### 設計取捨

- **Mirror vs 手展欄位**：Mirror 自動涵蓋未來新增的 `MetaData` 欄位，但丟失編譯期型別安全；因 CLI 為臨時工具，取 Mirror 的簡潔性。
- **`options` 迭代順序**：原版手展程式碼以 `values.enumerated()` 迭代 dictionary（無序），Mirror 版改為 `keys.sorted()`，確保選項索引穩定。
- **escapeForLiteralSearch**：`.strings` 與 Swift source 共用同一 escape 規則（`\\`、`\"`、`\n`、`\t`、`\r`），合併為單一 helper。

### 驗證結果

- ✅ `swift build --target vChewingSharedCLI` 編譯通過
- ✅ `convert-strings` ×8 `.strings` 檔案：IME 4×127 keys、Installer 4×0（無需變更）
- ✅ `convert-userdef-source` ×1 `UserDef.swift`：143 literals
- ✅ `list-pending-userdef`：0 pending（全部清零）
- ✅ `swift build`（整個 Shared package）編譯通過
- ✅ `swift LocalizableFileSorter.swift` 排序通過

### Post-Mortem 修復

**問題**：`SessionCtl_Menu.swift` 中直接使用 bare English string + `.i18n`（如 `"Per-Char Select Mode".i18n`），但在 `.strings` 檔案中這些 key 已全被轉為 `i18n:` 格式，`NSLocalizedString` 找不到對應翻譯，fallback 回 bare English，導致非英語 locale 下輸入法選單顯示英文。

**修法**：將 `SessionCtl_Menu.swift`（macOS + Legacy）中 11 處 bare English `.i18n` 替換為對應的 i18n key。

**受影響的 key 對照**：

| Bare English（舊） | i18n Key（新） |
|---|---|
| `"Per-Char Select Mode"` | `"i18n:UserDef.kUsingHotKeySCPC.shortTitle"` |
| `"Associated Phrases"` | `"i18n:UserDef.kUsingHotKeyAssociates.shortTitle"` |
| `"CIN Cassette Mode"` | `"i18n:UserDef.kUsingHotKeyRevLookup.shortTitle"` |
| `"CNS11643 Mode"` | `"i18n:UserDef.kUsingHotKeyCNS.shortTitle"` |
| `"Force KangXi Writing"` | `"i18n:UserDef.kUsingHotKeyKangXi.shortTitle"` |
| `"JIS Shinjitai Output"` | `"i18n:UserDef.kUsingHotKeyHalfWidthASCII.shortTitle"` |
| `"Currency Numeral Output"` | `"i18n:UserDef.kUsingHotKeyCassette.shortTitle"` |
| `"Half-Width Punctuation Mode"` | `"i18n:UserDef.kUsingHotKeyCurrencyNumerals.shortTitle"` |
| `"Reverse Lookup (Phonabets)"` | `"i18n:UserDef.kUsingHotKeyJIS.shortTitle"` |
| `"Symbol & Emoji Input"` | `"i18n:UserDef.kSymbolInputEnabled.shortTitle"` |
| `"Use Phrase Replacement"` | `"i18n:UserDef.kPhraseReplacementEnabled.shortTitle"` |

- ✅ `vChewing-macOS` `swift build` 通過
- ✅ `vChewing-OSX-legacy` `make debug-core` BUILD SUCCEEDED

--------------

## Phase 74 延伸：非 UserDef bare English i18n key 全面遷移（2026-05-18, Deepseek-v4-Pro + Kimi For Coding）

### 一、問題現狀

前述 Phase 74 主體已將全部與 `UserDef` enum 直接相關的 i18n key（共 127 keys × 4 locales）從 bare English 遷移至 `i18n:` 格式。但經清查，被 Phase 74 修改過的 `.strings` 檔案中尚有 **240 個 IME key + 23 個 Installer key** 仍為 bare English 舊格式，對應程式碼中約 219 處 `.i18n` 呼叫點（~126 unique 字串）。這些 key 不隸屬於 `UserDef` 體系，散布於 IME 選單、工具提示、鍵盤佈局、符號分類、設定面板、片語編輯器、安裝程式等十餘個上下文領域。

Phase 74 主體之所以能全自動化，在於 `UserDef.MetaData` 可透過 Swift `Mirror` reflection 程式化推導新舊 key 的對照——但此方法對這些非 UserDef 的 key 完全無效。本 Phase 的目標是建立一套可複用的轉換框架，以顯式映射表取代 Mirror，將所有剩餘的 bare English key 遷移至 `i18n:` 格式。

### 二、命名規範

為這 263 個 key 設計了 **21 個領域命名空間**，遵循三條核心原則：

- **上下文歸屬**：依 key 在程式碼中的實際用途分配命名空間（例：標記狀態下的加詞提示 → `i18n:StateOfMarking.Tooltip.*`）。
- **既有慣例延續**：既有的 dot-separated key（如 `typingMethod.romanNumerals.error.invalidCharacter`）保留層級結構，僅加 `i18n:` 前綴。既有的 `cat*` / `the*` 前綴剝離後歸入對應空間。
- **格式說明符後綴**：若翻譯值含 `%@` 或 `%d`，key 名末尾以 `:%@`、`:%@%d` 等標示參數序列，延續 `i18n:settings.importFromKimoTxt.finishedCount:%@%@`（Phase 74 主體引入）的既有慣例。

| 命名空間 | 數量 | 適用範圍 |
|----------|------|----------|
| `i18n:StateOfMarking.Tooltip.*` | 6 | 標記狀態下的工具提示（加詞長度檢查、選取提示等） |
| `i18n:StateOfInputting.Tooltip.*` | 8 | 輸入狀態下的工具提示（聲調覆寫、注音合併等） |
| `i18n:PhraseOperation.*` | 11 | 片語操作回饋（boost/nerf/filter 成功／失敗） |
| `i18n:DictionaryStatus.*` | 5 | 辭典載入狀態 |
| `i18n:UpdateNotification.*` | 4 | 版本檢查與更新通知 |
| `i18n:NotificationSwitch.*` | 3 | 通知開關狀態（ON/OFF/Revolver） |
| `i18n:InputMode.*` | 2 | 輸入模式切換 |
| `i18n:Menu.*` | 29 | IME 選單項目 |
| `i18n:KeyboardLayout.*` | 18 | 鍵盤佈局名稱 |
| `i18n:TypingMethod.*` | 10 | 拼音／注音方法名稱 |
| `i18n:SymbolCategory.*` | 25 | 符號分類名稱 |
| `i18n:LanguageName.*` | 8 | 語言名稱與代碼 |
| `i18n:Common.*` | 25 | 通用 UI 按鈕／標籤 |
| `i18n:Settings.*` | 21 | 設定視窗 UI |
| `i18n:PhraseEditor.*` | 16 | 片語編輯器 UI |
| `i18n:ClientManager.*` | 10 | 客戶端管理員 UI |
| `i18n:ErrorMessage.*` | 14 | 錯誤訊息 |
| `i18n:InfoMessage.*` | 24 | 資訊性訊息 |
| `i18n:Installer.*` | 21 | 安裝程式 UI |
| `i18n:Uninstaller.*` | 1 | 卸除程式 UI |
| `i18n:IME.*` | 2 | IME 其他雜項 |

### 三、轉換策略

擴充 Phase 74 主體建立的 `vChewingSharedCLI` 臨時工具，新增兩項能力：

1. **`VCSharedCLI_NonUserDefI18nMap.swift`**：263 項 `[bareEnglish: i18nKey]` 顯式映射 dictionary，以字串長度降冪排序避免部分匹配碰撞。

2. **兩個新子命令**：
   - `convert-bare-keys` — 對 `.strings` 檔案執行 `"bareEnglish" = ` → `"i18n:…" = ` 的 key 側置換，保留 value 側不變。透過 `escapeForLiteralSearch(_:)` 處理 `"`、`\`、`
` 等轉義序列。
   - `convert-bare-keys-in-source` — 對 Swift 原始碼執行 `"bareEnglish".i18n` → `"i18n:…".i18n` 的 literal 替換。以 `.i18n` 後綴為錨點，避免誤傷普通字面值。

### 四、執行過程與發現的遺漏

#### 4.1 首輪轉換（Deepseek-v4-Pro）

```
1. convert-bare-keys ×8 .strings（IME 4 locale + Installer 4 locale）→ IME 240 / Installer 23 key 全數轉換
2. swift LocalizableFileSorter.swift 排序
3. convert-bare-keys-in-source ×~45 Swift 檔案 → ~170+ literal 替換
4. swift build 驗證編譯通過
5. Legacy 倉庫同步（47 檔案 ~170+ literal，make debug-core 通過）
```

#### 4.2 格式說明符後綴補齊

16 個含 `%@`/`%d` 的 key 在首輪轉換中未附加 `:%@`/`:%d` 後綴。經全倉庫 `grep -E '=.*%[@d]'` 掃描後補齊（含既有 `i18n:DevZone.JSONPrefsExchange.ImportSummary` 與 `i18n:settings.dictionary.mergeUserDataToNewTarget.notification.filesMerged`），雙倉庫同步。

#### 4.3 稽核與修補（Kimi For Coding）

對首輪轉換結果進行全倉庫 `grep -rE '"[A-Z].*"\.i18n'` 掃描 + 編譯驗證，發現下列遺漏：

| # | 問題 | 影響範圍 | 根因 |
|---|------|----------|------|
| 1 | 5 處 bare English `.i18n` call site 未被轉換：`"Move Up"`、`"Move Down"`、`"CIN Cassette Mode"`、`"Service Menu"`、`"Reverse Lookup (Phonabets)"` | macOS + Legacy | `"Move Up/Down"` 從未在 `.strings` 存在對應條目，屬映射表遺漏；`"CIN Cassette Mode"/"Reverse Lookup"` 在 Phase 74 已轉 `i18n:UserDef.*`，但 `convert-bare-keys-in-source` 以 commit 前狀態為基準未命中 |
| 2 | 4 處既有 `i18n:` key 的 source call site 未更新：`InvalidAppBundle`、`CompatibilityNote`、`EndOfIMKCandidatesExplanation`、`DragTargetInstruction` | macOS + Legacy | 對應 `.swift` 檔案未被納入 `convert-bare-keys-in-source` 掃描批次 |
| 3 | zh-Hans `.strings` 第 1 行 bare English key 殘留，且 `kConsolidateContextOnCandidateSelection.description` 的 value 被錯寫為英文原文 | macOS + Legacy | 超長 key 的置換可能因換行／escape 差異失敗 |
| 4 | Installer `InstallerShared.swift` 語法損壞（unterminated string literal + brace mismatch）；`InstallerApp4SwiftUI.swift` 型別錯誤（`String` 傳入 `LocalizedStringKey` 參數） | macOS | 純文字替換工具不具備語法分析能力 |

#### 4.4 修復措施

- **既有 key 重新映射**：`"CIN Cassette Mode"` → `i18n:UserDef.kUsingHotKeyRevLookup.shortTitle`、`"Reverse Lookup (Phonabets)"` → `i18n:UserDef.kUsingHotKeyJIS.shortTitle`、`"The selected item is either not a valid…"` → `i18n:ErrorMessage.InvalidAppBundle` 等 4 處。
- **新增 5 個 `i18n:` key**：`i18n:Common.MoveUp`、`i18n:Common.MoveDown`、`i18n:Menu.ServiceMenu`、`i18n:Common.Add`、`i18n:ClientManager.DragTargetInstruction`，寫入全部 4 語系 `.strings` 並同步原始碼。
- **zh-Hans 修復**：刪除殘留舊 key，將正確簡中翻譯寫回新 key 的 value。
- **Installer 語法修復**：清除殘留舊字串，補回遺漏 `}`，修正 `LocalizedStringKey` 型別錯誤。
- **映射表補齊**：`VCSharedCLI_NonUserDefI18nMap.swift` 新增 `"Add": "i18n:Common.Add"`。
- 執行 `swift LocalizableFileSorter.swift` 重新排序。

### 五、最終狀態

| 指標 | macOS | Legacy |
|------|-------|--------|
| IME `Localizable.strings` (en) 總 key 數 | **542** | **542** |
| Installer `Localizable.strings` (en) 總 key 數 | 37 | 37 |
| Bare English key 殘留 | 0 | 0 |
| 原始碼 `"bareEnglish".i18n` 殘留 | 0 | 0 |
| 格式鍵 `:%@/%d` 遺漏 | 0 | 0 |
| `swift build` / `make debug-core` | ✅ | ✅ |
| Phase 74 主體 Post-Mortem 完整性 | ✅ | ✅ |
| `"rawValue".i18n` bug | ✅ 已修復 | ✅ 已修復 |

### 六、程式碼變更摘要

| 項目 | 內容 |
|------|------|
| **新建** | `VCSharedCLI_NonUserDefI18nMap.swift`（263 項映射表） |
| **擴充** | `VCSharedCLI_main.swift`（`convert-bare-keys` / `convert-bare-keys-in-source` 子命令） |
| **`.strings` 轉換** | 16 檔案（8 macOS + 8 Legacy）× 全 key 替換 + 重新排序 |
| **Swift 原始碼** | ~50 macOS + ~48 Legacy 檔案，~350+ 處 literal 替換 |
| **新增 key** | `Common.MoveUp/Down/Add`、`Menu.ServiceMenu`、`ClientManager.DragTargetInstruction`（5 個） |
| **修復** | Installer 語法損壞 2 處、zh-Hans key/value 雙錯 1 處、映射表缺項 1 處 |

### 七、後續建議

1. 大規模 i18n 遷移後應追加 `grep -rE '"[A-Z].*"\.i18n'` 全倉庫掃描作為最後防線。
2. 對 Installer 等獨立 bundle 的原始碼應獨立掃描，避免僅掃描 IME 目錄而遺漏。
3. 涉及多行字串或型別特定參數的替換，必須人工逐行覆核。
