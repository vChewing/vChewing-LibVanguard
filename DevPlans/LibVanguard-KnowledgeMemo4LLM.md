# LibVanguard - 格物致知專用文件

> 本文檔供 AI Agent 在每次開工前迅速了解專案全貌。
> 最後更新：2026-09-05（Phase 192 完工——**POM 引擎注入禁止跨聲調（客訴：打「有」出「右」／「體式」首候選，兩倉）**。v4.7.3 注音客訴：打「時」首候選「體式」、打「有」首候選「右」；清空 POM 即癒；v4.7.2 無；確認 v4.7.3 已含 P182/P183。根因：引擎注入 `perceptionsFor` 預設 `.toneInsensitivePrefix`（逐段去聲調）→ 帶調查詢（有ㄧㄡˇ／時ㄕˊ）撈回同音異調記憶（右ㄧㄡˋ／式ㄕˋ）；注入 gram keyArray（記憶自身調）與節點鍵不符仍可能被 DP 以 reading-mismatch 選中（fresh ≈−0.17 權重）→ 錯調字居首。修復（兩倉 LXPerceptor）：預設改 `.exact`，其語義＝「逐段依 query 段是否帶聲調」——帶調段需與記憶逐字等值（跨調不注入）；無調段（聲調桶代表鍵／前綴 partial）維持去聲調容錯（**不礙狂拼桶與拼音 partial matching**）；`.toneInsensitivePrefix`（狂拼 Typewriter 建議）不變。測試：LMA 原 tone-insensitive 測試反轉為 `ExactToneHeadMatchRequired`（跨調不注入＋同調注入＋無調桶仍容錯）；Typewriter IH508（(是)→右ㄧㄡˋ 不影響「是有」ㄧㄡˇ、同調仍注入）；IH507（狂拼桶 ㄅㄢˇ）維持綠燈。驗證：LangModelAssembly 150/150、Typewriter 161/161、MainAssembly4Darwin 68/68、Legacy debug-core ✅、swiftformat 0、LXPerceptor 鏡像逐字節一致。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0191-0200.md` Phase 192。）
（Phase 192 後續補充——**影片實證「打時出體式」＝錯位記憶套用（第二層守衛，兩倉）**。事主指正：錄影中「體式」出現時前段並未敲 ㄊㄧˇ——單鍵 ㄕˊ 直接組出並送出「體式」；harness 重現＝POM 記憶 `()&()&(ㄕˊ,體式)`（候選字數≠注音 head 段數的**錯位記憶**）於非狂拼單鍵查詢被 Typewriter 建議/套用套上單節點。施術：LXPerceptor 三候選迴圈（exact／tone-insensitive／perceptionsFor）加**注音讀音錯位守衛**（`isZhuyinReading`：Bopomofo 或聲調記號、且候選字數≠head 段數才跳過；ASCII 合成讀音不受影響）——既有髒資料讀取/餵入即忽略、毋需清 POM。測試：LMA `MisalignedCandidateNotFed`、Typewriter **IH509**（shi2→時）。驗證：LangModelAssembly 151/151、Typewriter 162/162、MainAssembly 68/68、Legacy ✅、swiftformat 0、LXPerceptor 鏡像一致。詳見 `DevPlans/Reqs4LLM/Reqs_0191-0200.md` Phase 192 補充。）
（Phase 191 完工——**狂拼啟用時抑制原廠注音文（兩倉）**。詳見 `DevPlans/Reqs4LLM/Reqs_0191-0200.md` Phase 191。）
（Phase 191 完工——**狂拼（`kFuriousTypingEnabled`，預設啟用）開啟時抑制來自原廠辭典（Vanguard TextMapTrie，type 10 zhuyinwen）的注音文資料（兩倉 LMAssembly）**。注音文供應只有單一匯流點 `makeFactoryUnigrams` 的兩 overload，比照既有 kana 抑制模式在 guard 處加 `entryType == .zhuyinwen && config.shouldSuppressFactoryZhuyinwenData` 即全線生效（configured／partial／chopped／strictSuperset 皆涵蓋）；`Config` 新增 `shouldSuppressFactoryZhuyinwenData` 並由 `syncPrefs` 接線 `prefs.furiousTypingEnabled && prefs.pinyinTypingEnabled`（狂拼與拼音打字模式皆啟用時才抑制）。語意：僅抑制原廠注音文、使用者片語／符號不受影響、非狂拼或非拼音模式時原狀；zhuyinwen 不在存在性查詢路徑、無需特判。新增 `LMInstantiator_FuriousZhuyinwenTests`（2 tests，錨點 ㄋㄟ-ㄋㄟ→ㄋㄟㄋㄟ：預設供應／狂拼抑制）。驗證：macOS LangModelAssembly **150/150**（148＋2）、legacy `make debug-core` ✅、swiftlint 無新增違規。LibVanguard／Homa 零改動。詳見 `DevPlans/Reqs4LLM/Reqs_0191-0200.md` Phase 191。）
（Phase 190 完工——**GBEX（GB18030-2022 擴充 C/D/E/F 生僻字）分表＋CNS+GBEX 模式（詞庫／macOS／LibVanguard／legacy 四倉）**。本 Phase 屬多倉複雜施工後的整體總結報告，貫徹目的為使唯音輸入法具備對 GBEX 標準漢字（GB18030-2022 擴充 C/D/E/F 生僻字）的打字能力。VanguardLexicon 收編於 `70f5238`（`char-kanji-gbex.txt` 資料）與 `c1d01d3`（DataBuilder 支援）：與 CNS 分表、`EntryType.gbex=11`、Legacy SQL `theDataGBEX` 欄、字根反查含 GBEX；tiny sample（ENTRY_COUNT 995、含 type-11）重新部署至 macOS 與 LibVanguard（TextMap／Plist／SQLite 同源、測試零崩潰）；macOS：LMInstantiator 於 CNS 開關開啟時補給 CNS＋GBEX——CHT GBEX 在 CNS 後、CHS GBEX 優先——CNS 濾通器仍只認 `.cns`、簡中豁免、權重 -11 與 CNS 相同；`TrieProtocol` 已知 type 集加 11；l10n 四語開關更名 CNS+GBEX；新增 `LMInstantiator_CNSGBEXTests`。legacy：同款語義移植（兩倉 LM 檔已分歧、非 hunk-identical；macOS／legacy commit hash 因兩倉可能隨 submodule／SPM remote URL rebase 而變動、不於此記錄）。LibVanguard 收編於 `ffa79e54`：`gbex=11` 編號就緒、引擎組句層補給排序留待引擎 LM 面對齊本 devreq。驗證：macOS LangModelAssembly 148/148、Typewriter 160/160、LibVanguard TrieKit 18＋LexiconKit 20、legacy `make debug-core` ✅。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 190。）
（Phase 189 後續補充——**path row 長路徑仍爆 `==993/1000`（兩倉 DSL 語意修復）**。事主複測見 `NSPathControl.width == 993` 與 569 欄衝突：`stack()` 補 `≥ intrinsicContentSize` 時 `makeSimpleConstraint` 的 (.equal, .greaterThanOrEqual) 分支把既有 `==` 寬度拉高成 intrinsicContentSize（沙盒短路徑首輪未觸發）。施術：兩倉 `AppKitImpl_NSView.stack()` 對已有自訂 == 寬/高之子視圖不再補 ≥ intrinsic（附 one-line 註解）；`CtlSettingsCocoa.selectTab` 改 public（兩倉）＋harness 新增遠端翻頁 `vchewingdbg://page/<tab>`（macOS）。複測：zh-Hant＋English 兩輪各翻 12 SettingsCocoa tab、stderr 零衝突（telemetry 24 次 page）；兩倉 build ✅、swiftformat 0 檔。commit 未提交（待事主指示）。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 189。）
（Phase 189 收尾補充——**About 頁寬度修正＋驗證管道更正**。Xcode 見 About 紫色警告：About 頂列 `[banner 63][8pt 行距][文字欄]` 釘 `==contentWidth`，文字欄卻寫死 `contentWidth−63`（eulaBox＋四 label）→ 總寬 577>569、漏扣 8pt 行距；新增 `textColumnWidth = contentWidth−imgWidth−12`、五處改用（兩倉 7+/5-）。**驗證管道更正**：macOS 26/27 衝突輸出走 os_log `com.apple.runtime-issues:AppKit`（非 Xcode 啟動對第三者 `<private>` 遮蔽）、不走 stderr——先前 stderr「零衝突」為盲測作廢；改 `log show` 計數重驗 zh/en × 12 SettingsCocoa tab 顯示期間 0 衝突（未修 About 一翻即 5 筆）。殘留：每關設定窗 ~1 筆 teardown 衝突（非 About 專屬、良性，事主同意結案）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 189。）
（Phase 188 完工——**SettingsUI 體系新增「就地加詞」指引列（兩倉）**。GitHub 工單 #607 使用者抱怨「用詞彙編輯介面＋注音反查手動建詞數月、不知就地加詞」→ 於 SettingsUI 體系詞彙編輯頁（SwiftUI PhraseEditorUI〔僅 macOS 倉、macOS 14+；事主先行樣張為底、label 改 `.i18n` house style〕＋兩倉 SettingsCocoa `SettingsPanesCocoa.Phrases` 鏡像 hunk）頂端新增 tip row：左 `YouMayAddPhrasesInPlace.shortTitle`、右 `Common.MoreIntel`「更多資訊…」鈕→`callAlert` 彈 `.explain` 新手教學（口徑同 runtime marking tooltip：組字時 Shift＋流向方向鍵〔直書客體 ↑/↓〕標記≥2 字→放開後就地提示→Enter 加詞；既有詞升/降權/排除）。新增 i18n key ×3 ×4 語 ×8 檔（ja 依 ja 語彙自擬）；Hant「點此了解更多」較 spec簡體「点此…」更正為正確繁體。另修 aaf8c638 回歸：`VwrPhraseEditorUI.txtContentStorage` 誤改 `private static`、致 CtlSettingsUI 跨檔清理無法編譯→回復 internal（P186 保留 static 語義不變）。Cocoa tip row 採 10.9-safe 建構（deployment 10.9：不用 `labelWithString:`／`NSControl.controlSize`）。驗證：macOS SettingsUI build ✅、Legacy debug-core（10.9、Xcode-15）BUILD SUCCEEDED ✅、兩倉 Cocoa pane 鏡像 diff 僅 #Preview tail、l10n 8 檔 plutil OK、lintFormatUncommitted收斂（macOS 6 檔／legacy 5 檔）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 188。）
（Phase 187 完工——**Installer postflight 更新：安裝完畢後強制結束 imklaunchagent／TextInputSwitcher／TextInputMenuAgent（兩倉）**。手術對象為兩倉各自的「pkg installer」與「App-bundle-based Installer」：安裝流程在輸入法註冊完成之後追加強制結束文字輸入系統 agent 程序之步驟，使部分新電腦安裝完不必重新登入即可啟用（不改任何安裝介面文字／L10n）。App-bundle-based Installer——兩倉 `InstallerVMProtocol.swift`（鏡像逐字節一致）於 `installInputMethod` 的註冊＋啟用迴圈後、提示面板前新增 `terminateTextInputSystemAgents()`（SwiftUI／Cocoa 兩 flavor 皆走協定延伸預設實作、單檔即覆蓋），依序對 `imklaunchagent`／`TextInputSwitcher`／`TextInputMenuAgent` 執行 `/usr/bin/killall`、不檢查結束狀態（舊版 macOS 或未啟動時可能不存在）。pkg installer postflight——macOS `ValueAdd/PKGInstallerAssets/pkgPostInstall.sh`（註冊完成後、`exit 0` 前）與 legacy `Installer/pkgPostInstall.sh`（`install --all` 註冊列後）追加同款 killall；root 時經 `su - ${login_user} -c` 以 console user 執行、非 root 直接執行、`2>/dev/null || true` 冪等（agent 屬 console user 的 GUI session）。驗證：macOS `vChewing_InstallerAssembly4Darwin` 套件 swift build ✅、legacy `make debug`（vChewingInstallerLegacy scheme、deployment target 10.9）BUILD SUCCEEDED ✅、兩倉 `make lintFormatUncommitted` 收斂後 git status 逐倉僅含手術檔（各 2）、手術後兩倉鏡像逐字節一致 ✅、pkg 兩腳本 `sh -n` ✅。commit 未提交（待事主指示）。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 187。）
（Phase 187 後續補充——**terminateTextInputSystemAgents 改以 GCD 延後半秒執行（兩倉）**。事主回報同步執行會導致安裝失敗——註冊／啟用流程內立刻殺掉文字輸入系統 agent 恐打斷輸入法啟用交握、且 killall 同步 `waitUntilExit` 卡住主執行緒。修正：該方法改以實例 `taskQueue` 之 `asyncAfter(deadline: .now() + 0.5)` 延後半秒於背景執行（agent 清單先取為區域常數 `agentNames`、閉包不捕獲 self／不觸 MainActor 隔離狀態），讓註冊／啟用→結果提示→使用者確認的流程先完成、agent 再被結束並重新載入輸入法清單；兩倉鏡像逐字節一致（+12/−8）。驗證：macOS `vChewing_InstallerAssembly4Darwin` swift build ✅、legacy `make debug` BUILD SUCCEEDED ✅、兩倉 lintFormatUncommitted 收斂、鏡像逐字節一致 ✅。commit 未提交（待事主指示）。pkg postflight shell 流程與 GUI session 解耦、無此問題、未更動。）
（Phase 187 後續補充（2026-09-05，仍歸 P187）——**安裝程式 agent 結束行為收斂：僅殺滅 TextInputMenuAgent（兩倉，amend 既有 commit）**。事主指示安裝流程的 agent 結束行為改為僅處理 TextInputMenuAgent、不再結束 imklaunchagent／TextInputSwitcher（原因：結束 imklaunchagent 會讓正在接收文字輸入的客體 App 失去與所有輸入法的對接、須重啟該客體 App 才恢復；TextInputSwitcher 只是純 UI 套件、結束無效——TextInputMenuAgent 重載即足以刷新輸入法清單；已加註於程式註解防回歸）。App-bundle Installer：`terminateTextInputSystemAgents()` 移除 agentNames 清單與 forEach、直接對 TextInputMenuAgent 執行 `/usr/bin/killall`（taskQueue 延後半秒、不檢查結束狀態維持）；pkg 兩腳本 killall 參數同步收斂為 TextInputMenuAgent（root 經 `su - ${login_user} -c`／非 root 直接執行、`2>/dev/null || true` 冪等不變）；註解同步改寫（Swift zh-Hant／shell en）。驗證：macOS `vChewing_InstallerAssembly4Darwin` swift build ✅、legacy `make debug`（vChewingInstallerLegacy）BUILD SUCCEEDED ✅、兩倉 lintFormatUncommitted 收斂、兩倉 InstallerVMProtocol.swift 鏡像逐字節一致 ✅、pkg 兩腳本 `sh -n` ✅、除註解內說明性提及外零可執行處理。commit：兩倉已各 amend（原 `af72c561`／`2e7c5aaa` 改寫為 macOS `8489c1e9`／legacy `880e7068`、原訊息與日期保留、未 push）。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 187。）
（Phase 187 收尾補充（2026-09-05，仍歸 P187）——**kill 挪至 register 之前＋register 併入主執行緒延後半秒（兩倉，含 pkg postflight；事主實測無副作用後 amend 定案）**。事主追加指示：TextInputMenuAgent 的殺滅須發生在輸入源啟用（register）之前，register 得等 kill 結束（無論成敗）後再延後半秒才執行。App-bundle Installer：`terminateTextInputSystemAgents()` 改同步（killall TextInputMenuAgent＋waitUntilExit、不檢查結束狀態、不再走 taskQueue）；`var mainInputSourceEnabled` 起至 `// 提示面板` 前的啟用迴圈整段移入提示面板主執行緒區塊、該區塊 `DispatchQueue.main.async` 改 `asyncAfter(deadline: .now() + 0.5)`——kill → 0.5s → register＋結果提示依序完成。pkg postflight（兩倉 shell）：kill 區塊移至 register 前並加 `sleep 0.5`。註解同步（Swift zh-Hant／shell en，含僅殺 TextInputMenuAgent 之原因）。驗證：macOS InstallerAssembly4Darwin swift build ✅、legacy make debug（vChewingInstallerLegacy）BUILD SUCCEEDED ✅、兩倉 lintFormatUncommitted 收斂、鏡像逐字節一致 ✅、pkg 兩腳本 `sh -n` ✅、事主實測無副作用 ✅。commit：兩倉已再 amend 定案（hash 鏈 macOS `af72c561`→`8489c1e9`→`f3243282`、legacy `2e7c5aaa`→`880e7068`→`fd77cfdd`；原訊息與日期保留）。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 187。）
（Phase 186 完工——**偏好設定視窗記憶體：解決 vChewing 可自行控制的內容放大因子（兩倉）**。Phase 185 術後報告（`DevPlans/Research/Phase186_ResearchAndReport-SettingsUIRAMFootprint.md` §5-H3／§7-M2/M3）定位：真實 IME 關窗後「>50MB 淨浪費」主體屬框架／allocator 層（H1/H2、非 vChewing 可控制），可自行控制者為 H3 內容生命週期兩項。施術：① **SettingsCocoa（AppKit，兩倉）**——`SettingsPanesCocoa.preload()` 移除 `ctlPagePhrases.loadView()`（比照 About 既有排除前例）：開窗不再把目前輸入模式完整詞庫全文 `retrieveData` 進 NSTextView 並保留至關窗，改為首次造訪 Phrases 頁（`viewWillAppear → initPhraseEditor()`）才載入；`viewWillDisappear` 清空、Broadcaster reload observer、`viewForTab` lazy `.view` 全數維持。已知取捨：Phrases 頁唯一 searchable UserDef（`kPhraseEditorAutoReloadExternalModifications`）改為首訪後才入搜尋索引（與 About 同等待遇）。② **SettingsUI（SwiftUI，僅 macOS 倉有 PhraseEditorUI）**——`VwrPhraseEditorUI.txtContentStorage` **保留 static**（事主 P.S.：保留前提＝及時清理成立），`CtlSettingsUI.close()`／`windowWillClose(_:)` 兩條關窗路徑抽離 contentView 後確定性清空該程序級文字暫存，釋放不再依賴 SwiftUI `onDisappear` 時序（原 onDisappear 清空保留、雙路徑冪等無害）。排除：H1/H2 框架／allocator、H4/M1（12 頁全量預建＋搜尋索引遷移，事主留待後續）、H5/M6 微項。驗證：macOS SettingsUI package build ✅、MainAssembly4Darwin **68/68**（含 test005_SettingsWindowDoesNotLeak）✅、Legacy `make debug-core` BUILD SUCCEEDED ✅、兩倉 `make lint; make format` 收斂後 git status 逐倉僅含手術檔（macOS 2：VwrSettingsCocoaPanes＋CtlSettingsUI；legacy 1：VwrSettingsCocoaPanes）、preload hunk 兩倉逐字節一致。commit：M2/M3 主手術 macOS `890acd30`／legacy `1e9c2d4e`、harness 改造 macOS `5b8671af`／`5db66377`（事主提交）；bypass 手術與文件補充未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。**後續補充（2026-09-04，仍歸 P186）**：① vChewingDebuggable 改造為 SettingsUI/Cocoa 記憶體診斷宿主（macOS 倉）——砍 pinyin harness、unit-test 沙盒＋原廠辭典開關兩 flavor；AppIcon 入 car 修復（`ASSETCATALOG_COMPILER_APPICON_NAME`＋`CFBundleIconName`）；purge-first internal＋malloc zone telemetry；URL scheme 外部控制＋自律 heap 快照＋log 鏡像；**SwiftUI 多窗 confound 修正**（`WindowGroup`→單一 `Window` scene，baseline 47→121→57.8MB 歸位）。serial 量測結論（見 research doc 附錄）：vChewing-pane 物件≈0（開窗 0.26MB）、開窗成本全屬框架側、關窗 inUse 歸還而 allocated 不回（H2 ratchet）。② `pendingUnitTests` 下 bypass 設定窗強制置前/level（兩倉三檔，包 `orderFrontRegardless`＋`level`＋`NSApp.popup()`、`showWindow` 保留）；驗證三綠。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 186。）
（Phase 185 完工——**copilot 選字窗對 candidate manipulation 完全停止響應（兩倉）**。狂拼 copilot 候選窗（`.ofInputting`＋附掛候選）的 boost/nerf/add-to-filter 三路徑本就不可達（session 端 `isCandidateContextMenuEnabled` 要求 `.ofCandidates`、鍵盤熱鍵 `manipulator:` 區塊亦同），唯一「響應」是右鍵 beep：兩視圖 `rightMouseUp` 的 `singleKanjiCheck` 在總守衛之前就對單字候選 `callError("44E0B7CF"…)`——語境已禁用選單仍先發拒絕聲。施術：`VwrCandidateGSI4AppKit`／`VwrCandidateTDK4AppKit` 於 `singleKanjiCheck` 前新增 `guard delegate.isCandidateContextMenuEnabled else { return }`（`conditions` 移除冗餘首項）——語境層級（copilot／關聯詞／符號表／黑名單客體）禁用操控選單時右鍵一律不響應（不彈選單、不發蜂鳴、不評估 macro-token）；`.ofCandidates` 正常窗行為零改動（含單字＋`kAllowRescoringSingleKanjiCandidates` 關閉的既有拒絕蜂鳴）。鍵盤路徑本就守衛不響應、零改動。兩倉四檔（GSI＋TDK ×2）鏡像 hunk 逐字節一致；右鍵屬 AppKit 視圖事件層、無單元測試靶位（以建置驗證）。驗證：CandidateWindow **20/20**、Typewriter **160/160**、MainAssembly4Darwin **68/68**、Legacy `make debug-core` ✅、兩倉 `make lint; make format` 後 git status 逐倉僅含兩 view 檔（macOS 2／legacy 2）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 185。）
（Phase 184 完工——**選字窗頂部新增「未完成讀音」pane（unfinished reading，狂拼 copilot 候選窗專用，兩倉）**。狂拼 copilot 窗原先以底部字根反查欄位回顯未完成讀音（tooltip 因與候選窗重疊被抑制時的敲鍵唯一可見管道）、缺乏視覺優先性；本 Phase 改於選字窗頂部另闢專用 pane——`CtlCandidateDelegate` 新增 nullable `unfinishedReading`（data provider 單方把守：僅 `isFuriousCopilotCandidateWindowVisible` 且注拼槽有未固化字母流時回傳 `composer.romajiBuffer`、其餘 nil）、`reverseLookup(for:)` 狂拼回顯分支移除（反查欄位回歸一般守衛路徑）；選字窗側 `CandidatePool4AppKit` 新增 `unfinishedReadingResult`＋`attributedDescriptionUnfinishedReading`（文字樣式與 page indicator 一致、前綴「`✍️ `」emoji＋ASCII SPACE、無底色）、`UIMetrics.unfinishedReading` 矩形、`updateMetrics()` 採 shift-last——pane 存在時候選行 cell／高亮矩形／peripherals／disambiguation 矩形整體下移 `paneHeight+padding`、`fittingSize` 加高、寬度放寬至可容納 pane（左右各留 `originDelta`）、撤除即復原（橫向／縱向、單行／多行共用既有幾何路徑、不逐一改寫公式）；兩 controller `updateDisplay()` 自 delegate 餵入 pool、兩視圖 normal-mode `draw` 繪製頂部 pane（GSI scroll mode 不受影響——狂拼窗恆單行不進 scroll、該路徑 pane 資料恆 nil）。兩倉鏡像逐字節一致（除既有 imports／guard 差異）。測試：Typewriter **IH120C／IH121C 改寫**（原「狂拼反查回顯字母流」→鎖定 `unfinishedReading` 資料源：繞過反查總開關與縱排守衛、非狂拼 nil、反查欄位回歸一般守衛路徑）＋TDK4AppKitTests ＋2（pane 隱藏／pane 位移並復原）。驗證：CandidateWindow **20/20**、Typewriter **160/160**、MainAssembly4Darwin **68/68**、Legacy `make debug-core` ✅、兩倉 `make lint; make format` 收斂後 git status 逐倉僅含手術檔（macOS 10 檔、legacy 7 檔）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 184。）
（Phase 183 完工——**狂拼 copilot 跨邊界整詞列舉（兩倉）**。diannaoban 回報：copilot 只剩組句預覽＋尾段 unigrams、「電腦班」類覆蓋前段多鍵節點＋尾段的整詞不現——根因為 `buildFuriousFrontCandidates` 跨邊界查詢僅末鍵（L=1）＋尾段桶（與 POM 無關）；新增 L≥2 列舉（尾端 L 鍵 2≤L≤min(count,3)＋尾段桶跑 lookupHub.grams 併入 ranked、value 去重）、兩倉鏡像一致。測試 IH507（版 置首、電腦班 次位、含電腦版）。驗證：Typewriter 160/160、MainAssembly 68/68、Legacy debug-core ✅、swiftformat 0 檔。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 183。）
（Phase 182 完工——**刪除 `kPOMAsNGramSourceEnabled`、POM 對組句的所有影響交由 `kFetchSuggestionsFromPerceptionOverrideModel` 單一把守（兩倉）**。事主實測：功能僅在啟用時於某些情況「變聰明」、未見 regression——但開關預設關閉值已寫入消費者裝置（改預設啟用對既有使用者無感）、使用者亦不知有此開關，故直接刪除、不繼承設定值。**移除面（兩倉生產碼全清）**：UserDef（case／預設值／i18n mapping）、PrefMgrProtocol、PrefMgr_Core、`LMInstantiator.Config.pomAsNGramSourceEnabled`、syncPrefs、P181 新增之辭典頁 toggle（SwiftUI＋Cocoa×2 全回收）、l10n 四語 desc＋shortTitle 8 檔刪鍵。**把守移交**：LMA Config 改為 `fetchSuggestionsFromPerceptionOverrideModel`（預設 true、鏡像主開關），`unigramsFor` 兩路徑注入（僅 contextual；unigram 記憶不進引擎、由 Typewriter 通道浮現）與 `syncPrefs` 改掛之；Typewriter `retrievePOMSuggestions(apply:)` 移除舊 pref 檢查（fetch 已於入口把守），P162 狂拼移交＋P181 非狂拼「override＝統計兜底」不變。**語義定案（探針）**：統計常駐使記憶於相近語境產生 bigram 層級統計泛化（僅植入三層記憶（留&一縷→芳），「幽蝶能留方」亦偏好「芳」）；override（fetch 建議）仍守上下文等值。IH202／test104「負面不可外溢」端對端期望編碼舊 override-only 語意、與常駐統計互斥，整函式刪除（perceptor 層等值覆蓋見 POMTolerantQueryTests）。**測試**：POMNGramSourceTests 4→5（預設注入／主開關關閉不餵入／容錯／unigram 不進引擎、記憶前後等值／快取世代）；IH137 重定義「主開關關閉＝記憶完全不影響組句」；IH138／IH504／IH150-152 移除舊 pref 行。驗證：LangModelAssembly 145/145、Typewriter 157/157、MainAssembly4Darwin 68/68、Legacy `make debug-core` ✅、兩倉 swiftformat 0 檔、l10n plutil OK、全倉零殘留引用。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 182。）
（Phase 181 完工——**POM 記憶參與組句移交 Homa n-gram／trigram：`kPOMAsNGramSourceEnabled` 預設翻轉為啟用＋設定 UI 曝露＋l10n 語義翻新（兩倉）**。事主覆核：P160「外源開關」預設 off 且從未曝露於設定頁，使 S2/S3/S4 全體系（POM→bigram／trigram 注入、P162 狂拼自動套用移交統計路徑）對正式使用者完全休眠、「Homa trigram 支援完全沒用上」——P160／P158 載明之「預設 off（有人不想用臨時記憶影響組句）」應為 opt-out 前提而非功能休眠。**預設翻轉（兩倉鏡像）**：`UserDef.kPOMAsNGramSourceEnabled` default false→true（UserDef.swift）；`LMInstantiator.Config.pomAsNGramSourceEnabled` false→true（欄位註解同步更新為「n-gram（bigram／trigram）統計來源」）。**UI 曝露（三面，辭典頁 POM 區）**：開關插入「漸退記憶」區塊（`kFetchSuggestionsFromPerceptionOverrideModel`／`kReducePOMLifetimeToNoMoreThan12Hours` 同區、緊接後者之後）——macOS SettingsUI（SwiftUI `renderUI`＋`SettingsUIHost.shared.syncLMPrefs()` closure，開關值需即時推回 LMInstantiator Config）、macOS＋legacy SettingsCocoa（`renderCocoa`＋`lmmgrSyncLMPrefs` target/action，鏡像 hunk 逐字節一致）。**l10n**：`kPOMAsNGramSourceEnabled.description` 四語全數翻新（兩倉 8 檔、shortTitle 不變）——移除「此開關預設關閉」之過時與誤導敘述，改為「預設啟用；記憶以 n-gram（bigram／trigram）統計參與組句、隨衰減自動淡化；啟用時狂拼模式在固化讀音時不再以覆寫方式直接帶出記憶詞、改由統計路徑自然選中；若不想讓臨時記憶影響組句可關閉，關閉後記憶不參與組句統計、維持既有行為」。**測試**：POMNGramSourceTests 原「預設關閉」案例改寫為「預設啟用（未設定即注入）」；Typewriter IH202 與 MainAssembly test104（「漸退記憶適用範圍」端對端鏡像：幽蝶能留一縷芳記憶不得外溢至「幽蝶能留方」）釘住 `pomAsNGramSourceEnabled = false`——兩測試驗證 Typewriter override 路徑之記憶適用範圍、與引擎統計餵入正交。**驗證**：LangModelAssembly 144/144、Typewriter 157/157、MainAssembly4Darwin 69/69、Legacy `make debug-core` ✅、兩倉 swiftformat lint 0 檔需格式化、l10n 8 檔 plutil OK、鏡像 hunk 兩倉逐字節一致、git status 逐倉僅含手術檔。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 181。）
（Phase 181 後續補充——**兩開關語意界線補完：fetch 開關 l10n 翻新＋S4 閘門精化（非狂拼 override 轉為統計兜底，兩倉）**。事主盤點辭典頁 POM 區兩開關語意後追加三項（仍歸 P181）：**① `kFetchSuggestionsFromPerceptionOverrideModel` shortTitle 改名**（「在敲字時自動套用…」→「啟用漸退記憶的學習與自動建議」，四語、UserDef key 不變）**② description 重寫**（原僅描述 POM 生命週期；改述為「學習＋自動建議總開關、關閉即停止學習與自動建議、若統計來源仍啟用則既有記憶以統計方式影響組句」，兩倉 8 檔、兩開關界線於文案明確）**③ S4-a 閘門精化**：先實測「把 P162 移交原樣延伸到非狂拼」→ **IH304／IH305（再創世凱歌，非狂拼注音漸進組句）紅燈**，證實一般拼音／注音依賴 override「記憶詞必出」的直接浮現（P158 D.2.6 顧慮實證），全面移交否決；定案為非狂拼 **override 轉為統計路徑兜底**——ngram 來源開啟、建議為 contextual、且 DP 已於游標節點選中同值記憶詞時，跳過重複 override 錨定（消除雙重加成、gram.previous 保留統計語義）；DP 未選中時仍套用（漸進浮現不變）。狂拼分支維持 P162 原樣；bare unigram 記憶維持既有套用。測試新增 **IH504**（非狂拼 shi4 ma1：嬤/媽 同讀音，記憶（是→媽）使 DP 選中媽、斷言 gram.previous==「是」）；IH304/305 維持綠燈。驗證：LangModelAssembly 144/144、Typewriter 158/158、MainAssembly4Darwin 69/69、Legacy debug-core ✅、兩倉 swiftformat 0 檔、l10n plutil OK、CoreProtocol 鏡像逐字節一致。commit：兩 IME 倉 amend（待事主指示之描述）。Homa 零改動。詳見 `DevPlans/Reqs4LLM/Reqs_0181-0190.md` Phase 181。）
（Phase 180 完工——**ComDlg32（NSOpenPanel / NSSavePanel）於 macOS ≤10.12 的偏好設定按鈕相容性修復（兩倉）**。macOS ≤10.12 的輸入法程序直接呼叫 ComDlg32 會使輸入法與客體 app 無限風火輪死迴圈（既有 `TechnicalReasonNSOpenPanel` 文案載明）、10.13+ 無此問題。全量盤點 SettingsCocoa 六處 ComDlg32 入口：Dictionary 資料夾選擇／Kimo 匯入／Cassette 磁帶選擇／Clients 客體瀏覽四者早已有 10.13 guard＋拖放／手輸替代交互（相容、不動）；DevZone JSON Import 僅警示無替代（dead-end）、DevZone JSON Export（NSSavePanel）完全無 guard（漏網之魚）——為此兩鈕重設計 ≤10.12 交互。改動 `VwrSettingsPaneCocoaDevZone.swift`（兩倉鏡像 hunk 逐字節一致）：① JSON Import 改為 `NSFileDragRetrieverButton`（.json 拖放於所有 OS 直接匯入，`postDragHandler` → 抽離的 `importPrefsFromJSONFile(at:)` 與 panel 共用）；點擊維持 10.13 guard（≤10.12 警示如今指向真實 drop target、10.13+ 開 NSOpenPanel 不變）。② JSON Export 補 10.13 guard——≤10.12 改走 `exportPrefsAsJSONToFileDump(_:)`：不經面板寫入 `<Application Support>/fileDump/`（同源 LMMgr.appSupportURL、沙盒自動落 container、無需授權），檔名 `vChewing_Preferences_<ddMMyy-HHmmss>.json`，NSAlert 告知路徑後 `activateFileViewerSelecting` 揭示；10.13+ NSSavePanel 零改動。L10n：新增 `ExportDumpedTitle`／`ExportDumpedMessage:%@` ×4 語系（兩倉 8 檔、字母序、plutil OK）。驗證：macOS SettingsUI package build ✅（6.70s）、MainAssembly4Darwin build ✅（233/233，11.16s）；Legacy `make debug-core` BUILD SUCCEEDED ✅（deployment target 10.9）；兩倉 `make lint; make format` 收斂後 git status 逐倉僅含手術五檔、跨倉 DevZone diff 僅存既有方言、L10n 逐語系兩倉一致；SettingsUI 無單元測試靶位（以建置為準）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。）
（Phase 179 完工——**SettingsCocoa 頁面排版重構：ABCDE SubTab 分頁廢除、macOS 10.9 起全面啟用捲動式設計（兩倉）**。先前 `CtlSettingsCocoa.showContentForTab(_:)` 誤判 NSScrollView 用法與 macOS 10.9 不相容、對 10.9–10.12 走「不捲動、直接塞入容器」分支（註解稱怕在輸入法程序觸發系統凍結），頁面內容被固定視窗高度綁死、部分頁面被迫以「ＡＢＣＤＥ」SubTab 分頁拆載；實測確認相容後該分岔與 ABCDE 分頁皆廢除。改動五檔（兩倉鏡像、手術 hunk 逐字節一致）：`showContentForTab` 移除 `#available(macOS 10.13, *)` 分岔、一律以 FlippedClipContainerView 為 documentView 的 NSScrollView 包覆（四邊釘齊、切頁滾回頂端）；Behavior（五頁籤）／Candidates（四頁籤）／Dictionary（四頁籤）／DevZone（三頁籤）四 pane 廢除 NSTabView chrome、各 SubTab 內容依原 Ａ→Ｅ 次序縱向串接成單一長頁（SettingsUI 式每頁多段內容排版）、section 寬度 innerContentWidth→contentWidth，Behavior／Candidates 外層 stack 改 insets all 14（其餘八頁本即單頁、零改動）。無新增 L10n 字串。驗證：macOS SettingsUI package build ✅（9.5s）＋MainAssembly4Darwin build ✅（228/228，41.8s）；Legacy `make debug-core` BUILD SUCCEEDED ✅（deployment target 10.9）；兩倉 `make lint; make format` 後 diff 僅含手術五檔、跨倉僅存既有方言差異；SettingsUI 無單元測試靶位（以建置為準）。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。）
（Phase 179 後續補充——**SettingsCocoa 分頁的「內容陳列順序與 Section 規劃」與 SettingsUI 對齊（兩倉六檔）**。Behavior／Candidates／Dictionary／DevZone 四頁依對應 SwiftUI 頁的 Section 分組與順序重排（Behavior→9 段、Candidates→5 段、Dictionary→4 段、DevZone 三單項小框合併為一框並把 OptionsMoved 註記移至框下、對齊 SUI footer 位）；四月一日彩蛋 `kShouldNotFartInLieuOfBeep` 由 Behavior 遷至 General（SUI 同頁、UseSCPC 之後），處理器一併搬移；Output 補 `SectionExperimental` 標題列（SUI Section header 對齊）。逐 item 搬移重排、無增刪選項、無新增 L10n。驗證：macOS MainAssembly4Darwin build ✅（12.46s）、Legacy `make debug-core` ✅、兩倉 lint/format 收斂後 diff 僅含六檔、跨倉 numstat 一致、逐頁 item 序與 SUI 一致（dump 比對）。commit 未提交（待事主指示）。）
（Phase 178 完工——**半形標點模式啟用時 bypass「Option(+Shift)+主鍵盤區數字鍵」數字輸出功能（兩倉）**。既有「Option(+Shift)+主鍵盤區數字鍵」數字輸出功能（全形標點模式下 Option+MainNum→半形數字、Option+Shift+MainNum→全形數字；組字區有內容另經標點詞庫鍵頭插入數字）與半形標點模式語意不相容——半形模式啟用時這兩組按鍵不應再被數字輸出功能攔截、應回歸鍵盤佈局原有的處理。改動兩處（鏡像逐字節一致）：`InputHandler_HandleStates.swift` 的 `handleArabicNumeralInputs` 開頭新增 `guard !prefs.halfWidthPunctuationEnabled else { return false }`（半形模式不再攔截數字鍵、按鍵透傳由系統依佈局輸出字元）；`InputSession_HandleEvent.swift`「Alt(+Shift)+主鍵盤區數字鍵 預先處理」新增同款前置（不再把 Option 組合鍵字符換算為 ASCII 數字、保留佈局自訂字元、避免動態鍵盤佈局模式誤提交）。全形（非半形）標點模式行為零改動（探針實證 .ofEmpty 下 Option+1→「1」、Option+Shift+1→「１」原樣）。測試：MainAssembly test206 改寫（全形模式仍輸出數字、半形模式兩組合皆不攔截、客體零輸出）＋Typewriter 新增 IH434（直接 triage 層鎖定 bypass）。驗證：Typewriter 157/157、MainAssembly4Darwin 69/69、Legacy `make debug-core` ✅；兩倉 lint/format 冪等、鏡像逐字節一致；commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。）
（Phase 177 完工——**測試輸出噪音清理：vChewingDebug log 通道 filter 感知（兩倉）**。Linux／WinNT CI 完整測試輸出海量 `POM Error: Unable to clear data because file URL is nil.`／`POM saveData() failed...` 錯誤訊息與 typewriter state changes（`StateChanging`／`Current State`／`Blocked data`）——皆 mixedAlnum 等大量觸發 POM 儲存／偵錯路徑的測試所印（測試環境 POM 儲存 URL nil、no-op）。改動：兩條 vChewingDebug 通道本體加入「測試模式（`pendingUnitTests`）且無過濾參數（`--filter`／`--skip`／`-XCTest`，`hasTestFilterArguments()` 偵測 `ProcessInfo` args）→ 靜音」前置判斷——`Shared.vCLog`（Typewriter state changes／`POM: Start Observation`／`Blocked data`）與 `LMAssembly.vCLMLog`（`lmPerceptionPersistor` POM 三處 nil-URL 錯誤、lmCassette 載入訊息等）；`lmPerceptionPersistor` 三處 guard 維持直呼 vCLMLog（還原個別包覆）。探針實證 SwiftPM 以 `swiftpm-testing-helper … --filter …` 執行、`--filter` 確入測試 binary args（filter 模式放行／無 filter 完整測試靜音）；macOS 26 `Process.consoleLog` 走 OSLog、噪音僅 Linux／WinNT CI 可見、本機以探針驗證分支。驗證：Typewriter 完整測試 156/156 ✅（85.15s、零噪音）、兩倉 lint/format＋build ✅、legacy `make debug-core` ✅；鏡像同步（legacy 無 `nonisolated` 方言）。commit：macOS `59658c10`（LangModelAssembly）＋`b93ea836`（Shared）／Legacy `3aff82c3`＋`2379f745`（兩倉同訊息分離 commit）。）
> （Phase 176 完工——**OS-Independent Session（Sessions 體系遷移至 Typewriter package，兩倉）**。`InputSession`＋`SessionProtocol`＋`IMEState` factories＋`IMEStateParsed`（原 `IMEStateParsed4Darwin`）＋具體 `InputHandler` 等 11 檔自 `MainAssembly4Darwin` 遷入 `vChewing_Typewriter`（Session／InputHandler 子目錄），Typewriter 自此可直接編譯 Session 參與的開發邏輯（Linux／Windows 目標）。動作依賴採「lambda-expression property assignment 或 Host Delegate」：新檔 `SessionHost.swift`（`SessionHost.shared` 單例）以閉包屬性注入全部 OS-dependent 動作（IMEApp／LMMgr 12 法／Notifier／SpeechSputnik／AppDelegate／NS* 系列／IMKHelper／Broadcaster／IMKControllerLifetimeTracker／SessionUI／PrefMgr／ChineseConverter／UserPhrase／CandidateTextService），未注入＝無操作預設；新檔 `SessionClientProxy.swift`（跨平台客戶端 proxy 抽象，Darwin 端 `IMKInputSessionController` 併入）。`Shared.InputMode.langModel` 延伸遷入 Typewriter（POM URL 走 `pomDataURL` 閉包），兩倉 `LMMgr_Core.swift` 移除原延伸。Darwin 表面留守 MainAssembly4Darwin：新檔 `InputSession_DarwinSurface.swift`（IMK surface＋NSEvent 轉換入口＋toggleInputMode）＋`SessionHostWiring.swift`（`wireUp()` 於 MainSputnik4IME.init＋測試套件 init）。Legacy 鏡像：portable 11 檔遷入 `Shared/vChewingComponents/Typewriter/`（去 @MainActor／nonisolated deinit 方言）、DarwinSurface＋Wiring 入 Modules/SessionController、LMMgr_Core 移除 langModel 延伸、pbxproj 增 13 檔刪 9 檔。驗證：macOS root build ✅、Typewriter 156/156 ✅、MainAssembly4Darwin 69/69 ✅（含 SessionHost.wireUp 測試注入）；Legacy `make debug-core` ✅；兩倉 lint/format 後重建 ✅、diff 僅含手術檔案。commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。另依事主提示簡化 Typewriter imports 清單（沿用 P175 `_ModuleReexport` 前例）：遷入之 11 檔重複模組 import 一律剝離、僅留 `import Foundation`，檔面與 legacy 鏡像逐字節一致（Session 9 檔＋SessionCoreProtocol 逐字節相同；InputSession／InputHandler 僅存 @MainActor／nonisolated deinit 另經 Linux 實編（swift-corelibs-foundation）抓出並修復 `InputSession_HandleStates.swift` 兩處 Darwin 專屬 `NSAttributedString` 用法（零參數 init→`init(string:)`；optional `isEqual(to:)`→`map` 解包、nil 視為不相等），兩倉鏡像同步；Linux／WinNT CI 與 macOS 26 / Xcode 26.6 CI 全數通過（事主回報）。commit：macOS `539ee616`／Legacy `36b7fdb0`（Typewriter // Absorb the OS-independent part of Session.）；LibVanguard 文件隨 HEAD aggregate、不另記 hash。方言差）。）（Phase 175 完工——**SettingsUI SPM 抽取（兩倉）**。偏好設定畫面（SettingsCocoa＋SettingsUI，31 檔）自 `MainAssembly4Darwin` 抽取為獨立 Swift Package `vChewing_SettingsUI`（單 target `SettingsUI`），不依賴 MainAssembly4Darwin、可獨立以 executable bundle target 除錯（本次動機：Settings 畫面記憶體佔用過大且不釋放）。動作依賴採「lambda-expression property assignment」：新套件定義 `SettingsUIHost`（@MainActor 單例）——LMMgr 21 個方法（dataFolderPath／cassettePath／resetCassettePath／loadCassetteData／syncLMPrefs／resolveUserSpecifiedURL／checkCassettePathValidity／importCassetteFileToCache／cassetteAccessFailureDescription／initUserLangModels／connectCoreDB／loadUserPhraseReplacement／checkIfSpecifiedUserDataFolderValid／resetSpecifiedUserDataFolder／chkUserLMFilesExist／migrateUserDataFrom／importYahooKeyKeyUserDictionary／retrieveData／saveData／tagOverrides／openPhraseFile）、`SessionUI.resyncShiftKeyUpCheckerSettings`、`AppDelegate.updateDirectoryMonitorPath`、`InputSession.recentClientBundleIdentifiers`、`PhraseEditorDelegate` 全部以 closure 屬性注入（未注入＝無操作預設、套件可獨立運行）；`PrefMgr.shared` 單例＋`validate(candidateKeys:)` 遷移至 Shared_DarwinImpl（validate 依賴 Darwin 專屬 `IMEApp.isKeyboardJIS`、無法下放跨平台 Shared），Settings 檔案約 120 處 `PrefMgr.shared` 呼叫零改動；宿主注入 `SettingsUIHostWiring.swift`（`wireUp()` 於 `MainSputnik4IME.init`＋測試套件 init 呼叫、含 PrefMgr 殘餘 didSet 回呼）；`_ModuleReexport` 增 `@_exported import SettingsUI`、pbxproj 登錄新套件。Legacy 鏡像：SettingsCocoa 14 檔同一橋接改造（59 處）、新增 `SettingsUIHost.swift`（去 @MainActor 方言）＋`Modules/SettingsUIHostWiring.swift`、`PrefMgr_Singleton.swift` 保留原位（單一 app target 行為等價）、pbxproj 登錄兩新檔。驗證：macOS `vChewing_SettingsUI` 獨立 build ✅、root build ✅、MainAssembly4Darwin 69/69 ✅；Legacy `make debug-core` ✅；兩倉 lint/format 還原無關 churn、diff 僅含手術檔案。**第二波追加（同日，事主補述三項）**：PhraseEditorUI 併入 SettingsUI 套件（移入 `Sources/SettingsUI/PhraseEditor/`、刪除獨立套件）；NotifierUI 依賴改 lambda（`SettingsUIHost.notify`、SettingsUI 套件移除其依賴）；SettingsUI 套件內建 `_ModuleReexport.swift`（BookmarkManager／LangModelAssembly／Shared_DarwinImpl）、剝離 44 行模組 import、利於 legacy 同步。 commit 未提交（待事主指示）。Homa 零改動、無 LibVanguard 分離 commit。
（Phase 174 完工——**狂拼長簡拼字母流不再被注拼槽音頭丟棄截斷（三倉）**。使用者回報：敲「slliang」只出「力量」類「ㄌㄧˋ ㄌㄧㄤˋ」二字詞、開頭「s」像沒敲過；「sllian」則正常。根因：Tekkon `SyllableComposer.receiveKey(fromScalar:)` 的 romajiBuffer FIFO 音頭丟棄——單音節上限 6 碼（Wade-Giles 7）、「slliang」7 字元敲入第 7 碼「g」時開頭「s」被靜默截斷成「lliang」，後續 α 整詞簡拼查詢以「lliang」為準、命中「力量」類 ㄌ-ㄌㄧㄤ 詞。修復：Tekkon 新增 `allowsExtendedRomajiBuffer`（預設 false 維持防呆；true 關閉音頭丟棄、由呼叫端管理注拼槽）＋Typewriter BPMF 接收點依 `isFuriousTypingModeEffective` 即時設定。測試 fail-first 已驗（Tekkon＋IH154 雙重）；Tekkon 37/37、Typewriter 156/156、LibVanguard ✅、Legacy build ✅；commit Tekkon macOS f407555f／Legacy 315da18f／LibVanguard 0a4d6c9、Typewriter macOS 586f89e6／Legacy 1324d2ee（Homa 零改動）。
（Phase 174 完工——**狂拼長簡拼字母流不再被注拼槽音頭丟棄截斷（三倉）**。使用者回報「slliang」只出「力量」類二字詞、開頭「s」像沒敲過、「sllian」正常。實機探針（真實 factory 辭典）逐拍輸出 α 鏈路：`sllian`／`slliang` 的 cells 皆正確（`["ㄕ&ㄙ","ㄌ","ㄌㄧㄢ&ㄌㄧㄤ"]`／`["ㄕ&ㄙ","ㄌ","ㄌㄧㄤ"]`）、頂級候選皆為「山林涼」、各拍 dominance < 3.0 不觸發自動套用——α 查詢本身無問題。根因：Tekkon `SyllableComposer.receiveKey(fromScalar:)` 對 romajiBuffer 的 FIFO 音頭丟棄——單音節最長 6 碼（Wade-Giles 7）、超出即丟棄最早輸入音頭；「slliang」7 字元、敲入第 7 碼「g」時 buffer 已為「sllian」（6 碼）、開頭「s」被靜默截斷成「lliang」→ 後續 α 整詞簡拼查詢以「lliang」為準、命中「力量」類 ㄌ-ㄌㄧㄤ 二字詞（「sllian」6 碼不觸發、與使用者觀察完全吻合）。修復：Tekkon `SyllableComposer` 新增 `public var allowsExtendedRomajiBuffer = false`——預設維持既有音頭丟棄防呆；true 時關閉丟棄、由呼叫端負責在固化／提交／auto-chop 時清空或重建注拼槽；`receiveKey(fromScalar:)` 的音頭丟棄段以 `if !allowsExtendedRomajiBuffer { ... }` 包覆（`_refreshRomajiBufferIfNeeded` 維持無條件執行）。Typewriter `Typewriter_BPMFFullMatch.swift` 注拼槽接收點於 `receiveKey(fromString:)` 前依 `isFuriousTypingModeEffective` 即時設定旗子——狂拼（含中途切換）恆正確、非狂拼零改動、無需與 `ensureKeyboardParser` 同步。測試 fail-first 已驗：Tekkon `testExtendedRomajiBufferPreservesLongAbbreviationStream`（修復前 extended 段紅燈、「slliang」變「lliang」）＋Typewriter IH154 `test_IH154_FuriousLongAbbreviationKeepsFrontLetters`（修復前 buffer 實測「lliang」紅燈；Tekkon 旗子還原時亦紅燈——雙重 fail-first）。驗證：macOS Tekkon 37/37（36＋1）、Typewriter 156/156（155＋1）、LibVanguard 全量 ✅（含 Tekkon 新測試）、Legacy `make debug-core` ✅、swiftformat 冪等；三倉 `Tekkon_SyllableComposer.swift` 逐字節一致、兩倉 `Typewriter_BPMFFullMatch.swift`（去既有 `self.onLexiconMatchFailure = nil` 方言後）逐字節一致、兩倉 Tekkon 測試逐字節一致（legacy 無 Tekkon／Typewriter 測試檔）；commit Tekkon 分離 macOS f407555f／Legacy 315da18f／LibVanguard 0a4d6c9（同訊息「Tekkon // Composer: Allow extended romaji buffer for multi-syllable abbreviation streams.」）、Typewriter 分離 macOS 586f89e6／Legacy 1324d2ee（同訊息「Typewriter // Furious: Keep full abbreviation stream in composer (no head truncation).」）；Homa 零改動。）（Phase 171 完工——**狂拼 copilot 窗單漢字候選排序修正（兩倉）**。使用者回報「wo」時 copilot 窗「喔」在前、「我」需滑鼠滾輪翻頁、明明「我」權重更大。實機探針（真實 factory 辭典 `VanguardFactoryDict4Typing.txtMap` 桶查詢）實證：`我（ㄨㄛˇ）=-5.043`、`喔（ㄨㄛ）=-5.183`、另有無調墊底 `我（ㄨㄛ）=-9.465`（count=0 floor；該「聲調專屬＋無調 fallback」雙鍵結構為 Vanguard Lexicon 資料本身所帶）。根因：`unigramsForWithAlternatives` 的 `factoryChoppedUnigramsFor` 依 trie 鍵序（ㄨㄛ 先於 ㄨㄛˇ）回傳條目，下游 `consolidate(filter:)`「先插入者勝」（於 sort **之前**執行）把同詞值高權重聲調條目丟掉、保留無調低權重條目——桶查詢（multipleKeys，copilot 窗候選來源）中「我」以 -9.465 沉到「喔」之後。修復：`factoryChoppedUnigramsFor(keyArray:entryType:partiallyMatch:)` 回傳前依詞值去重、保留最高機率者（`[String: Homa.Gram]` 收集、機率高者覆蓋；空詞值跳過）；範圍收斂——僅原廠「&」連讀查詢內部去重、使用者片語／磁帶「先插入者勝」覆蓋語義（前插）與 fast path（單鍵查詢無跨鍵重複）零改動；呼叫端最終仍 sort 故迭代順序無關。測試：新增 `testFactoryChoppedQueryDedupsValueByHighestProbability`（自訂 textMap：我 ㄨㄛ/-9.465＋ㄨㄛˇ/-5.043、喔 ㄨㄛ/-5.183；修復前 2 斷言紅燈——我 以 [ㄨㄛ] 鍵、-9.465 且排在喔後；修復後 [ㄨㄛˇ]、-5.043 浮至首位）；實機探針複驗「我」升至桶查詢首位。驗證：LangModelAssembly 144/144（143＋1）、Typewriter 155/155、MainAssembly4Darwin 69/69、Legacy `make debug-core` ✅、swiftformat 冪等、兩倉 `LMInstantiator_TextMapExtension.swift`（去既有 import／EntryType 方言後）逐字節一致；commit macOS ac8b6bd9／Legacy cd5ededa（Homa 零改動無分離 commit；legacy 無 LMAssembly 測試檔）。）（Phase 170 完工——**狂拼模式標點輸入不再蜂鳴：`handlePunctuation` 先固化前方讀音（兩倉）**。`handlePunctuation` 於 `isComposerOrCalligrapherEmpty` 守衛前新增 `!isComposerOrCalligrapherEmpty && hasFuriousFrontPending` 分支——先以與空格／Tab／Enter 同語義的 `solidifyFuriousFrontReading()` 固化前方讀音（只插聲調桶、不覆寫、完整音節 trail 累積），再讓標點鍵（`_punctuation_`／`_half_punctuation_`／`_alt_punctuation_`／`_letter_`／`_NumPad_` 全收斂於 `handlePunctuation`）循既有路徑插入；固化失敗（無效前綴無法展開成讀音桶）維持既有 A9B69908D 蜂鳴語義（與普通拼音模式「讀音未敲完無視標點」一致）。測試 IH153 新增（tama＋「？」→ 無蜂鳴、注拼槽清空、鍵數 1→3、組句含「？」；fail-first 已驗——修復前 errorMessages 實測 ["A9B69908D"]）；Typewriter 155/155、LMA 143/143、MainAssembly 69/69、Legacy build ✅、兩倉 swiftformat 冪等、鏡像逐字節一致；commit macOS 2f33b672／Legacy 51494b86。）（Phase 169 完工——**狂拼 copilot 窗 POM 建議段數溢出＋首段重合多讀音桶誤判修復（P165 邏輯盤點，兩倉）**。`applyFuriousFrontCandidate` 的 `leadingOverlapCount` 改 k 恆為 1（多讀音桶下 `prefix(while:)` 連續數算會讓 dropFirst(k) 與覆寫 span 錯位）；新增收斂「候選段數 > 組字器鍵數＋1 且非 bucket-pinned 時靜默退回」（避免「嗎他媽的」讀音重疊；組字器空 R2-α ysxb 與置頂無橫跨 bucket-pinned 不攔截）；`buildFuriousFrontCandidates` T1 段 POM 置頂建議加段數上限 `keys.count + 1`（tama 類不再撈回 3 段「他媽的」與 copilot 2 鍵脫節；不動 `alternateKeys` 既有 short→long 語義——初版誤傷 AC03 已回退）。測試 IH150 改寫（直接確認路徑）＋新增 IH152（tama 不置頂 3 段／tamade 照常）；Typewriter 154/154、LMA 143/143、MainAssembly 69/69、Legacy build ✅、兩倉 swiftformat 冪等、鏡像逐字節一致；commit macOS ae84fe20／Legacy a11b48e3。）**狂拼簡拼感知重切＋α 自動套用（R3＝δ，兩倉）**。沿 P155 報告 §7.5／§九 路線圖（R3）施工；α（R2）的整詞簡拼查詢是評分器；**Homa 預設零改動**（原子 `replaceKeys` 評估後確認無必要）。**R3-a**：`FuriousTypingSegmentor` 簡拼感知由呼叫方閉包注入（`isValidSyllable` 接受「完整音節 OR 合法簡拼前綴」）；新增 `autoApplyFuriousAbbreviationIfClearWinner(appending:)`——auto-chop 不可行時以 α 整詞查詢評分注拼槽整段（含本拍字元）、頂級候選「明確勝出」（讀音數與簡拼段數一致＋唯一匹配或 log-prob 差 ≥ 3.0）時自動把實際讀音單鍵寫入組字器——`ysxb` 全程自動出「野獸先輩」不必等 Shift+選字鍵；`furiousAbbreviatedCells(romaji:)` 拆參數版、接線 auto-chop 失敗分支。**R3-b（跨音節數枚舉能力＋重切安全範圍）**：`candidateSegmentations` 的 `syllableCount` 改可選（nil＝不限）——跨音節數枚舉能力保留於 Segmentor 純函式層供今後經設計的觸發條件；整合層重切維持「同音節數枚舉＋trail ≥ 2＋總分比較」、固化不觸發重切（設計依據：拆開型重切在打字中途拆開單音節 trail、會誤傷「先生」類組句〔「xiansheng」→「西岸生」〕，且「每音節平均」偏好多音節切分——P150 長度偏差取捨的實證）；`fangan`→`[fan,gan]` 邊界修正由 auto-chop 路徑同音節數重切提供。**R3-c（不施工）**：lazy 展開（手段 F）——枚舉有 DP beam 兜底、α 查詢有界。**紀律**：trail 不變量維持、顯式干涉後 trail 失效沿用。測試 Segmentor ＋2、IH132-134 改模稜兩可場景、新增 IH139/142/143（IH143＝「先生」回歸防護）；Typewriter 148/148、LMA 143/143、MainAssembly 68/68、Legacy build ✅、LibVanguard build＋test ✅。（Phase 162 完工——**Typewriter POM apply 路徑瘦身：狂拼自動套用移交 n-gram（兩倉）**。**S4-a（雙重加成收斂）**：`retrievePOMSuggestions(apply:)` 在狂拼＋`pomAsNGramSourceEnabled` 時跳過自動套用（override 錨定）、回傳唯讀提取——contextual 記憶由 DP 以 n-gram 自然選中；開關關閉時原行為不變。**S4-b（範圍收斂）**：retokenization 重試／preConsolidate／bleaching／short→long 閘門**維持**——屬顯式選字路徑、為切分深度不匹配必要機制。**S4-c**：顯式 `.withSpecified` 釘住、unigram 記憶、POM 觀察全維持。測試 Typewriter ＋1（IH138：n-gram 選取帶 previous、非 bare override）；Typewriter 143/143、LMA 143/143、MainAssembly 68/68、Legacy build ✅。（Phase 161 完工——**Homa trigram 支援＋Trie 第五欄＋POM→trigram 餵入（三倉）**。**Homa**：`Gram.anterior`（Codable/Hashable/等值/描述全更新）；`Node.getScore(previous:anterior:)` 掃描三元＋雙元、`selectOverrideGram` 增 anterior；PathFinder 1D DP 零改動（trigram 前驅之二＝`parent[i-parent[i].segLength]` 最佳路徑回看）；選字窗排除 previous＋anterior 皆 nil。**語意精化**：getScore 基線改「top gram 匹配語境取其分數、否則退回最佳 unigram」＋bonus 不綁 currentGram——修「未匹配 bigram 洩漏」quirk（既存 bigram 測試全綠）。**TrieKit**：`Entry.anterior`＋型別 C 第五欄＋4-tuple→5-tuple 傳播。**LMAssembly**：`makeFactoryUnigrams` 攜帶 previous/anterior（斷鏈修復）＋POM→trigram 餵入。測試 Homa ＋3（`HomaTests_Trigram`）；macOS Homa 61/61、LMA 143/143、TW 142/142；Legacy build ✅；LibVanguard build＋test ✅（含 LexiconKit `HomaGramTuple` 消費者修正）；三倉 swiftformat 冪等。（Phase 160 完工——**POM 資料作 Homa bigram 來源＋外源開關（S2，兩倉）**。`perceptionsFor` 全量記憶查詢＋`unigramsFor` 兩路徑注入帶 previous 的 bigram gram（附加於 consolidate＋sort 後、選字窗排除機制自動隔離、記憶 head 讀音段作 keyArray）；`kPOMAsNGramSourceEnabled`（預設 off）＋Config＋syncPrefs＋l10n 四語；`mtxPOMGeneration` 世代計數納入 unigramsFor LRU fingerprint；分數直接注入＋衰減自限。測試 LMA ＋4（POMNGramSourceTests）、TW ＋1（IH137）；LangModelAssembly 143/143、Typewriter 142/142、MainAssembly4Darwin 68/68、Legacy build ✅（含 UserDef 方言變換）。（Phase 159 完工——**POM 容錯查詢 API＋T1 copilot 套用 POM（S1，兩倉）**。**S1-a**：`LMAssembly.POMQueryMode`（.exact／.toneInsensitivePrefix）＋`fetchSuggestion(matchMode:)`；`getToneInsensitiveSuggestion`＝LRU 全表掃描（≤500）——讀音逐段去聲調等值（段數一致）、上下文 values exact＋query 無上下文萬用、head values 放寬（容許修正建議）、`_` 前綴讀音與 threshold 保留。語義定案：query 讀音恆為完整音節→prefix 退化為去聲調等值（防 ma↔mang 誤配）。**S1-b（T1）**：`retrievePOMSuggestions` 狂拼用容錯模式（固化 apply＋候選窗置頂受益）；`buildFuriousFrontCandidates` copilot 窗置頂 POM 建議（組字器副本＋虛擬尾段唯讀查詢、不寫記憶）；α 路徑無需專碼。測試 LMAssembly ＋7、Typewriter ＋2（IH135 置頂／IH136 固化套用）；LangModelAssembly 139/139、Typewriter 141/141、MainAssembly4Darwin 68/68、Legacy build ✅（含方言變換）。**工具漂移**：`make lint` 誤修 158 無關檔案已還原。（Phase 158 完工——**研究任務（不施工）：POM 查詢彈性化與 Homa n-gram 體系**。交付 `DevPlans/Research/Phase158_Research_POMAndHomaNGram.md`。核心發現：**① POM 容錯查詢 API（tone-insensitive prefix）可行且為後續一切解鎖前題**——現行 POM 只有 exact ngramKey＋`alternateKeys` 等值/後綴比對，Furious 聲調桶（首位＝無調形）與不完整讀音使兩級皆落空；建議三位置讀音「去聲調＋前綴」比對（values exact），成本＝既有 `alternateKeys` 同級全掃描（容量 ≤500）；亦為 P151 待決 T1（copilot 套用 POM）啟用前題。**② POM 資料作 n-gram 來源資料就緒**——POM ngramKey 天生三層、perception 帶 count/timestamp；注入點＝`LMInstantiator.unigramsFor`；選字窗既有排除帶 previous grams 機制故不污染候選窗；需外源開關（UserDef＋Config＋syncPrefs＋l10n）、unigramsFor 快取指紋納入 POM 世代、分數映射防過度支配（新鮮 ≈ −0.171 vs 詞典 −0.x～−11）。**③ Homa trigram 引擎端代價低、資料才是真問題**——DP 已 1D、bigram 採「最佳路徑前驅」近似，trigram 往回多看一跳、DP 結構零改動；但 factory 詞典（442,105 行）零 ≥4 欄列＋LMAssembly `makeFactoryUnigrams` 兩處丟棄 trie `previous` 欄→**bigram 鏈路生產環境三重休眠**；唯一現成 trigram 語料＝POM。**④ DAG-DP 自動選取 vs override 錨定文書整理完成**（`.withTopGramScore` 軟性路徑選取 vs `.withSpecified` 硬錨定、互補）。**⑤ 方向五（DevReq 追加）**：S2／S3 落地後 Typewriter 層「對 assembler 套用 POM 記憶」的**部分**內容不再重要、甚至可省略（能省很多問題）、但有些內容不能省——「自動段」（solidify 後自動 apply、候選窗自動置頂）可移交 n-gram 體系、刪掉 4 次 retokenization 重試／preConsolidate／bleaching／short→long 閘門等複雜度；「顯式段」不能省（unigram 記憶無上下文無法以 n-gram 表達、顯式選字釘住語意、切分深度不匹配需 retokenization、候選窗置頂 UX、寫入側觀察為語料生產者）；正確紅利＝自動那半沉進引擎、顯式那半脫掉歷史包袱。**後續 Phase 規劃**：S1（P159）POM 容錯查詢＋T1 copilot 套用（兩 IME 倉）／S2（P160）POM→bigram 餵入＋外源開關（兩 IME 倉）／S3（P161）Homa trigram 支援＋Trie 第五欄＋POM→trigram（三倉）／**S4（P162）Typewriter POM apply 路徑瘦身（可選收尾、前置 S2、同時為雙重加成風險根治）**；四階段互不硬綁。讀音限定 previous 記為獨立評估項。（Phase 157 完工——**R2 簡拼整詞能力（兩倉）**。**A**：`LMCoreEX` 多前綴交集掃描（`keys(matchingPrefixesByPosition:)`／`unigramsFor(keyPrefixesByPosition:)`——排序鍵陣列即索引、位置 0 首字元二分收斂＋逐位置 byte 前綴比對、語義＝舊逐 combo 前綴查詢聯集的超集、per-key 下游與舊路徑逐字節一致），`unigramsForWithAlternatives` partiallyMatch 分支改走之。**α**：`LookupHub.abbreviatedWordCandidates(keysChopped:)`（factory「&」連讀恆 partial＋user-phrase 多前綴掃描；分區＝置頂整詞猜測→factory→user-phrase）；Typewriter `furiousAbbreviatedCells`（chop＋deductChoppedPinyinToZhuyin）＋`generateStateOfInputting` 單音節桶失效時附整詞簡拼候選＋confirm/preview 守衛放寬（α 候選走既有 `applyFuriousFrontCandidate`＝實際讀音單鍵寫回、Homa 零改動）；整詞確認＝顯式選字→trail 失效。**D**：「&」cells 僅用於 LM 直接查詢、不進組字器（組字器桶形態／trail／候選窗過濾／tone-fuzzy 零改動）。**G**：copilot 窗「1 桶＋1 固定鍵」紀律維持。**測試**：LMAssembly ＋1（整詞查詢命中＋實際讀音 keyArray＋分區順序）、Typewriter ＋2（IH132 `ysxb` 候選窗顯示整詞、IH133 Shift+1 單鍵寫回＋trail 失效）。**驗證**：Typewriter 138/138、LangModelAssembly 132/132、MainAssembly4Darwin 68/68、Legacy `make debug-core` ✅、兩倉 lint/format 冪等。**補修（空格固化 α 路徑）**：α 時 pre-triage 固化因 `furiousFrontContext` nil 而 no-op、空格流入 `receiveKey(" ")`（中性聲調）→ `composer.clear()` 丟失前方整詞——`solidifyFuriousFrontReading()` 新增 α 分支（取整詞候選之首實際讀音單鍵插入、不覆寫、trail 失效）＋pre-triage 注拼槽殘留時直接消費空格；Typewriter ＋1（IH134）、139/139。（Phase 156 完工——**R1 防禦體系精化：笛卡爾積防禦改為「乘積預算」（三倉）**。**γ（Homa）**：`assignNodes` 以「窗內各鍵讀音數乘積」取代「任一 isMultiple 即縮短」，乘積 > 10,000（飽和、早停）才縮減 `maxSegLength→4`；全拼長詞與免聲調 5 音節以下（5⁵=3,125）整詞可組、免聲調 6 音節以上（5⁶=15,625）防線保留。**B／E（LMI 預算閘）**：`unigramsForWithAlternatives` 開頭算笛卡爾乘積、超閾時放棄展開（記憶體防禦）、逐 combo 迴圈全數短路、保留 factory「&」有界路徑（O(候選節點)）、CNS 過濾超閾跳過、早退不吞使用者造詞。**附帶修復（三倉 TrieKit）**：`decodeUTF8ScalarValue`／`decodeUTF8Scalar` 缺 ASCII `case 1`（`41a99834` 引入）→ `getEntryGroups(keysChopped:)` 對 ASCII 讀音鍵永遠回傳空，補分支＋回歸測試。**l10n**：`kKeyboardParser4Pinyin.description` 四語改寫。**測試**：Homa ＋3（免聲調 5 音節整詞／全拼 7 音節整詞（例詞「七見斷滅智論抄」、單元測試不用國號）／6 音節仍分段防禦回歸）、LMAssembly ＋2（預算閘＋ASCII chopped 回歸）。**驗證**：Homa 58/58、Typewriter 136/136、Tekkon 36/36、MainAssembly4Darwin 68/68、LangModelAssembly 127/127；Legacy `make debug-core` ✅；LibVanguard build＋test ✅（Homa/TrieKit 鏡像逐字節一致）；兩倉 lint/format 冪等。（Phase 155 完工——**研究任務：狂拼簡拼與長詞瓶頸調研（不施工）**。交付 `DevPlans/Research/Phase155_Research_FuriousTypingMode.md`。核心：① `ysxb`→「野獸先輩」卡在 `pinyinAutoChopResult` 完整音節門檻（多音節簡拼進不了組字器）；② 數學障礙＝「multipleKeys 桶進組字器」模型（`maxSegLength→4`＋user-phrase 全量笛卡爾 ≈8.57×10⁷），factory「&」chopped 查詢本已有界（`deductChoppedPinyinToZhuyin`＋`getEntryGroups`；`ysxb` initial-only 僅 4 種初始模式、野獸先輩全中）；③ `maxSegLength→4` 縮短為**刻意防卡死防禦**（2026-04-26 `bbe42490` 引入，拼音免聲調時代、狂拼引入前即有；非獨立問題），狂拼每讀音必插 5 聲調桶而使其恆啟動，全拼長詞（>4 音節）不可達為其代價；④ 建議路線 R1（γ：保留防線、把縮短條件依實際笛卡爾乘積精化，Homa 小改）→ R2（α：整詞簡拼查詢＋實際讀音單鍵寫回，Homa 零改動）→ R3（δ：簡拼感知重切）；⑤ P151 待決清單重盤點（T2 Shift 提示已完成應移除）；⑥ **查詢鏈路審計（§十，2026-08-29 事主授權）**：Homa→LM→Trie 全鏈核對，新手段 A–G——關鍵為 **A：user-phrase 多前綴區間掃描**（排序鍵陣列上 O(匹配鍵×位置) 取代 O(乘積)、免建索引）與 **B：LMI 笛卡爾預算閘**（硬體無關兜底）；R1 併入 B/C/E、R2 併入 A/D。（**Phase 154 完工**——(Shift+)Tab 狂拼尾段先固化後輪替＋furious `tail` 方向隱喻全面更名 `Front`（8 符號＋3 測試名＋全量註解，`trail` 蹤跡未誤傷）；Typewriter 136/136、兩倉鏡像。）（Phase 153 完工——**漢字轉換輪替枚舉化＋拼音/注音打字切換＋kKeyboardParser 分裂（兩倉全鏡像）**。`kKanjiConversionPreferences`（0 不轉換／1 康熙／2 JIS）取代 `chineseConversionEnabled`＋`shiftJISShinjitaiOutputEnabled` 兩布林，使用點（`ensureCurrencyNumerals`／`ChineseConverterBridge` config 與執行／`InputSession_Delegates` locale 與 accent color）全數改讀新 pref；menu 中 KangXi＋JIS 合併為「輪替漢字轉換」（`⌃⌘⇧K`、CHT 限定），toast 第一行＝切換後模式名、第二行＝新增 `NotificationSwitch.Switched`（「↻ 已切換／↻ Switched to／↻ 切替完了」）；輸出面板兩布林改單一三態 picker。新增 `kPinyinTypingEnabled`（default false）＋`kUsingHotKeyPinyinZhuyinTypingSwitch`（`⌃⌘⇧J`、簡體開放），action＝reset→toggle→`ensureKeyboardParser()`（`resetInputHandler` 不刷新 parser）。`kKeyboardParser4Pinyin`（default 100）＋`kKeyboardParser4Zhuyin`（default 0）取代單一 parser；`PrefMgr.keyboardParser` 保留為相容層計算屬性（getter 依 `pinyinTypingEnabled` 選槽、setter 依值域自動分流並同步旗子），使 `currentKeyboardParserType`、佈局翻譯與全部測試賦值零改動。遷移：舊 KeyboardParser 依語系分流＋同步 `pinyinTypingEnabled`（狂拼用戶升級後模式保持）、兩布林合併遷移、舊 key removeObject；`fixOddPreferencesCore` 兩路糾錯。l10n 16 新 key × 四語（兩倉）。macOS root build ✅、MainAssembly4Darwin 68/68 ✅、Typewriter 134/134 ✅（測試零改動）、Legacy `make debug-core` BUILD SUCCEEDED ✅、兩倉 swiftformat 冪等 ✅、複製檔兩倉逐字節一致。詳見 `DevPlans/Reqs4LLM/Reqs_0151-0160.md` Phase 153。)（**追加修正（同日，事主指定）：kUsingHotKey* i18n 映射錯位修復**——五個熱鍵 pref 的 shortTitle 值與 key 名環狀錯位（磁帶/大寫數字/半形標點/JIS/注音反查互換）與設定面板/menu 熱鍵條件脫節；四語值對齊語義、`kUsingHotKeyHalfWidthASCII`→`kUsingHotKeyHalfWidthPunctuation`（含 rawValue 遷移）、menu title key 逐一改正、反查視窗改用 RevLookup key 並刪除錯位的 `kUsingHotKeyJIS.shortTitle`（四語）、磁帶 tooltip 改用 Cassette key；兩倉鏡像、l10n 逐字節一致、macOS build ✅、Legacy ✅、Typewriter 134/134 ✅、MainAssembly4Darwin 68/68 ✅。）（**收尾（P153）：HomePage.io 熱鍵指南等教學內容更新**——shortcuts.md 熱鍵表、toggles.md C/D 段合併為「漢字轉換模式輪替」、preferences.md（輸出設定表三態＋注音/拼音排列分裂＋鍵盤熱鍵表）、README.md；兩倉 shortcuts.html 八檔 K/J 行描述更新；`Reqs_0151-0160.md` Phase 153 段落重寫為一氣呵成版本。）
> AI Agent 得特別注意本文所提到的「Response Pattern」。

---

## 一、專案概述

**LibVanguard**（先鋒引擎）是 vChewing Project 下一代輸入法的引擎的核心套件庫，以 Swift Package 形式提供。專案採用 LGPL v3.0 授權，涵蓋從注音符號解析、辭典樹查詢、語言模型聚合、到組字引擎的完整輸入法管線（pipeline）。

- **Swift 工具版本**：6.2
- **最低平台支援**：macOS 15 / macCatalyst 18 / iOS 18 / visionOS 2
- **跨平台目標**：保持對 Linux 與 Windows 的可建置性（透過 `#if canImport(Darwin)` 條件編譯平台限制）。

> 該專案本身不應視為 vChewing 唯音輸入法的一部分，除非今後刻意有此安排。

---

## 二、專案架構

### 2.1 目錄結構

```
[REPO_ROOT]/
├── Package.swift                    # SPM 套件定義（含 @resultBuilder DSL）
├── makefile                         # lint / format / test / dockertest 指令
├── EVOLUTION_MEMO.md                # 各模組研發備忘錄
├── CSQLite3/                        # 本地 SQLite3 C 套件依賴
├── Sources/
│   ├── LibVanguard/
│   │   └── LibVanguard.swift        # 主模組（聚合所有子模組）
│   └── _Modules/
│       ├── BrailleSputnik/          # 盲文點字轉換
│       ├── CandidateKit/            # 候選字池資料型別
│       ├── Homa/                    # 護摩組字引擎
│       ├── LexiconKit/             # 語言模型聚合中樞
│       ├── SharedCore/             # 共用協定、型別、狀態機
│       ├── SwiftExtension/         # Swift 語言擴展與鍵盤映射
│       ├── Tekkon/                 # 注拼引擎（聲韻並擊）
│       └── TrieKit/                # 辭典樹（RAM Trie + SQLite Trie + TextMap Trie）
├── Tests/
│   ├── LibVanguardTests/           # 主模組佔位測試
│   └── _Tests4Components/          # 各子模組測試
│       ├── _HomaSharedTestComponents/   # Homa 測試共用元件
│       ├── _SharedTrieTestDataBundle/   # 預編譯 Trie 測試資料
│       ├── BrailleSputnikTests/
│       ├── CandidateKitTests/
│       ├── HomaTests/
│       ├── LexiconKitTests/
│       ├── SharedCoreTests/
│       ├── TekkonTests/
│       └── TrieKitTests/
├── DevPlans/                       # 開發計畫與 LLM 知識文件
│   ├── LibVanguard-KnowledgeMemo4LLM.md  # 本文件
│   ├── LibVanguard-DevReqsHistory.md     # 開發階段歷史
│   └── Reqs4LLM/                         # Phase 需求文件
└── LICENSES/                       # 授權文件
```

### 2.2 技術架構

模組依賴關係（上層依賴下層）：

```
LibVanguard (主整合模組)
├── CandidateKit ── SharedCore ── SwiftExtension
├── Tekkon
├── BrailleSputnik ── Tekkon
├── TrieKit ── CSQLite3
├── Homa
├── LexiconKit ── TrieKit + Homa
└── SharedCore
```

**公開產品（Products）**：LibVanguard、TrieKit、Tekkon、Homa、SharedCore、CandidateKit
**內部模組**（不匯出）：BrailleSputnik、SwiftExtension、LexiconKit

---

## 三、核心資料模型

| 型別 | 模組 | 用途 |
|------|------|------|
| `Homa.Gram` | Homa | 元圖單位（Unigram/Bigram），承載讀音、字詞、機率、前驗詞 |
| `Homa.Node` | Homa | 組字節點，含元圖陣列、覆寫狀態、Bigram 快取 |
| `Homa.Segment` | Homa | 字詞幅節，`[Int: Node]` 字典（鍵為幅節長度） |
| `Homa.Assembler` | Homa | 組字核心處理器，持有遊標、段落、配置、動態規劃演算法 |
| `VanguardTrie.Trie.TNode` | TrieKit | Trie 節點，含 id / readingKey / children / entries |
| `VanguardTrie.Trie.Entry` | TrieKit | Trie 詞條，含 value / typeID / probability / previous |
| `Tekkon.Phonabet` | Tekkon | 單注音符號（Unicode Scalar + PhoneType） |
| `Tekkon.Composer` | Tekkon | 音節合成器，逐字元組合注音 |
| `CandidateCellData` | CandidateKit | 候選字格資料型別 |
| `Lexicon.HomaGramTuple` | LexiconKit | Homa 元圖格式元組（keyArray + value + probability + previous） |

---

## 四、核心組件詳解

### 4.1 Tekkon — 注拼引擎

負責注音符號與拼音的解析、輸入法鍵盤映射。

- **`Tekkon.Composer`**：音節合成器，接收按鍵輸入並組合出完整的注音音節。
- **`Tekkon.MandarinParser`**：支援 11 種注音排列（大千、ETen、許氏、IBM、星光等）與 6 種拼音風格（漢語拼音、耶魯、韋氏等）。
- **`Tekkon.PinyinTrie`**：簡化版拼音辨析 Trie，用於狂拼輸入的 `chop()` 拆解。
- **轉換 API**：`cnvPhonaToHanyuPinyin`、`cnvHanyuPinyinToPhona` 等注音⇄拼音雙向轉換。

### 4.2 TrieKit — 辭典樹

支援對讀音的多音字首字元檢索配對，分三種實現：

- **`VanguardTrie.Trie`**（記憶體型）：全 RAM 常駐、無 QueryBuffer、支援 Codable（Plist）序列化。亦支援 **Vanguard Pragma TextMap 格式**讀寫（見下方說明）。
- **`VanguardTrie.SQLTrie`**（SQLite 型）：DFD 硬碟直讀，僅用於原廠辭典，內建 7 秒 QueryBuffer 惰性清理快取。Phase 05 起單節點查詢會重用 cached prepared statement；在完整 Lexicon partial-match 場景下，剩餘性能瓶頸主要來自 SQLite fetch pattern 本身。
- **`VanguardTrie.TextMapTrie`**（TextMap 專用型）：Phase 35 定案後，已不再只是「惰性解析版 Trie」，而是 canonical specialized backend。其核心結構為 raw `Data` 常駐、排序後 full-key index、binary search exact lookup、lexicographic prefix-range scan、eager reverse lookup table、bounded parsed-entry cache，以及 partial-match 用的 `keyInitialsIDMap` prefilter。這讓它既維持低穩態記憶體占用，又能在 exact / longer-segment / revlookup 熱路徑上避免舊有通用 node materialization 成本。（Phase 11 曾嘗試 DFD 模式以 `pread()` 取代常駐 Data，但實機測試記憶體反飆升 20~30 MB 而作廢；詳見 Reqs4LLM/Reqs_0011-0020.md。）Phase 38 起，Typing TextMap 的 `KEY_LINE_MAP` canonical key 已改為 raw phonabet；TextMap caller layer 不得再對該 backend 做額外 encrypt/decrypt。
- **`VanguardTrieProtocol`**：統一查詢介面。核心 surface 為 `getNode(...)`、`getNodes(...)`、`getEntryGroups(...)`；高階 query API（`hasGrams` / `queryGrams` / `queryAssociatedPhrasesAsGrams`）已改以 entry groups 為主要抽象，使 `any VanguardTrieProtocol` existential 仍能命中 TextMapTrie specialized backend。
- **特殊功能**：部分比對（不完全讀音）、多音切割（分隔器 `&` 用於拼音→TrieKit 的多候選搜尋）。

#### TextMap 格式概要

`VanguardTrieIO` 提供 `parseTextMap` / `serializeToTextMap` / `loadFromTextMap` / `loadFromTextMapLazy`。`parseValueLine` 為共用的 VALUES 行解析方法，供全物化與惰性兩條路徑共用；RevLookup 由 MainTextMap 自動生成，不再讀取 external `.revlookup`。

TextMap 現行為單一 `.txtMap`（三段式 PRAGMA：HEADER / VALUES / KEY_LINE_MAP）。RevLookup 不再有獨立 sidecar，而是由 runtime 依 MainTextMap 自動構建。VALUES 行有三種型別：

- **型別 A 合併行** `>typeID\tencodedCell`：具有 DEFAULT_PROB 的 typeID 條目按讀音合併為單行，grouped cell 使用 escaped pipe 編碼。
- **型別 B CHS/CHT 機率分組行** `@probability\tchsCell\tchtCell`：僅限 `TYPE=TYPING`。`@` 前綴用來消除與一般三欄個體行的歧義，grouped cell 同樣使用 escaped pipe 編碼，BEL (`\u{7}`) 佔位空側。
- **型別 C 個體行** `value\tprobability\ttypeID[\tprevious]`：不具備 DEFAULT_PROB 或需要 Bigram `previous` 的條目。

完整格式規格引用：`DevPlans/Reqs4LLM/Reqs_0000-0010.md` Phase 02 格式規格段落。

### 4.3 Homa — 護摩組字引擎

Megrez 的繼任者，實現漢字組句動態規劃演算法。

- **Bigram 支援**：Gram 承載真實讀音；Bigram 描述僅以前驗字詞為準（不糾結前驗讀音）。
- **節點覆寫**：`OverrideType.withTopGramScore`（頂分覆寫）與 `.withSpecified`（明確指定）。
- **候選字輪替與鞏固**：內建 `Homa_CandidateAPIs_Revolver`（輪替）與 `Homa_ConsolidatorAPIs`（上下文鞏固）。
- **路徑搜尋**：`PathFinder` 以動態規劃 + Unigram/Bigram 結合評分產生最佳組句。
- 取消了舊版 `LangModelProtocol` 協定，改用回調函式 `gramQuerier`。`gramAvailabilityChecker` 已於 Phase 44 移除（`insertKeys()` 直接使用 `queryGrams()` 檢查可用性，無需獨立回調）。
- **Phase 05 熱路徑最佳化**：`Assembler` 具備跨連續 `insertKey()` 的 bounded gram query cache；`queryGrams(using:cache:)` 以結構化 comparator / hash 去除原本字串插值排序與去重成本。

### 4.4 LexiconKit — 語言模型聚合中樞

整合多種語言模型來源，向上層提供統一查詢介面。

- **`VanguardTrie.TrieHub`**：Trie 資料庫中樞，管理多個 Trie（原廠辭典等），支援 SQL 與非 SQL 切換。
- **`Perceptor`**：使用者習慣洞察器，基於 Ngram 的行為追蹤，時間衰減三次曲線。
- **`Lexicon.LMPlainBPMF`**：最小化注音語言模型。
- **`LexiconGramSupplierProtocol`**：統一所有語言模型來源的元圖供應協定。
- 不負責去重複化（交由 Homa 引擎處理）。

### 4.5 SharedCore — 共用核心

跨模組共用的協定、型別、工具。

- **鍵盤事件**：`KBEvent`（含 FCITX 擴展）、`KeyCode` / `KeyCodeBlackListed`、`ModifierFlags`、`EventType`、`SpecialKey`、`EmacsKey`。
- **協定**：`KBEventProtocol`（位於 `Protocols/`）。

### 4.6 BrailleSputnik — 盲文點字

- 支援 1947 與 2018 兩種盲文標準。
- 核心 API：`convertToBraille(smashedPairs:, extraInsertion:) -> String`。
- 內含聲介韻調完整映射表與標點符號映射。

### 4.7 SwiftExtension — Swift 語言擴展

- Bool / Double / String / Array / Set 等型別的便利運算符與擴展。
- `LatinKeyboardMappings`：拉丁字母鍵盤映射表。
- `SwiftFoundationImpl`：Foundation 相容實現（跨平台）。

---

## 五、已實作功能清單

- Phase 104 已完成：**Homa Revolver soft revolve 特性**。`Homa.Assembler.revolveCandidate` 新增 `softRevolve: Bool = false`；`softRevolve == true` 時在候選選擇層做「安全子集」過濾（新增私有 helper `isCandidateSafeToSoftRevolve`：segLength ≤ 1 短路放行，否則以 `calculateTargetCandidateRange` 算覆寫範圍，剔除會跨越「非當前節點」之 explicit 節點的多音節候選），再在安全子集上計算輪替索引。回傳的 `current` 為目標候選在**原始候選列表**中的索引，`total` 仍為原始候選總數，保留完整候選窗語義。鞏固邏輯不動（與 `skipInitialConsolidation` 正交）。Typewriter 端 `revolveCandidate(reverseOrder:softRevolve:)` 透傳該參數，凡由 SPACE 鍵（無論修飾鍵有無）啟動的輪替一律 `softRevolve: true`，其餘入口（Tab / ContextMenu / 方向鍵 / 方括號）維持 hard。解決「一個字一個字選」時鄰近已覆寫節點被多音節候選跨越毀掉的問題（根因：`calculateNextCandidateIndex` non-explicit 分支回傳索引 0 → segLength 最大候選 → `overrideCandidate` targetRange 跨越 explicit 鄰節點）。新增兩個 soft 驗收測試 + 保留兩個 hard 對照測試。跨 vChewing-LibVanguard + vChewing-macOS + vChewing-OSX-Legacy 三倉同步；LibVanguard Homa 48/48、macOS Typewriter 86/86、Legacy `make debug-core` 全數通過。
- Phase 105 已完成：**Homa Revolver soft revolve user-configurable 化**。新增 `UserDef.kPreferredRevolverForceLevel`（integer, default 2, range 0...2），讓使用者在偏好設定中選擇「一律強輪替（0）/ 僅 SPACE 路徑弱輪替（1）/ 一律弱輪替（2，預設）」。Typewriter 端所有 revolve 呼叫點（Tab / ContextMenu / 方向鍵 / 方括號 / SPACE 兩路徑）改為依 `prefs.preferredRevolverForceLevel` 動態決定 `softRevolve`：SPACE 路徑在 `!= 0` 時 soft，其餘入口在 `== 2` 時 soft。`PrefMgrProtocol` / `PrefMgr_Core` 新增 `preferredRevolverForceLevel: Int` @AppProperty 與 `fixOddPreferencesCore` 範圍自糾。Settings UI 控件由維護者從 Candidates 頁面調整至 Behavior 頁面（SwiftUI `VwrSettingsPaneBehavior` / Cocoa `VwrSettingsPaneCocoaBehavior`）。L10n 四語系（zh-Hant / zh-Hans / en / ja）5 條字串經 Gemma4 潤色，仔細闘述機理/目的/受眾/影響；ja 移除不自然的「就地」改用「その場での候補切り替え（お回し）」、各選項加上語義標籤。跨 vChewing-macOS + vChewing-OSX-Legacy 兩倉同步。macOS `swift build` + Typewriter 86/86 PASS、Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 106 已完成：**InputHandler `rotation` symbol name 消歧義為 `revolution`**。施術範圍限 InputHandler，跨 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 兩倉庫同步。`InputHandler_TriageInput.swift` 的 `triageByKeyCode` 內箭頭鍵分診臨時變數 `rotation` 改名為 `revolution`（該變數用來決定垂直向箭頭鍵在按住 Option/Shift 且處於 `.ofInputting` 時要改走 `revolveCandidate` 而非 `handleClockKey`，本意是「像左輪手槍一樣懸轉輪換」的 revolver-style revolution，與 client text writing direction 無關）；`InputHandler_TypingMethod.swift` 的 `// MARK: - Handle Rotation Toggles` 同步改名為 `// MARK: - Handle Revolution Toggles`，與其下的 `revolveTypingMethod` 語意對齊。既有 API 名 `revolveCandidate` / `revolveTypingMethod` / `revolveNext` / `preferredRevolverForceLevel` 不受影響。全倉 Swift 檔案已確認無 `rotation` 殘留。macOS `swift build` ✅、Legacy `make debug-core` ✅（兩倉 `make lint; make format` 均已執行並復編確認無回歸）。
- Phase 108 已完成：**Installer app bundle 舊版清除流程強化**。施術範圍為 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 app bundle 安裝包（pkg 不在本次範圍）。安裝前先 `killall vChewing` / `killall vChewingPhraseEditor`；以 `InputMethodConnectionName`（fallback `CFBundleIdentifier`）掃描 `~/Library/Input Methods` 與 `/Library/Input Methods` 中的 `.app` bundle；使用者層級舊版先改名為 `vChewing-yyyy-MM-ddTHH-mm-ss.appTrashed`（名稱碰撞時遞增 `-N`）再嘗試 trash；系統層級舊版以 `osascript` 索取管理員權限後 `mv` rename，失敗時記錄路徑並於安裝後以 alert 提示手動移除；`updateUpgradeableStatus()` 同步檢查使用者與系統雙目錄；新增 l10n key `i18n:Installer.AdminRenameFailureNotice`（四語系）。macOS `swift build`/`swift test`（InstallerAssembly4Darwin）✅、Legacy `make debug` BUILD SUCCEEDED（編譯目標為 `vChewingInstallerLegacy`）。
- Phase 109 已完成：**SwiftUI app installer alert refactor**。將 `VwrAppInstaller4SwiftUI.swift` 上多個 chained `.alert(...)` 改為單一 `.alert(item: $vm.config.alertItem)`；新增 `InstallerAlertItem` 與 `AlertType.makeAlertItem(paths:)`；SwiftUI 透過 `.onChange(of:)` 把 `currentAlertContent` / `adminRenameFailureAlertPaths` 同步成 `alertItem`；Cocoa 端統一由 `config.alertItem` 顯示 alert，僅在 `alertItem == nil` 時才轉換 `currentAlertContent`。所有 non-SwiftUI 修改同步至 `vChewing-OSX-Legacy`。macOS `swift build`/`swift test`（InstallerAssembly4Darwin）✅、Legacy `make debug` BUILD SUCCEEDED（編譯目標為 `vChewingInstallerLegacy`）。

- 注音符號與拼音的雙向解析與轉換（Tekkon）
- 11 種注音排列 + 6 種拼音風格支援（Tekkon）
- RAM 常駐 Trie + SQLite DFD Trie + TextMap 惰性 Trie 三模式辭典（TrieKit）
- Vanguard Pragma TextMap 格式讀寫（TrieKit / TrieIO）：三種 VALUES 行型別（`>typeID\tencodedCell` / `@probability\tchsCell\tchtCell` / 個體行），DEFAULT_PROB 壓縮，escaped pipe grouped cell，BEL 佔位空側；RevLookup 由主 `.txtMap` 自動生成；Phase 38 起 Typing TextMap `KEY_LINE_MAP` 直接儲存 raw phonabet key（format version `1.1`）
- TextMapTrie canonical specialized backend（TrieKit，Phase 35）：原始 Data 常駐 + 排序後 full-key index + binary search + prefix-range scan + eager reverse lookup + entry-group fast path；穩態記憶體仍約為全物化 Trie 的三分之一（Phase 11 DFD 嘗試已作廢）
- Phase 35 的最終定案不是下游 `FactoryTextMapLexicon` 回退。`vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 factory dictionary runtime 最終皆維持 `factoryTrie: VanguardTrie.TextMapTrie?`，`VanguardTrieProtocol.getEntryGroups(...)` 則負責讓 existential query fast path 成立。
- 下游 Phase 36 已整體完成：`LMInstantiator.Config` 新增 `partialMatchEnabled = false` 作為 partial-match 總控；factory lookup 以 `FactoryCoreLookupStrategy.configuredLookup` / `.strictSuperset` 區分預設查詢與 strict-superset；plain BPMF / 倚天 DOS / cassette quick-result 以 `SupplementalLookupStrategy.configuredLookup` / `.exactMatch` / `.partialMatch` 收斂 vocabulary；最終 `LMInstantiator.lookupHub` 成為唯一 canonical public lookup surface，舊 `query*` / `cassetteQuickSetsFor(...)` wrappers 已自 current tree 移除。
- Phase 36 的完整敘事，以及 P5 wrapper retirement 的準入清單與 kickoff template，現已整併在 `DevPlans/Research/Phase036_Research.md` appendix；不再維護獨立 checklist/template 檔案。
- Phase 38 已完成：`vChewing-LibVanguard` 的 `TrieHub` TextMap path，以及 `vChewing-macOS` / `vChewing-OSX-Legacy` 的 `LMInstantiator_TextMapExtension`，都已移除對 TextMap backend 的 phonabet encrypt/decrypt 假設；raw phonabet key 已成為目前唯一 canonical TextMap query surface。
- Phase 39 實作完成（A~D 全部）：
   - **Phase A**：`mergedAlternativeBucketUnigrams(for:)` 改寫，原廠辭典查詢改走 `factoryChoppedUnigramsFor()` / `factoryChoppedCoreUnigramsFor()`（`trie.getEntryGroups(keysChopped:...)` 路徑），原廠端查詢量從 O(tone_variants^syllable_count) 降為 O(1 per trie column)；`config.partialMatchEnabled` 正確傳遞至 `partiallyMatch` 參數。`makeTextMap` 測試輔助函式修正為 `VERSION 1.1`（修復 Phase 38 引入的 10+ 既有測試靜默失敗問題）。Review follow-up 再補 3 項 tone-bucket regression tests，鎖住單字升權、replacement/filter 排序與 CNS demote parity。跨 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 同步實裝。LangModelAssembly 87/87 通過。調研報告存為 `DevPlans/Research/Phase038_PostResearch.md`。
  - **Phase B**：`TextMapTrie.getNodeIDsForKeyArray` 的 `keyInitials` 計算由 `getCachedFirstChar`（NSLock + Dictionary subscript + `_stringCompareInternal`）+ `internKey`（再一次 NSLock）改為直接 `$0.first?.description`（零鎖），消除每次呼叫 2N 次 lock 爭用與 N 次 string comparison overhead。
  - **Phase C**：`QueryBuffer<T>` 全面改寫——`DispatchQueue.sync` 改為 `NSLock`（消除 GCD 基礎設施開銷）；`Date()` / `TimeInterval` 時間戳改為 `DispatchTime.now().uptimeNanoseconds`（單調時鐘，無系統呼叫）；改以每 64 次操作才觸發一次 `removeExpiredEntriesLocked(now:)` 清理，取代每次 set/get 都建立 `Date()` 的模式。注意：`cleanupCheckInterval` 必須宣告為 `static var`（computed），因 Swift 不允許 generic type 內的 `static let` stored properties。
   - **Phase D**：`Homa.Assembler.gramQueryCache` 從 `[[String]: [Homa.Gram]]`（需對整個 `[String]` 陣列重新雜湊）改為 `GramQueryCacheKey`（完整 `keyArray` + 預先計算 hash）的結構鍵，`gramQueryCacheOrder` 亦同步改為 `[GramQueryCacheKey]`，維護插入順序以實現 LRU 半量淘汰（淘汰最舊 50%）取代舊的 `removeAll()` 全量清空。Review follow-up 再修補原先直接使用裸 `Int` hash 的碰撞誤命中風險，保留預先計算 hash 的成本優勢但恢復 structural correctness。
   - 三個 Phase（B/C/D）已同步鏡像至 `vChewing-macOS`（`vChewing_LangModelAssembly/TrieKit/`、`vChewing_Homa/`）與 `vChewing-OSX-Legacy`（`Shared/vChewingComponents/LMAssembly/TrieKit/`、`Shared/vChewingComponents/Homa/`）；Phase D review fix 亦同步補到 `vChewing-LibVanguard`。LibVanguard HomaTests 42/42、vChewing-macOS Homa 42/42、Typewriter 44/44 通過。
- Phase 46 已完成（Omega-1 + Alpha + Beta + Zeta）：**Omega-1** 以 `Homa.PossibleKey` 結構化枚舉取代 `&` 字串編碼；`Config.keys` / `Assembler.keys` 由 `[String]` 改為 `[PossibleKey]`；`assignNodes` 以笛卡爾積內部展開 `allValues`、跨組合去重後按 `keyArray.count` 降冪再 `probability` 降冪排序；`insertKey` 保留 `String` / `[String]` / `[[String]]` 三種過載橋接；Typewriter `makeToneInsensitivePinyinQueryKey` 與 `readingKeyForQuery` 改回傳 `[String]`；LMInstantiator 移除 `mergedAlternativeBucketUnigrams`、`expandAlternativeKeyArrays`、`AlternativeKeyArrayIterator` 與 `&` 分支；修正 `queryGramsForAlternatives` 未排序導致 test_IH107 工廠 unigram 壓過使用者臨時詞的迴歸。**Alpha+Zeta**：`queryGramsForAlternatives` 移除冗餘 `insertedIntel` Set、加入無替代讀音快速路徑。**Beta**：`unigramsFor` LRU cache（上限 1024 + config fingerprint 自動失效）。**補遺**：`insertTemporaryData` / `clearTemporaryData` / `injectTestData` / `loadUserPhrasesData` / `reloadUserFilterDirectly` 在 Phase 46 Beta 後未同步使 LRU cache 失效，導致 sub-LM 資料變更後查詢仍返回過時結果；已補上對應的 cache 清除邏輯。
- Phase 47 已完成：**動態 `maxSegLength`**。`Homa.PossibleKey` 新增 `count`（可能讀音數量）與 `isMultiple`（是否多讀音）屬性；`assignNodes` 在掃描半徑計算後、span 遍歷前，檢查 `rangeOfPositions` 內是否存在 `isMultiple` 的 key——若存在且 `maxSegLength > 4`，將 `maxSegLength` 動態縮減為 4。對完整注音 / 有聲調拼音（無 `.multipleKeys`）保持 `maxSegLength = 10`，長詞不受影響；對無聲調拼音（有 `.multipleKeys`）每次插入新增查詢量封頂在 780 次（5^1 + ... + 5^4），繞開 5^5 = 3,125 的 severe hang 臨界點。已同步至 `vChewing-macOS`、`vChewing-OSX-Legacy`、`vChewing-LibVanguard`。
- Phase 48 已完成：**LMCoreEX 前綴部分匹配**。在 `LMCoreEX` 之上疊加輕量前綴索引 `sortedKeys: [String]`（由 `rangeMap.keys.sorted()` 產生），新增 `keys(matchingPrefix:)`（二分搜尋 sortedKeys + 線性掃描 temporaryMap）與 `unigramsFor(keyPrefix:)`（對每個匹配 key 自動推導正確 `keyArray` 後走 exact-match 路徑）。`LMInstantiator.unigramsFor(keyArray:)` 新增 `partiallyMatch: Bool` 過載，預設 `false` 以維持既有 exact-match 行為；`LookupHub.grams(for:)` 改傳 `partiallyMatch: lmi.config.partialMatchEnabled`，使使用者片語（`lmUserPhrases`）在開啟 partial match 時也能回傳前綴匹配結果。單漢字權重上限邏輯修正為僅對單音節匹配結果生效（避免多音節前綴匹配結果被錯誤壓分）。`lmUserSymbols` 與 `lmFiltered` 維持 exact-match 不變。**Review follow-up**：`LMInstantiator.unigramsFor(keyArray:partiallyMatch:)` 的 LRU 快取鍵加入模式維度（`PM0/PM1`）以隔離 exact / partial 查詢結果；`LMCoreEX.keys(matchingPrefix:)` 新增 key 去重並將 `temporaryMap.keys` 排序，修復重複回傳與順序抖動。新增 `testPartialAndExactQueriesDoNotShareCacheEntry`、`testPrefixMatchingDeduplicatesOverlappingMainAndTemporaryKeys` 回歸測試。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；LangModelAssembly focused tests 16/16、Legacy `make debug-core` 均通過。
- Phase 49 已完成：**narrateTheComposer 朗讀改進**。A) 防禦性注音轉換：無論 `maybeKey` 來源為何，若內容含 ASCII 字母，一律嘗試以 `Tekkon.cnvHanyuPinyinToPhona` 轉為注音，失敗則退回 `composer.value`。B) 正常組字路徑改以 `actualKeys[cursor - 1]` 為朗讀對象（取代 `readingKey.first`），確保 pathfinding 結果正確反映。C) 後置聲調覆寫 `.success` 分支補上朗讀回饋。**額外修復**：`getPreviousRearSyllableSnapshot` 原以 `receiveSequence(readingKey, isRomaji: true)` 重建 composer，但 `actualKeys` 永遠儲存注音，導致拼音模式下後置聲調覆寫完全失效；修正為統一使用 `receiveKey(fromPhonabet:)`。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；Typewriter 47/47 PASS。
- Phase 50 已完成：**冗餘 `assemble()` 清理與 Homa `dropKey` 根因修復**。Typewriter / InputHandler 層面刪除 18 處冗餘 `assemble()` 呼叫（macOS 9 + Legacy 9），涵蓋 `insertKey`、`dropKey`、`overrideCandidate`、`consolidateCursorContext`、`updateUnigramData`、`commitOverflownComposition` 等路徑。**手術中發現**：`assembler.dropKey` 的 `try? assignNodes()` 在 `noNodesAssigned` 時會跳過 `assemble()`，導致 `assembledSentence` 停留在刪鍵前狀態；`test_IH204_DropKeyAgainstAnOverriddenCandidate` 因此失敗。根因修復：將 `dropKey` 改為 `do { try assignNodes() } catch { assemble() }`，確保無論異常與否均重新組句。已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`；Typewriter 47/47、Homa 43/43、LangModelAssembly 89/89、LibVanguard Homa 44/44、Legacy `make debug-core` 全部通過。
- Phase 51 已完成：**Homa revolver 首輪 pre-consolidation 過強鎖定修復**。`test_IH201_RevolvingCandidates` 在 `main` 失敗（`流一縷` 退化成 `留一縷`）的根因為 Homa revolver 在「旅 -> 一縷」輪替路徑中，首輪 pre-consolidation 以強覆寫方式鎖住 overlap edge（前一節點的「留」），壓掉 LM 重新評分後應得的 `流`。修復以「Homa 可配置首輪 pre-consolidation + Typewriter 游標結構策略」落地：`revolveCandidate(...)` 新增 `skipInitialConsolidation: Bool = false`（預設維持既有行為）；Typewriter 依 `homaCandidateCursorType` 與 `assembler.isCursorAtAssemblerEdge(direction:)` 決定是否跳過首輪鞏固——僅 `.placedFront` 且游標在 front edge、或 `.placedRear` 且游標在 rear edge 時啟用。這使 `IH201` 的 edge-overlap case 避免過強鞏固，同時保持 `JiHuQiKeng` 非 edge 情境的既有穩定路徑。新增 Homa 層回歸測試 `Assembler_RevolveCandidateAvoidsOverConsolidatingLeadingOverlap`（macOS + LibVanguard），以及 Typewriter 層 `test_IH201` 增強 case。已同步至 `vChewing-macOS`、`vChewing-LibVanguard`、`vChewing-OSX-Legacy`；Typewriter 47/47、MainAssembly4Darwin 63/63、Homa 44/44（macOS）/ 45/45（LibVanguard）、Legacy `make debug-core` 全部通過。
- Phase 56 已完成：**MixedAlphanumericalTypewriter 系統性前綴吸收修復 + Izanami 窮舉測試落地**。Izanami 測試以 `["", "ai", "AI"]` 為參數、窮舉 `lmPlainBopomofo.sortedKeys` 所有 reading，核心斷言為「non-empty prefix 必須保留於 commissions」，一旦 composer 誤吸收即失敗。經三輪迭代共定位八類機制缺陷：純注音路徑超長吞噬、auto-split 排序偏向長後綴、Space 缺乏 non-word-like fallback、ETenDOS 被 `hasUnigramsForFast` 過濾、Tekkon auto-correct 干擾 suffix 判斷、`shouldPreferASCIIWordPath` 誤傷 2-char 前綴、整段注音路徑優先於 auto-split、`hasNoDestructiveOverwrite` 未檢查詞庫命中。修復：動態碼長上限（Dachen26=6，其餘=4）、關閉 auto-correct、引入 `twoCharPrefixIsPhonetic` 豁免、長後綴優先、結構化排序、Gated `hasNoDestructiveOverwrite`。驗證：`vChewing-macOS` Typewriter 78/78 PASS（含 Izanami 3 cases）；Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 68 已完成：**MixedAlphanumericalTypewriter camelCase 後綴防禦**。`macOS ` 被 auto-split 誤拆為 ASCII 前綴 `ma` + 注音後綴 `OS`（Tekkon 對大小寫不敏感）。修復：`buildAutoSplitCandidate` 新增 `suffixHasUppercase` guard，拒絕後綴含大寫字母的候選。僅檢查 suffix（prefix 允許含大寫如 `Hello你好`），不影響既有 auto-split 行為。新增 `test_IH429`（macOS + This）。兩倉庫同步，Typewriter 10/10 mixed-mode PASS，Legacy BUILD SUCCEEDED。已知限制：`iPhone ` 等中段大寫 + 單字母後綴仍可能誤拆（既有問題，留待後續 Phase）。
- Phase 55 已完成：**MixedAlphanumericalTypewriter 聲調與 Shift ASCII 阻斷修復**。第一要務：在 `MixedAlphanumericalTypewriter` 內對 `BPMFFullMatchTypewriter` 委派點（拼音模式 fallback、Space finalize、fully-parser-covered 注音確認）強制關閉 `isToneOverrideEnabled` 與 `isLeadingIntonationAccepted`；`isLeadingToneBlocked` 與 auto-split 後綴首鍵檢查均移除對 `handler.prefs.acceptLeadingIntonations` 的依賴，改為無條件阻斷——Mixed mode 永遠不接受聲調前置鍵入。第二要務：新增 `isShiftASCII` 與 `shouldBlockPhoneticAbsorption`（digit + uppercase）閘門，確保 Shift 敲入的 ASCII 及純數字/大寫字母不會被 Tekkon composer 吸收；`isFullyParserCovered` 路徑排除以 ASCII 數字開頭或包含大寫字母的序列，讓 auto-split 有機會正確切分「ASCII 前綴 + 注音後綴」。新增 IH425A–IH425C（digit/uppercase/Shift 阻斷）與 IH426–IH427（leading digit + uppercase auto-split）回歸測試。i18n 同步更新：四語系 `Localizable.strings` 為 `kAcceptLeadingIntonations` 與 `kSpecifyIntonationKeyBehavior` 新增 `i18n:` 鍵值，並在描述中明確載明 mixed mode 無視這些設定。其中 digit 阻斷以 `hasIntonation(withNothingElse: true)` 精準區分聲調數字鍵（大千 `3`/`4`/`6`/`7`）與聲母/韻母數字鍵（`1`/`2`/`5`/`8`/`9`/`0`），避免 `5k4`→`這` 被誤阻斷；`isFullyParserCovered` 移除 `!fullInputStartsWithASCIIDigit`，交由既有 `isLeadingToneBlocked` 統一判定。新增 IH428（`5k4`→`這`）。驗證結果為 `vChewing-macOS` Typewriter 77/77 PASS，`vChewing-OSX-Legacy` `make debug-core` BUILD SUCCEEDED。
- Phase 54 已完成：**LMInstantiator TextMap path 直接收斂到 `VanguardTrie.Trie.EntryType`，並修補 partial-match kana suppression parity**。`CoreColumn` 已自 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 LMInstantiator factory lookup surface 退場，`defaultScore` 直接掛到 `EntryType` extension；`factoryUnigramsFor`、`factoryStrictSupersetUnigramsFor`、`factoryPartiallyMatchedUnigramsFor`、`factoryChoppedUnigramsFor`、`hasFactoryCoreUnigramsFor` 等呼叫面統一改為 `entryType:`。同時將 `suppressFactoryUnigramsOfKanaSyllables` 補進 `queryGrams(...)` 對應的 `makeFactoryUnigrams(queriedGrams:...)`，修復 `partialMatchEnabled = true` 時 kana-only 結果仍漏出的 regression；其後又將 suppression predicate 固化為 value/script-based kana detection，以避開 TextMap build 對 raw kana weights 的正規化。`vChewing-macOS` 新增 `testSuppressFactoryKanaSyllablesStillWorksWithPartialMatchEnabled`；`LMInstantiatorTextMapTests` 22/22 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 53 已完成：**Typewriter 層中英文混輸**。新增 `MixedAlphanumericalTypewriter`（`TypewriterProtocol` 實作），採 `composer`（注音）+ `mixedAlphanumericalBuffer`（ASCII）雙軌模型，由 `mixedAlphanumericalEnabled` 偏好控制（預設關閉），與 cassette/pinyin 互斥，不侵入 Session 層。核心行為：auto-split 演算法即時計算最優 ASCII/Phonetic 邊界（單音節後綴搜尋，四層排序：digit-leading → longer-pure-alnum → best-probability → suffix-length）；`shouldPreferASCIIWordPath` 啟發式偵測英文詞的 destructive overwrite trace 防止 `tod `/`film ` 被誤判注音；Space finalize 設 `onLexiconMatchFailure` fallback（詞庫查無時 commit 中文段+ASCII+空白）；Enter/Space 提交時先 flush 中文段再提交 ASCII；`resolveVisibleInputText` 保留 Shift+符號可見語義；Option+主鍵盤區 ASCII 直交；`acceptLeadingIntonations = false` 在 mixed mode 下雙重防護。Session 層僅 `resetInputHandler()` 新增一行 buffer 提交。後續 follow-up 已整併：修復 `?c96` digit-leading 邊界、`tod `/`film ` 誤判、聲調前置繞過。驗證結果為 IH401–IH424 共 42 項測試全數 PASS，Legacy `make debug-core` BUILD SUCCEEDED。已知限制：全 parser-covered 短英文 token 在持續鍵入時仍可能 zhuyin-first。完整研究報告見 `DevPlans/Research/Phase053_Research.md`。
- Phase 52 已完成：**nonKanji（假名、鴨蛋零等）原廠辭典查詢修復**。根因確認為下游 `LMInstantiator` 的 `theDataMISC` 查詢被 `keyChain.hasPrefix("_")` 條件限制，導致普通讀音 key 下的 `nonKanji`（typeID 8）條目（如假名 `あ`、鴨蛋零 `〇`）永遠不會被返回。修復以新增 `CoreColumn.theDataNonKanji`（textMapTypeIDs = [8]）並在 `unigramsFor` / `hasUnigramsForFast` 中為所有普通讀音 key 啟用該查詢路徑落地，同時保留 `_` 開頭 key 的 MISC（typeID 4 + 8）既有行為。`vChewing-macOS` LangModelAssembly 新增 2 項回歸測試鎖住假名/鴨蛋零查詢與標點隔離。已同步至 `vChewing-macOS` 與 `vChewing-OSX-Legacy`；LangModelAssembly 91/91、Typewriter 74/74 PASS，Legacy `make debug-core` BUILD SUCCEEDED。
- Phase 52 follow-up 已完成：新增偏好旗標 `suppressFactoryUnigramsOfKanaSyllables`，允許使用者手動停用原廠假名音節輸出。LMI 在 `theDataNonKanji` 路徑新增條件過濾，最終改為以詞值的 kana script 判定是否濾除，不再依賴會被 TextMap build 正規化的 raw probability bucket，並保留 `〇` 等非假名 nonKanji；Settings 與四語系 i18n 亦已同步補齊。另在 mixed mode 下，除了 `input.isSymbolMenuPhysicalKey` 例外鍵之外，主鍵盤所有數字鍵 / 字母鍵 / 符號鍵只要 Alt 被摁著，就直接以 keyCode + `LatinKeyboardMappings` 還原 raw ASCII；若 assembler / `mixedAlphanumericalBuffer` 已有內容，先 flush 舊內容，再把當前 ASCII 直接 commit。`vChewing-macOS`（SwiftUI + Cocoa）與 `vChewing-OSX-Legacy`（Cocoa）均已鏡像同步；LangModelAssembly focused tests、Typewriter IH412–IH424 focused tests 與 Legacy `make debug-core` 均通過。
- 完整 Lexicon partial-match 熱路徑最佳化（Phase 05）：Assembler 跨插入查詢快取、Trie split cache 重用、node.id 去重、SQL prepared statement reuse
- 下游驗證（Phase 06 / Phase 07 Review）：`vChewing-macOS/Packages/vChewing_LangModelAssembly` 已完成以 `vanguardTextMap` 取代 legacy SQLite，採 full-match-only TextMap 存取層（sorted-array key index + binary search + bounded NSCache）。Phase 07 最終規則為：runtime 與測試路徑都不再接受 external `.revlookup` payload；反查只索引 single-segment `@` 分組行內的 ideographic characters，並無條件納入全部 CNS 條目；輸出會將 ASCII phonabet key 還原為注音。
- 不完全讀音部分比對與多音切割查詢（TrieKit）
- Unigram + Bigram 動態規劃組句（Homa）
- 節點覆寫、候選字輪替、上下文鞏固（Homa）
- Homa rare-case candidate revolver / consolidator 修補（Phase 26）：`CandidatePairWeighted.weight` 明確退回語言模型 metadata 身分，不再被當成 `.withSpecified` explicit override score；`consolidateCandidateCursorContext()` 對 overlap range 改以真實 node range 錨定；`revolveCandidate()` 對 non-explicit 且 target span 改變的首輪情形新增條件式 consolidation，並以「句子確實改變 + 目標候選落到邏輯游標 + 節點成為 explicit」作為成功條件。新增 `JiHuQiKeng` rare-case 與 true-node-anchor regressions。
- 多語言模型來源聚合（LexiconKit）
- 使用者習慣 Ngram 洞察與時間衰減（LexiconKit / Perceptor）
- 1947 與 2018 盲文標準轉換（BrailleSputnik）
- 完整的鍵盤事件協定（SharedCore）
- 下游原廠 VanguardTextMap 非同步載入（Phase 10）：`connectFactoryDictionary` 依 `asyncLoadingUserData` 拆分同步/非同步雙路徑；在當前 final tree 當中，生產環境走 `VanguardTrie.TextMapTrie` 初始化路徑，以避免輸入法載入時 1-2 秒 UI 凍結。跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 factory dictionary completion-driven 載入狀態修補（Phase 40）：`connectFactoryDictionary` 對外改為 completion callback 回報最終載入結果，`LMMgr.connectCoreDB()` 改為先發 Started 通知、再於 completion 成功時發 Complete 通知，消除 async 載入路徑的「提前宣告完成」謊報。該修補已同步鏡像至 vChewing-macOS / vChewing-OSX-Legacy；Legacy 端另依現況以明確 main-queue + `@MainActor` 發送完成通知以滿足 Swift 5.10 actor isolation。
- 下游拼音免聲調長句效能手術（Phase 41）：TrieKit `TextMapTrie` 新增 chopped lookup specialized fast path（避免 protocol default 的笛卡爾積全展開）；`LMInstantiator.mergedAlternativeBucketUnigrams(for:)` 改為先索引原廠候選再做 deferred filter removal，移除展開迴圈中的 repeated full scans；`QueryBuffer.removeExpiredEntriesLocked` 由 `cache.filter` 重建字典改為 in-place 逐鍵移除，直接收斂 `_NativeDictionary.filter` 熱點。三項修補已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。另依任務要求，worst-case profiling 入口已新增到 `vChewing-macOS` 的 `vChewingDebuggable` target（baseline / worst-case 雙按鈕）。驗證結果為 LibVanguard `swift test --filter TrieKit` 通過、LangModelAssembly `swift test --filter LMInstantiator` 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游拼音免聲調長句效能精修（Phase 42）：TrieKit `TextMapTrie` chopped path 再往 byte-oriented 推進（ASCII initials / prefix/equality 走 UTF-8 bytes，比對遇非 ASCII 自動 fallback string path 保語義）；`exactNodeByReadingKey` 命中後改重用 `getNode(...)` cache；`LMInstantiator` 的 alternatives 展開新增去重與 joined key query-scope cache，收斂 `expandAlternativeKeyArrays` / `joined(separator:)` 殘餘成本；Tekkon `PinyinTrie` 新增 parser-keyed shared cache，`pinyinAutoChopResult` 改走 shared trie，移除連續 keydown 的重建成本。上述修補已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LibVanguard `swift test --filter 'TrieKit|Tekkon'` 通過、LangModelAssembly TextMap/LMInstantiator 回歸測試通過、Tekkon Pinyin/auto-chop 測試通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 QueryBuffer NSMutex 漸進清理 + LMInstantiator 單輪 memo（Phase 43）：`QueryBuffer` 全面改為每個 mutable property 各自 `NSMutex` 保護，移除單一 `NSLock` critical section；cleanup 改為時間閘門 + 單輪互斥 + queue 頭段壓縮，避免熱路徑反覆重掃與重建。`LMInstantiator.mergedAlternativeBucketUnigrams` 新增單輪 scope memo（`FactoryLookupMemoKey`）與 `joinedKey` cache，收斂同輪重複 factory query 與 key 串接。已同步至 `vChewing-LibVanguard`、`vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LibVanguard `swift test --filter TrieKit` 通過、LangModelAssembly `swift test --filter 'LMInstantiatorTextMapTests|TrieKitTextMapTests|TrieKitTests'` 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 `hasUnigramsForFast` 輕量可用性檢查 + 非原廠迴路批量預篩選 + 惰性展開生成器（Phase 44）：**方案 A**：`LMInstantiator` 新增 `hasUnigramsForFast(keyArray:)`，跳過濾除表、語彙置換、InputToken 展開、DateTime、倚天排序等後處理步驟；對含 `&` 的 tone bucket 原廠查詢改走 `factoryChoppedCoreUnigramsFor`（O(1) per keyArray）；user phrases / symbols / cassette 僅做 hash lookup 且 early-exit。`LookupHub.hasGrams(for:)` 與 `Typewriter_BPMFFullMatch` 的 `composeReadingIfReady` / `performPinyinAutoChopIfNeeded` 同步改走 fast path，消除 `hasUnigramsFor` 造成的完整 `unigramsFor → mergedAlternativeBucketUnigrams` 重入。**方案 B**：`mergedAlternativeBucketUnigrams` 迴路前新增 `Set<String>` 批量預篩選，先將所有 expanded key 的 joined string 與 `lmUserPhrases` / `lmUserSymbols` / `lmFiltered` 的 `rangeMap + temporaryMap` keys 做 `Set.intersection`；只有交集內的 key 才執行 `unigramsFor()`，將非原廠迴路從 O(M^N) 降為 O(1)（無 user data 命中時）或 O(K)（K = 實際有資料的 key 數）。**方案 C**：`expandAlternativeKeyArrays` 改為 `AnySequence<[String]>` 惰性生成器（`AlternativeKeyArrayIterator` 以遞增計數器取代遞迴物化），`hasUnigramsForFast` early-exit 時不再生成後續組合，徹底避免 M^N 的 peak heap 壓力。同步修復 `lmCoreEX.hasUnigramsFor(key:)` 未檢查 `temporaryMap` 的 false negative，避免單元測試臨時資料被誤判為不存在。已同步至 `vChewing-macOS`、`vChewing-OSX-Legacy`。驗證結果為 LangModelAssembly 87/87 通過、Typewriter 44/44 通過、Legacy `make debug-core` BUILD SUCCEEDED。
- ~~下游 FactoryTextMapLexicon DFD 硬碟直讀（Phase 11，已作廢）~~：實機測試記憶體飆升 20~30 MB，全數撤銷。失敗根因：pread() 碎片化 heap allocation + 物化讀音鍵字串陣列開銷 + Data(contentsOf:) 隱性 mmap purgeable 優勢被低估。
- 下游 LMCassette（CIN2 磁帶模組）連續記憶體最佳化（Phase 12）：仿照 `FactoryTextMapLexicon` 模式，將 `charDefMap`、`charDefWildcardMap`、`symbolDefMap`、`reverseLookupMap` 四個大型 Dictionary 替換為 `CassetteSortedMap`（contiguous Data blob + sorted byte-range index + binary search）；`octagramMap` 與 `octagramDividedMap` 分別改為 `CassetteOctagramMap` 與 `CassetteOctagramDividedMap`。第二輪修正再將 builder 改為直接接受 grouped Dictionary，並以 lightweight prototype grouping 直接生成 wildcard / reverse lookup，移除 `tmpReverseLookup`、`tmpCharDefWildcard`、`tmpDict.map { ... }` 與 sort comparator 的短命 `Array(rawData[range])` allocation，針對載入期 HEAP 峰值進一步收斂。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 LMCassette 索引壓縮與 wildcard map 去重（Phase 128）：在 Phase 12 的 contiguous-memory 基礎上進一步壓縮索引：所有 offset / count 欄位由 `Int` 改為 `UInt32`（`CassetteMapEntry` 32 bytes → 16 bytes）；移除 `CassetteByteRange`，將 `CassetteSortedMap.allValues` 改為 `[UInt32]` flat array，每個 value 只佔兩個 slot（start、end）。同時刪除獨立的 `charDefWildcardMap` 與 `buildWildcard(from:wildcard:)`，wildcard 查詢改為直接對 `charDefMap` 做前綴掃描，經由新 API `wildcardValuesFor(key:wildcard:)` / `containsWildcardMatch(key:wildcard:)` 即時產生結果，避免載入期複製大量 value 字串；掃描熱徑再以 byte-length 判斷取代 `String` 解碼，避免熱迴圈配置。`LMCassette.open()` / `clear()` / `unigramsFor(key:keyArray:)` / `hasUnigramsFor(key:)` 同步改接新 API；單元測試移除對 `charDefWildcardMap.count` 的斷言，改以 wildcard 查詢結果非空覆蓋語義。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 94/94 通過、root `swift build -c release` 成功、Legacy `make debug` BUILD SUCCEEDED。詳見 `DevPlans/Reqs4LLM/Reqs_0121-0130.md` Phase 128 的 wildcard 效能估算。
- 下游 LMCassette CIN v2.7 花牌鍵與任意單字元鍵分化（Phase 129）：新增 `%anysinglecharkey` 解析（僅首次生效、與 `%wildcardkey` 碰撞時後來者被忽略），並將其與 wildcardKey 一併寫入 `keyNameMap` 以進入 `allowedKeys`。原 `wildcardValuesFor` / `containsWildcardMatch` 改為通用 pattern matcher `patternValuesFor(key:wildcard:anySingleChar:)` / `containsPatternMatch(key:wildcard:anySingleChar:)`：以 UTF-8 bytes token 化 pattern，開頭 wildcard 且後方全 literal 走 anagram（任意字根序）分支，其餘以 literal 前綴 lower-bound 加回溯匹配（wildcard 消秏 1+ 字元、anySingleChar 消秏恰好 1 字元），`y*` 這類查詢因此正確對應 `key count >= 2` 的全部結果。Typewriter 端移除「leading wildcard 視為錯誤」分支，開頭花牌改為合法筆畫；花牌立即組字改以 `calligrapherWasEmpty` 判定；`processCassetteQuickSelection` 新增 functional-key bypass，使 quick-candidate 狀態下的 `Shift+?`（服務選單鍵）在 `?` 對當前磁帶為 functional key（wildcard / anySingleChar / allowed key）時讓位給組筆錄入。`LMInstantiator` 新增 `cassetteAnySingleCharKey`。新增 `testCassetteCIN27WildcardAndAnySingleCharExamples`，直接載入 `array30.cin2` 重現 CIN v2.7 規格範例（「培」以 `r?;` / `ry?` / `?y;` / `r??` 可查、`y*e` 與 `yk*` 的 wildcard 聯集語義、`*ry;` 開頭花牌 anagram 找出 `ry;` 培與 `yr;` 詰）。Typewriter 端另新增 `test_IH105E_CassetteShiftQuestionTypesAnySingleCharKey`（搭配測試組件新增的可指派 `MockCandidateController`）、MainAssembly 端新增 `test108D_InputHandler_CassetteShiftQuestionTypesAnySingleCharKey`（完整 NSEvent → Session → InputHandler 路徑），雙雙鎖定「array30 敲 `y?` 應打出『熟』、不得叫出服務選單」的迴歸情境；兩者均已實測確認停用 bypass 時能精確重現原始故障。注意：bypass 生效的前提是當前磁帶確實將 `?` 定義為 `%anysinglecharkey`（或 `%wildcardkey`），且該定義未被 quick/chardef/symboldef 章節的同名資料鍵失效化；若使用者實機上的磁帶檔（或 LMMgr 快取的磁帶副本）缺少該定義，服務選單攔截 `Shift+?` 屬預期行為。另追加調整：組筆區顯示字根的 fallback 由一律「？」改為花牌鍵「♧」（U+2667）、任意單字元鍵「⍰」（U+2370）——配合 v2.7 語義分化，且刻意選用使用者無法由鍵盤直接敲出的符號；仍僅為 `%keyname` 未定義時的預設值。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 95/95 通過、Typewriter `swift build` 成功、Legacy `make debug` BUILD SUCCEEDED。詳見 `DevPlans/Reqs4LLM/Reqs_0121-0130.md` Phase 129。
- 下游 LMCassette 反查索引零複製化（Phase 130）：廢除 `reverseLookupMap: CassetteSortedMap`（對全部反查 key＋value 的第三份全量複製 blob），改為 `CassetteReverseIndex`（`revChars` 僅收去重後唯一反查字詞 bytes、`revEntries` 16B、`revCodeRefs` 指向 charDef/symbolDef 合併 namespace 的 entry 索引），查詢時依 refs 自來源 map 的既有 `rawData` 按需物化碼字串。建置掃描兩張 map 的全部 entries（含 Phase 128 起混存於 charDefMap 的花牌 pattern keys，與舊版自暫存辭典建置等價）；同字詞多碼順序由非穩定 sort 的未定義順序改為確定性（charDef 先、各依碼 bytes 序），屬已記錄的可接受漂移。採三步差分驗證：新舊並存 → 以 array30.cin2 與 wubi.cin 對每一個反查字詞逐筆比對（全數一致）→ 移除舊欄位與 `buildReverseLookup`、立正式斷言 `testCassetteReverseLookup`（「培」→ `ry;`、symboldef 合併「ㄅ」→ `w0`、nil 案例）。`open()` 於全部 map 建完後即刻釋放六個暫存 Dictionary。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 96/96 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `DevPlans/Reqs4LLM/Reqs_0121-0130.md` Phase 130。
- 下游 LMCoreEX / LMReplacements / LMAssociates 索引壓縮（Phase 131，Phase 127 手法推廣）：三模組的 Dictionary + `strData: String` 改為 `rawData: [UInt8]` + sorted UInt32 byte-offset entries + binary search。`strData` 對外維持 `String` computed 唯讀屬性（`LMInstantiator.retrieveData`、詞庫編輯器、MainAssembly 測試等既有唯讀消費端零修改——此為對原試作案的必要修訂）。`RangeParserAPI` 新增 `[UInt8]` byte 級解析 helper（`parseByteLines` / `parseByteCells` / `compareByteRange` / `compareByteSlices`），完整複刻 `String.parseRanges` 語義（跳過空片段、dense 索引、提早停止、尾端無分隔符的最後片段照常給出）。LMReplacements：entries 16B/key 直指 rawData，保留「同 key 最後一行覆蓋」語義。LMAssociates：converted-key blob + 16B entry + per-line `(lineStart, lineEnd, valueCellCount)` 12B lineRef（取代 24B tuple），保留 `#` cell 提早停止、雙鍵查詢 + dedup、dictRepresented 不 dedup；並確認 `cnvNGramKeyFromPinyinToPhona` 對純英數 pinyin 不生效屬既有行為。LMCoreEX：converted-key blob + per-line 16B entries（同 key 依檔案行序），`_sortedKeysBox` 惰性排序盒刪除（sorted entries 直接做前綴下界掃描），保留 tab 正規化、replaceData 清空 temporaryMap、第三欄權重與 `#` 壞權重、省略單漢字過濾、saveData 附加 temporaryMap 等全部行為。新增 `LMReplacementsTests`（6 項）與 `LMAssociatesTests`（7 項），`LMCoreEXTests` 既有 6 項原樣保留並追加 strData / saveData round-trip 2 項。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 LangModelAssembly 111/111 通過、Typewriter 88/88 通過、MainAssembly4Darwin 64/64 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `DevPlans/Reqs4LLM/Reqs_0131-0140.md` Phase 131。
- 下游磁帶模式花牌鍵三明治組合修復（Phase 132）：刪除 `Typewriter_Cassette` 的「組筆區非空時敲花牌鍵立即組字」條件——該條件導致敲 `y*y` 時在 `*` 當下就被迫以 `y*` 組字完畢、三明治 pattern 永遠敲不出來（任意單字元鍵 `?` 自始無此條件）。花牌鍵與任意單字元鍵一律僅作普通筆畫錄入，組字時機與一般筆畫一致（滿筆長自動組字、或空白/Enter 強制組字）；`shouldFormLongestCassetteKey` 移除花牌鍵豁免，花牌敲入後同樣參與最長可能碼判定（`zzzx*` 類無更長可能時仍自動組字，`y*` 因 `y**` 仍有匹配而保留錄入空間）。注意行為變更：敲 `y*` 後需按空白/Enter 組字，與 `y?` 及一般短碼一致。新增 `test_IH105F_CassetteWildcardSandwichStaysInCalligrapher`（`y*` / `y*y` 全程留在組筆區、空白組字後候選含「誰」「譶」），並已做「恢復舊條件→精確重現故障→恢復修復」的迴歸有效性驗證。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy；驗證結果為 Typewriter 89/89 通過、MainAssembly4Darwin 64/64 通過、Legacy `make debug` BUILD SUCCEEDED。詳見 `DevPlans/Reqs4LLM/Reqs_0131-0140.md` Phase 132。
- Homa Assembler 查詢結果去重與元圖位置唯一性修復（Phase 134）：`queryGrams` 以 keyArray 內容為鍵的快取會將同一批 `[Homa.Gram]`（含相同 FIUUID）原樣分發給不同位置的節點，導致 `generateKeyForPerception` 以 `lastIndex(id ==)` 定位 head 時誤判至最後出現的節點、POM 生成錯誤上下文記憶鍵——「分屬不同 nodes 的 Gram 其 FIUUID 必不同」的假設對快取命中共享的 Gram 並不成立。修法：快取改為純模板角色，節點收治查詢結果時（`assignNodes` 新建節點與 `syncingGrams` 兩條路徑）一律以新增的 `Gram.withNewIdentity()` 物化 FIUUID 全新的節點專屬副本；同時刪除與 instance 級 `gramQueryCache`（bounded 512 + 半量淘汰）功能重疊的 `assignNodes` 區域 `queryBuffer`，`queryGrams(using:)` / `queryGramsForAlternatives(_:)` 移除 `cache:` 參數，`insertKeys` warmup 路徑同步簡化。`assignNodes(updateExisting: Bool)` 改為 `assignNodes(updateBehavior: NodeUpdateBehavior = .fillVacancies)`（`.fillVacancies` / `.refreshExisting`），全部呼叫端（Typewriter `updateUnigramData` ×2 倉庫、HomaTests_Advanced ×2、TrieKit_Tests ×2）已遷移。新增 `Assembler_GramIdentitiesArePositionallyUnique`（相鄰同讀音節點 gram 內容雷同但 FIUUID 集合互不相交、路徑 id 唯一、`refreshExisting` 後仍成立）與 `Assembler_PerceptionKeyGenerationRespectsPositionalIdentity`（「的 的」句中 cursor 0/1 的 head 定位與上下文索引鍵正確）兩項測試，並完成「還原物化 → 兩測試精確失敗 → 恢復修復」的迴歸有效性驗證。修改範圍跨 vChewing-LibVanguard、vChewing-macOS 與 vChewing-OSX-Legacy 三倉庫；驗證結果為 LibVanguard Homa+TrieKit 50/50 通過、macOS Homa 50/50 通過、Typewriter 89/89 通過、MainAssembly4Darwin 64/64 通過、LangModelAssembly 111/111 通過、Legacy `make debug` BUILD SUCCEEDED（三倉 lint/format 後複驗均通過）。詳見 `DevPlans/Reqs4LLM/Reqs_0131-0140.md` Phase 134。
- 下游 LMAssembly 共用 parser / cell tokenizer 最佳化（Phase 13）：未直接將 `lmAssociates` / `lmCoreEX` / `lmReplacements` 全面改為 `Data`，而是先將共用 `RangeParserAPI.parse()` 改為「單一 ASCII separator 走 `String.UTF8View` byte scan、非 ASCII separator 保留 `Character` fallback」，再新增 `parseCells(in:splitee:task:)` 收斂每行 space-separated token 掃描，移除三個 SubLM 內部的 `split(separator: " ")` allocation。`lmCoreEX`、`lmAssociates`、`lmReplacements` 因此都改為直接在 `Range<String.Index>` 上抽取所需 cell；其中 `lmAssociates` 仍維持 `(lineRange, cellIndex)` 索引語義，但改為需要時對該行重掃 cell，不再物化整行 token array。以約 13.7 MB UTF-8 文本測得 `String.parse(Character)` 約 158.9 ms、`String.UTF8View` 約 14.9 ms、純 `Data` byte scan 約 5.1 ms；replaceData profiling 亦指出 LMCoreEX 的下一個熱點是 `split(separator: " ")`、字串物化與 Dictionary 寫入，因此本階段正式把第二刀落在共用 tokenization，而非全面 `Data` 化。驗證期間曾出現 associated phrase 測試誤報，最終確認根因是 MainAssembly / Typewriter 單元測試 `UserDefaults` reset 順序不當造成的 stale `cassetteEnabled` 狀態，亦已一併修正。修改範圍跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。
- 下游 InputHandler 倚天 DOS 候選補缺字修復（Phase 14）：為解決 Issue 580，本輪不動 `LMInstantiator`，而是在 `vChewing-macOS` / `vChewing-OSX-Legacy` 的 `InputHandler_CoreProtocol.generateArrayOfCandidates()` 尾段新增 `segregateCandidatesForETenDOS(from:)` 與 `supplementalETenSingleKanjiCandidates(reading:existingSingleSegments:)`。當 `prefs.enforceETenDOSCandidateSequence` 開啟時維持既有單字重排；關閉時則只在單一讀音的單字候選情形下，查詢倚天中文 DOS 序列，將現有候選中缺席的 ideographic 單漢字以 `-9.5` 權重追加到尾端，避免組句模式比逐字選字模式少字。Review 後追加修正 `segregateCandidatesForETenDOS` 對空 `keyArray` 候選的防禦——`switch` 改為 `keyArray.count` pattern match（`case 1:` / `case 2...:` / `default: break`），空陣列候選不再落入 `singleSegments`。`vChewing-macOS` 另新增兩個最小 TextMap regression tests，以 reversed factory order fixture 驗證 tail-append 與 enforced reorder 兩種行為。驗證結果為 Typewriter package 30 項測試全數通過，Legacy `make debug-core` 建置成功。
- 下游 ButKo BPMFVS 特殊遞交（Phase 15）：`vChewing-macOS` 新增本地 package `ButKo_BPMFVS` 的 `phonic_table_Z.txt` parser、cached lookup table、`convert(value:reading:)` / `convert(value:readings:)` 與 `convertToBPMFVS(smashedPairs:)`，以 ButKo 的讀音順序直接決定 IVS slot：首讀音維持原字、次讀音起附加 `U+E01E0 + slot`。查表前會先把 Typewriter raw key 的後置輕聲（如 `ㄉㄜ˙`）正規化成教材式前置輕聲（`˙ㄉㄜ`），避免與 ButKo 資料格式錯位。`vChewing_Typewriter` 新增 `CommitableMarkupType.bpmfvsAnnotationButKo = 4` 並在 `specifyTextMarkupToCommit(behavior:)` 直接走該 transformer；後續追加 `UserDef.kReflectBPMFVSInCompositionBuffer`，使組字區、選字狀態與候選預覽只在 behavior 4 且開關啟用時才投影成 BPMFVS 顯示，避免污染一般 commit path。`UserDef` / `PrefMgr` / 設定面板 / 四語系文案同步擴充。其後將同一套邏輯同步到 `vChewing-OSX-Legacy`，但資源路徑改用 `Bundle.main`。驗證結果為 `ButKo_BPMFVS` package 6 項測試通過、Typewriter package 32 項測試通過、MainAssembly4Darwin 狀態測試子集 15 項通過、Legacy `make debug-core` 建置成功。Review（Claude Opus 4）通過，無需修正。
- `vChewing-VanguardLexicon` C-based 新酷音辭典純 Swift 生成器（Phase 16）：`chewingCBasedCHS` / `chewingCBasedCHT` 已改由 `ChewingCBasedDatabaseGenerator` 直接生成 `dictionary.dat` 與 `index_tree.dat`，不再依賴 precompiled `libchewing-database-initializer`。生成器本體依掛靠的 `libchewing-sans-tsi` C 原始碼重建 `phone.cin` / `tsi.src` 解析、例外詞條檢查、字串去重與 index tree 序列化流程；其中單字 `char-misc-nonkanji` 類的負詞頻行會直接忽略，避免把 libchewing 無法表達的權重調整語義誤塞進 C-based 產物，而 root node key 仍採 16-bit truncation 以對齊原版 C 行為。驗證結果除 `swift run VCDataBuilder chewingCBasedCHT chewingCBasedCHS` 與 `LibVanguardChewingDataTests` 通過外，亦已將產物回灌至 `libchewing-sans-tsi` 既有工具 / public API 做回讀與開候選 smoke test，確認 C-based libchewing 可正常利用該產物。**Performance follow-up (2026-04-14)**：builder 端改以 reserved `Data` 直接組裝 `tsi.src` / `phone.cin`，generator 端改用 `Substring` 解析、scalar-based word lookup、binary-searched internal children 與 append-only leaf collection，將同機器同語料下 `swift run VCDataBuilder chewingCBasedCHT chewingCBasedCHS` 的總耗時由約 30.56 s 壓到約 16.29 s；兩個 target 的追加建置階段則由 CHT 約 10.28 s / CHS 約 11.19 s 降至 CHT 約 3.54 s / CHS 約 3.50 s。
- `vChewing-VanguardLexicon` HealthCheck duplicate 偵查修復（Phase 17）：確認 duplicate 偵查在 SPM 化後退化的根因是雙重的：舊版 `cook_mac.swift` 的 `value + "\t" + key` duplicate 掃描沒有移植進 `Collector.healthCheckPerMode()`，而 `prepareRawUnweightedUnigramsForPhrases()` 的 `processedPairs` / `handledHashes` 又會先吞掉部分 duplicate raw entries，使單純掃描最終 `getAllUnigrams()` 也會漏報。本輪修復改為在 `Collector_HealthCheck.swift` 直接重讀 raw source assets（`char-kanji-core`、`char-misc-bpmf`、`char-misc-nonkanji`、當前語系所有 `phrases-*`），以與 ingest 相同的 normalization 規則重建 occurrence 清單；只要 `phrase + reading` 相同即視為 duplication，不論 freq/count 是否一致，並將該組所有 occurrence 連同 `source:line` 完整彙總後寫入 `healthCheckException([String])` 再拋出失敗。另新增 2 項 synthetic tests 驗證 grouping 與 report summary。實測目前主語料沒有 duplicate 會觸發失敗，但 HealthCheck 已重新具備在 raw source 層完整列舉並中止建置的能力。**1st Review (Claude Opus 4.6)**：修正 `normalizedLinesForDuplicateScan` 行號偏移 bug（bulk-normalize + filter 後 enumerated index 不等於原始檔案行號），改為逐行正規化並保留原始行號；強化測試資料驗證 comment 行不影響行號追蹤。
- `vChewing-VanguardLexicon` Rust 版新酷音辭典純 Swift 生成器（Phase 18）：`chewingRustCHS` / `chewingRustCHT` 已改由 `ChewingRustDatabaseGenerator` 直接從 `tsi.src` / `word.src` 生成 libchewing-rust 可讀的 `tsi.dat` / `word.dat`，不再依賴外部 `chewing-cli`。生成器本體重建 libchewing-rust `TrieBuilder` 的 syllable `u16` 編碼、BFS trie index layout、DER `CHEW` 封裝、leaf phrase ordering 與 duplicate update semantics。實作過程另確認 `char-misc-nonkanji` 存在單字負詞頻（如 `ば -1 ㄅㄚ`），因此比照 Phase 16 採相容策略：單字負詞頻忽略、多字負詞頻拒絕。驗證結果為 `ChewingRustBuilderTests` 6 項測試通過、`swift run VCDataBuilder chewingRustCHS chewingRustCHT` 建置成功，且產物已回灌至 `libchewing-rust` 倉庫並由 `trieloader` 與 `chewing-cli info/dump` 成功讀取。另確認 `fuzzer/src/bin/fuzzer.rs` 目前在 Rust 2024 edition 下因 `gen` 保留字問題無法編譯，屬下游既存瑕疵，故未作為最終驗證路徑。**1st Review (Claude Opus 4.6)**：逐段比對 Swift 生成器與 Rust `TrieBuilder::write()` / `Syllable` bit layout / DER document spec，確認所有路徑一致（syllable 編碼、arena 結構、BFS serialization、leaf phrase ordering、DER 封裝含 IMPLICIT context-specific tag）。手算 syllable golden values 正確。觀察到 `appendUInt16BE`/`appendUInt32BE` 為 dead throws、`encodeUTF8String`/`encodeInteger` 以 `try?` 靜默吞錯但實務安全。**2nd Review (Claude Opus 4.6)**：追加修正——移除 dead throws 並清除 call site `try`、將 `encodeUTF8String`/`encodeInteger` 改為 properly throwing（fail-fast）、BFS 改為雙陣列層輪替（峰值記憶體 O(最大層寬)）。6/6 測試通過。**Performance follow-up (2026-04-14)**：children insertion/lookup 改為維持 syllable-sorted 陣列 + binary search、leaf duplicate 覆寫改用 `phraseIndicesByLeafID` 索引表、source parsing 改為串流灌入 builder，移除 encode 階段對每個 internal node 的重複排序。以同機器同資料對比 baseline commit `9fee096`，`writeArtifacts` 耗時由 CHS 約 20.31 s / CHT 約 20.60 s 降至 CHS 約 3.27 s / CHT 約 3.35 s，約 6x 改善。
- 下游 BPMFVS commit 洩漏修復與詞庫 Unicode 污染稽核（Phase 19）：修補 `prefs.reflectBPMFVSInCompositionBuffer` 導致 display projection 滲入一般 commit path 的問題，在 `vChewing-macOS` / `vChewing-OSX-Legacy` 新增 `committableDisplayText(...)` 作為統一 raw commit source，將一般 Enter、Space 邊界提交、候選與關聯詞提交、SCPC 選字、reset，以及 state 轉 `.ofEmpty` 的自動提交全部收斂到不含 BPMFVS 投影的原始字串。`vChewing-macOS` 新增 3 項回歸測試，鎖住 plain Enter、empty transition 與 SCPC commit 三條路徑。另對 `vChewing-VanguardLexicon` 執行 `4.3.3..HEAD` 的 components Unicode scalar 稽核，直接掃描 `U+FE00...U+FE0F` 與 `U+E0100...U+E01EF`：結果顯示歷史 diff 沒有任何 Variation Selector，現行 tree 唯一帶 VS 的 component file 是既有且未改動的 `common/data-symbols.txt`（內含 `U+FE0E` / `U+FE0F` emoji/text presentation selectors），因此未發現 `給` vs `給󠇡` 這類 Unicode identity-splitting 詞條污染。**1st Review (Claude Opus 4.6)**：逐一追蹤 Typewriter + Session 層全部 15 條 commit 路徑，確認每條都正確走 `committableDisplayText(...)` 而非 `displayedText`；唯一產出 BPMFVS 的路徑是刻意的 `commissionByCtrlOptionCommandEnter`。Legacy mirror 與所有 3 項 regression tests 通過。觀察到 `InputSession_HandleStates.swift` 的 `.ofEmpty` auto-commit 分支 `?? previous.displayedText` fallback 在理論上可傳回帶 BPMFVS 的文字；維護者據此追加修正，三處（macOS production、Legacy、Mock test session）統一改為 `if let inputHandler` 條件綁定，完全消除理論洩漏路徑。
- 下游 BPMFVS marking state 使用者詞語污染修復（Phase 20）：Phase 19 的 `committableDisplayText` 僅覆蓋 commit 路徑，但 marking state → user dictionary 加詞走的是 `state.displayTextSegments` → `displayedText` → `userPhraseKVPair` 管線，BPMFVS Variation Selectors 直接滲入使用者詞語。修復方式為在 `IMEStateData` 新增 `rawDisplayTextSegments: [String]?` 欄位實作 raw/display 雙軌分離：`displayTextSegments` 繼續持有投影文字供渲染，`rawDisplayTextSegments` 儲存保證未投影的原始字串；`userPhraseKVPair` 改從 `rawDisplayedText`（`rawDisplayTextSegments?.joined() ?? displayedText`）截取標記範圍。Typewriter 端新增 `rawDisplayTextSegmentsIfNeeded` 與 `insertReadingIntoSegments` helper，6 處 state 建構點全數填入 raw segments。同步移植至 `vChewing-OSX-Legacy`。**Review follow-up (GPT-5.4)**：補修 candidate preview 只更新 `displayTextSegments` 卻未同步 `rawDisplayTextSegments` 的一致性漏洞，新增 `test_IH103E_ButKoBPMFVSCandidatePreviewKeepsRawStateInSync`。最終驗證結果為 Typewriter 35/35、LangModelAssembly 55/55、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Hotenka 繁簡轉換引擎 v2.0.0 升級（Phase 21）：將 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 的 Hotenka 繁簡轉換引擎從 SQLite 後端（v1.3.1）升級至 StringMap 後端（v2.0.0）。核心變更：`HotenkaChineseConverter` 移除 `CSQLite3Lib` 依賴，新增 `HotenkaStringMap.swift`（binary-search-based sorted text map）；`convdict.sqlite` 替換為 `convdict.stringmap`；`init(sqliteDir:)` 替換為 `init(stringMapPath:)` throwing initializer。`ChineseConverterBridge` 的 singleton 從直接建構改為 `Optional` + `try?`（graceful degradation on load failure），所有 `convert` call site 改為 optional chaining + `?? string` fallback。`vChewing-macOS` Hotenka Package 升級至 Swift 6.2 + `defaultIsolation(MainActor.self)`，測試從 XCTest 轉換至 Swift Testing（`@Suite struct` + `@Test`）。`vChewing-OSX-Legacy` 的 Xcode project 同步更新：新增 `HotenkaStringMap.swift` source ref、替換 `convdict.sqlite` → `convdict.stringmap` resource ref。驗證結果為 Hotenka 8/8、Typewriter 35/35、LangModelAssembly 55/55、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 CNS11643 全字庫讀音過濾 UX 優化（Phase 22）：`kFilterNonCNSReadingsForCHTInput` 原先對所有不合規 Unigram 一律 `removeAll` 濾除，導致啟用該選項時「播」（CNS 僅收 ㄅㄛˋ）等常用漢字在 ㄅㄛ 讀音下完全消失。本輪改為：對單一讀音（`keyArray.count == 1`）的不合規 Unigram 以 `-9.5` 權重 demote（建構新 `Megrez.Unigram`，因 `score` 為 `let`），多讀音詞組仍維持濾除。同時為該 UserDef case 新增 `description` 欄位與四語系（en / zh-Hant / zh-Hans / ja）翻譯。既有 `testCNSMask()` 斷言更新為驗證 demote score。**Review follow-up (GPT-5.4)**：補 `test_IH113_FilterNonCNSReadingsStillAllowsSelectingDemotedSingleKanji()` Typewriter regression test，以最小 `bo` TextMap + CNS side entry 驗證 demoted `播` 仍可在候選窗被選取並成功覆寫組字結果。跨 vChewing-macOS 與 vChewing-OSX-Legacy 兩倉庫。最終驗證結果為 LangModelAssembly 54/54、Typewriter 36/36、MainAssembly4Darwin 50/50 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 BookmarkManager 熱點快取修補（Phase 23）：根據 Instruments 可確認卡頓主因不是 userphrase write flow 本身，而是 `LMMgr.dataFolderPath(isDefaultFolder: false)` / `cassettePath()` 每次都會無條件 `BookmarkManager.shared.loadBookmarks()`，而舊版 `loadBookmarks()` 又會先 `stopAllSecurityScopedAccesses()` 再逐筆 `restoreBookmark(...)`。在 iCloud Drive 路徑與 macOS 26.4.1 下，這段 security-scoped bookmark restore 會放大成 `Security::CodeSigning::KernelCode::identifyGuest` / `_CFBundleCreate` / plist parse 熱點，進而拖慢 `initUserLangModels()` 與 userphrase write。修復方式是在 `Jad_BookmarkManager` 與 legacy copy 內新增 bookmark store signature 快取：當 bookmark 檔案未變且 access 尚在時直接返回；只有在書籤檔更新、access 被 stop、或 override 改變時才重新 restore。`vChewing-macOS` 另新增 `testLoadBookmarksSkipsRestoreWhenBookmarkStoreUnchanged()` regression test。驗證結果為 BookmarkManager 10/10、MainAssembly4Darwin `swift build` 成功、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游沙盒卸除 Fallback UX 設計（Phase 24）：`vChewing-macOS` 與 `vChewing-OSX-Legacy` 在收緊 sandbox 後，已不再嘗試透過 NSOpenPanel 取得 `~/Library/Input Methods/` 目錄授權來自刪；改由 `AppDelegate.selfUninstall()` 顯示指引 NSAlert，並以 Finder 依序揭示使用者詞語資料夾、App Support 父資料夾與目前執行中的 bundle 位置，讓使用者手動完成卸除。CLI `uninstall` / `uninstall --all` 則直接輸出 wiki 與 `uninstall.sh` 提示。**Review follow-up (GPT-5.4)**：發現 GUI alert 與 CLI banner 仍沿用「未能取得目錄存取權限」舊敘述，與本 phase 已證實的 entitlement 根因不符；已修正 macOS / Legacy 兩倉庫 `Uninstaller.swift` 與四語系 `Localizable.strings`，統一改為說明 sandboxed IME 無法自行將自身 bundle 移到垃圾桶，並更正文檔中的 Finder 第三個揭示目標為 runtime `Bundle.main.bundleURL`（通常是 `~/Library/Input Methods/vChewing.app`）。驗證結果為 vChewing-macOS `swift build` 成功、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 iCloud Drive 磁帶書籤持久化失敗 workaround（Phase 25）：iCloud Drive 管理的 `~/Documents`、`~/Desktop` 等目錄中的 CIN2 磁帶檔案的 security-scoped bookmark 無法跨 reboot 持久化（macOS 26.4.1 資安升級加劇，但此行為已被開發者報告約十年）。修復方式為在 `LMMgr` 新增「Import to AppSupport cache」機制：使用者選取 CIN2 檔案時同步複製到 `~/Library/Application Support/vChewing/Cassettes/`（App Sandbox 天然可讀寫，不需 bookmark）；`cassettePath()` 在 bookmark 還原失敗時自動 fallback 至快取副本（私有 `isFileReadable()` 避免 `Broadcaster` side effect flash alert）；`loadCassetteData()` 在外部路徑可用時靜默重新整理快取副本保持最新；`resetCassettePath()` 新增快取檔案清理。所有 UI 入口（SwiftUI drag-drop/fileImporter、Cocoa drag-drop/NSOpenPanel）皆在 `saveBookmark` 後呼叫 `importCassetteFileToCache`。`toggleCassetteMode()` 與 SwiftUI toggle 的可用性判斷收斂為 `cassettePath().isEmpty`（走完整 fallback 解析）。**Review follow-up (GPT-5.4)**：快取目錄名稱統一為 `Cassettes`；`cassetteCacheDirectoryURL` 在 unit tests 下改走 unit-test sandbox，而不是實際 App Support；`resetCassettePath()` 不再誤刪使用者直接指定在 `Cassettes/` 目錄內的來源檔；`vChewing-macOS` 新增 4 項 regression tests。**UX/localization follow-up (GPT-5.4)**：新增 path-aware alert description helper，對 `~/Library/Mobile Documents/com~apple~CloudDocs/` 仍直接視為 iCloud 路徑；但對 `~/Documents` / `~/Desktop` 則改以 `getxattr` 檢查 `com.apple.icloud.desktop` / `com.apple.icloud.documents`，只在 mirror sync 真的開啟時才追加「移到本機位置、必要時可洽詢 Apple Support」提示。這套警告也同步到 macOS / Legacy 的 en / zh-Hant / zh-Hans / ja 設定 prompt，另新增 3 項 regression tests 鎖住 guidance 的顯示條件。**Symbolic link follow-up (GPT-5.4)**：新增 `resolveUserSpecifiedURL(_:)` 專門把 Phase 25 的使用者指定 URL 先做 symbolic link 解碼，再交給 path validity、bookmark、cache import 與 user-data folder 指定流程使用；涵蓋 Cocoa `NSOpenPanel`、Finder drag-drop 與 SwiftUI `fileImporter`，避免 App 只記住 symlink 殼層。另新增 2 項 regression tests 鎖住 symbolic link 檔案與目錄都會 resolve 到來源 URL。複驗結果為 MainAssembly4Darwin 59/59 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游磁帶最長可能碼自動組字、overflow trap、滿碼刪退與 Shift+Backspace 析構修補（Phase 27）：`vChewing-macOS` / `vChewing-OSX-Legacy` 的 `Typewriter_Cassette.handle(...)` 不再於 append 當前按鍵之前就判定 `isStrokesFull` / longest-possible key，而是拆成「pre-existing full buffer overflow」與「post-append auto-combine」兩段，修復 `wubi.cin` 的 `qqqq` 第 4 碼被誤當 overflow、無法直接形成「金」的 regression；同時將 quick-set refresh 後移到 combine 判定之後，且只允許在本次事件真的 append 了磁帶筆畫時才刷新，避免組字事件被提早 `return true` 吃掉，也避免 Backspace/Delete/Shift+Backspace 這類未改動 calligrapher 的事件在 cassette path 尾端被 `renderQuickSetsIfNeeded(...)` 誤吞。cassette 專用 overflow handler 仍負責讓關閉自動組字時的第 5 碼只回報 `2268DD51`，不再漏到 `triageInput` 尾端的 `A9BFF20E` blocked-data trap。這代表 Task 2 的正解不是 editing-key early-return，而是把 quick-set refresh 重新綁回真正的 stroke-append 事件，讓刪鍵自然回到通用 `handleBackSpace(...)` / `handleDelete(...)`，同時保住 Shift+Backspace 的析構語義。`vChewing-macOS` 新增 `test_IH105A_CassetteAutoCompositeWithLongestPossibleKey()`、`test_IH105B_CassetteOverflowDoesNotLeakToBlockedDataTrap()`、`test_IH105C_CassetteBackspaceWorksAtFullCalligrapherLength()`、`test_IH105D_CassetteShiftBackspaceDisassemblesPreviousCalligraph()`，另於 MainAssembly4Darwin 新增 `test108A_InputHandler_CassetteBackspaceShrinksCalligrapher()`、`test108B_InputHandler_CassetteBackspaceWorksAtFullCalligrapherLength()`、`test108C_InputHandler_CassetteShiftBackspaceDisassemblesPreviousCalligraph()`；array30 cassette tests 也已改為透過 `LMATestsData.getCINPath4Tests(...)` 取用 bundle fixture，不再因錯路徑而靜默 skip。驗證結果為 Typewriter 40/40 通過、MainAssembly focused regressions 通過，Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Perception Override / Megrez 低風險演進落地（Phase 32）：依 Phase 31 調研結果，先在 `vChewing-macOS` 與 `vChewing-OSX-Legacy` 落地 P0~P3。Megrez `Compositor` 新增 Assembler 級 `perceptor` 注入，`3_KeyValuePaired.swift` 以 `perceptionHandler ?? perceptor` dual-dispatch 自動送出 `PerceptionIntel`；`LMPerceptionOverride` 將 `kDecayThreshold` 由 `-9.5` 放寬至 `-13.0`，並把 WAL / JSON snapshot / CRC32 compaction 抽離至新 `PerceptionPersistor`，POM 只保留觀測與權重邏輯。`LMInstantiator_POMRepresentable` / `LMMgr_Core` 新增 `pomReducedLifetime` → `reducedLifetime` 注入鏈，移除權重熱路徑 `UserDefaults` 讀取；legacy 端同步移植並將 `lmPerceptionPersistor.swift` 納入 Xcode project。**Review follow-up (GPT-5.4)**：移除 macOS / legacy POM 內殘留的 debug `print`，並將 rapid-forget 測試改為直接驗證 `reducedLifetime` 注入與 `LMInstantiator` 轉發，不再依賴舊的 `UserDefaults` 路徑。驗證結果為 Typewriter 36/36、`POMRapidForgetTests` 3/3、Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 Phase 33 Homa transplant / LXPerceptor 對齊（2026-04-20）：`vChewing-macOS` 完成 Megrez → Homa、InputHandler A2 委派化、`LMPerceptionOverride` → `LXPerceptor` canonical rename，並修補 `CandidatePair` 權重語義、Homa revolver / consolidator identity 對位與同分標點保序；`vChewing-LibVanguard` 同步補齊 `LX_Perceptor.reducedLifetime` parity 與 Homa mirror regression。其後 `vChewing-OSX-Legacy` 亦完成對位 transplant，包含 Homa runtime、LMAssembly compat / adapters、Typewriter / MainAssembly bridge、Xcode project 與 Homa 授權文件同步。repo-local dotnet audit 報告為 `Actionable = 0`（37 mirrored / 1 equivalent-noop / 10 no-counterpart / 23 test-only skip），Legacy `make debug-core` BUILD SUCCEEDED。
- 下游 LMCoreEX tab→space 正規化（Phase 70）：使用者直接以 tab 字元編輯語彙檔案（如 `userdata-cht.txt`）時，因 `LMCoreEX.replaceData()` 內部 `parseCells(in: splitee: " ")` 只認 ASCII space；且 `LMConsolidator.consolidate()` 在 pragma header 完好時會 early-return 略過格式整理，導致 `open()` 與非同步 `readFileContentAsync` 兩條載入路徑的 tab→space 正規化都無法被執行，所有 tab 分隔行被靜默丟棄。修法：在 `replaceData()` 最前端加一行 `let rawStrData = rawStrData.replacingOccurrences(of: "\t", with: " ")`，以唯一匯流點確保所有載入路徑都能正確處理 tab 分隔資料。`vChewing-macOS` 新增 `testReplaceDataHandlesTabDelimitedInput` 回歸測試。`vChewing-OSX-Legacy` 同步移植。驗證結果為 LangModelAssembly 107/107 PASS（含新測試），Legacy BUILD SUCCEEDED。
- 下游候選字排序雙重修復 + 原廠辭典重載 LRU cache 自動失效（Phase 77）：Task 1 修復兩個排序缺陷：(a) `LMInstantiator.unigramsFor()` consolidate 後補 `sort { $0.probability > $1.probability }`——使用者片語 reversed 後 prepend 到原廠結果前，consolidate 去重後陣列未按 score 重排，導致低分詞條可能排在高分詞條之前；(b) `Homa.Assembler.fetchCandidates()` 排序 tuple 補第三級鍵 `$0.weight` / `$1.weight`——舊排序僅包含 segLength 與 keyArray，同讀音不同字時分數被徹底無視。Task 2 新增 `static var factoryGeneration` 世代計數器，於 `factoryTrie` setter 內遞增，並將 `unigramsFor()` 的 LRU fingerprint 改為 `config.hashValue ^ factoryGeneration`，確保原廠辭典重載後舊快取自動失效。修改範圍跨 `vChewing-macOS`、`vChewing-LibVanguard`、`vChewing-OSX-Legacy` 三倉庫。驗證結果為 LangModelAssembly 109/109、Typewriter 82/82、Homa 44/44（macOS）、LibVanguard 105/105 全數通過。

---

## 附錄一、開發階段歷史

因該章節過於龐大，故挪至 `[REPO_ROOT]/DevPlans/LibVanguard-DevReqsHistory.md` 單獨管理。

---

## 附錄二、已知問題與注意事項

### A2.1 當前已知問題

1. `LXTests4TrieHub/testTrieHubAssemblyingUsingPartialMatchAndChops` 若暫時使用完整 Lexicon 測資，會因語料語義與 Tiny Sample 不同而出現 sentence expectation failure；目前應將 benchmark 視為有效信號、將 assertion mismatch 視為已知暫時狀態。

### A2.2 開發注意事項

1. 始終保持對 Linux 與 Windows 的可建置性（透過 `#if canImport(Darwin)` 處理平台差異）。
2. TrieKit 不計畫直接支援 Regex Fuzzy Match；不完全讀音檢索需先經 Tekkon 的 `chop()` 拆解。
3. Homa 引擎取消了舊版 `LangModelProtocol` 協定，改用回調函式。
4. LexiconKit 不負責查詢結果的去重複化，該工作由 Homa 引擎完成。
5. 下游 `vChewing_LangModelAssembly` 目前的預設 factory lookup 已由 `FactoryCoreLookupStrategy.configuredLookup` 表達；當 `partialMatchEnabled` 開啟時，這條預設路徑可走 partial-match path。`FactoryCoreLookupStrategy.strictSuperset` 則是獨立的 strict-superset strategy，不得將兩者混為一談。另在 plain BPMF / 倚天 DOS 與 cassette quick-result facade 上，`SupplementalLookupStrategy` 已明確區分 `.configuredLookup` / `.exactMatch` / `.partialMatch`；其中 `configuredLookup` 保留各 backend default，不等於強制 exact，也不等於強制 partial。Phase 36 結束後，repo 內唯一 canonical public lookup surface 已是 `LMInstantiator.lookupHub`；舊 `query*` / `cassetteQuickSetsFor` wrappers 已自 current tree 移除。若未來再做類似 breaking cleanup，請直接以 `Phase036_Research.md` appendix 內的 P5 gate 與 kickoff template 為範本。
6. **Phase 46 後 `&` 字串編碼已從 Homa ↔ LMInstantiator 介面移除**。`Homa.Assembler` 現以 `[PossibleKey]` 儲存每個位置的可能讀音，並在 `assignNodes` 內部以笛卡爾積展開為精確 `[String]` 陣列後呼叫 `gramQuerier`。Typewriter 不再產生 `&` 字串；TrieKit `keysChopped:` 仍保留內部 `&` 處理供自身 partial-match 路徑使用，但 Homa 傳遞至 `LMInstantiator` 的皆為精確陣列。
6. 下游測試 fixture 的 typeID 必須與 VanguardLexicon 建置器的實際 typeID 分配一致（Phase 09 教訓：`_punctuation_list` 等 `_` 前綴 key 在 production 為 typeID=4，fixture 誤標為 5/6 導致漏洞）。
7. 不要再把 Phase 35 理解成「回退到下游 `FactoryTextMapLexicon`」。那條路徑只屬中間驗證；最終 source of truth 是 canonical `VanguardTrie.TextMapTrie` 與 `VanguardTrieProtocol.getEntryGroups(...)`。

---

## 九、關鍵檔案速查

| 檔案路徑 | 說明 |
|----------|------|
| `Package.swift` | SPM 套件定義（含 @resultBuilder DSL） |
| `makefile` | `make lint` / `make format` / `make lintFormat` / `make lintFormatUncommitted` / `make test` / `make dockertest` |
| `EVOLUTION_MEMO.md` | 各模組研發備忘錄（人工維護） |
| `Sources/_Modules/Homa/Homa_MainComponents/Homa_Assembler.swift` | 組字核心引擎（Phase 05：跨插入 gram query cache / 熱路徑去 allocation） |
| `Sources/_Modules/Homa/Homa_MainComponents/Homa_PathFinder.swift` | 動態規劃路徑搜尋 |
| `Sources/_Modules/Tekkon/Tekkon_SyllableComposer.swift` | 音節合成引擎 |
| `Sources/_Modules/TrieKit/VanguardTrie_Core.swift` | Trie 核心資料結構 |
| `Sources/_Modules/TrieKit/TrieProtocol.swift` | Trie 共用查詢邏輯（Phase 35：`getEntryGroups(...)` / existential fast path） |
| `Sources/_Modules/TrieKit/TrieTextMap_Core.swift` | TextMapTrie canonical specialized backend（Phase 35） |
| `Sources/_Modules/TrieKit/TrieSQL_Core.swift` | SQLite Trie 核心 |
| `Sources/_Modules/TrieKit/VanguardTrieIO.swift` | Trie IO（Plist + TextMap 序列化/反序列化） |
| `Sources/_Modules/TrieKit/TK_QueryBuffer.swift` | 7 秒動態清理查詢快取 |
| `Sources/_Modules/LexiconKit/LexiconHub.swift` | 詞庫中樞型別定義 |
| `Sources/_Modules/LexiconKit/LX_Components/LX_TrieDBRelated/LX_TrieHub.swift` | Trie 資料庫中樞 |
| `Sources/_Modules/LexiconKit/LX_GramConcatAPI.swift` | 元圖查詢結果聚合（Phase 05 Review：結構化排序 / 具型雜湊） |
| `Sources/_Modules/LexiconKit/LX_Components/LX_SubLMs/LX_Perceptor.swift` | 使用者習慣洞察器 |
| `Sources/_Modules/SharedCore/KBEvent.swift` | 鍵盤事件與按鍵碼定義 |
| `Sources/_Modules/SharedCore/Protocols/` | 全部共用協定 |

---

## 十、建置與開發

### 10.1 建置要求

- Xcode 16.0+
- Swift 6.2+
- macOS 15+ / iOS 18+ / visionOS 2+

### 10.2 建置指令

```bash
swift build
```

### 10.3 測試指令

```bash
make test          # Release 模式測試
make test-debug    # Debug 模式測試
make dockertest    # Docker 容器測試（Linux）
```

### 10.4 程式碼格式化

```bash
make lintFormatUncommitted  # 交差／commit 前慣用：僅對 tracked 且未 commit 的 Swift 檔依序執行 lint＋format
make lintFormat             # 全倉：SwiftLint 自動修正（先）＋ SwiftFormat（後）
make lint                   # 僅 SwiftLint 自動修正
make format                 # 僅 SwiftFormat（縮排 2 空格）
```

> 交差前「一次性運行」 `make lintFormatUncommitted` 即可（先 lint 後 format 的順序已內建）。注意 lint+format 可能將 `count == 0` 轉為 `.isEmpty`，需確認對應型別確有此屬性，否則改用 `.count * 1 == 0`。

---

## 十一、參考資料

- 詳見 `[RepoRoot]/DevPlans` 目錄的其餘 Markdown 檔案。
- `[RepoRoot]/EVOLUTION_MEMO.md` 記錄了各模組的原始設計備忘錄。

---

> ⚠️ **注意**: 本文檔需要定期更新以反映最新程式碼狀態。最後更新：2026-08-27（Phase 152 完工）。


---

## 十二、AI Agent 反應模式（Response Pattern）

> 本節供 AI Agent 參考，當用戶提出新的 Phase 開發任務時，應遵循以下標準流程。

### 12.1 工作流程（Workflow）

當用戶提出新的 Phase 需求時，按以下順序執行：

```
┌─────────────────────────────────────────────────────────────────┐
│  Step 1: 讀取相關檔案                                            │
│  - 讀取用戶指定的 Phase 描述         │
│  - 讀取需要修改的原始碼檔案                                       │
│  - 確認現有實作與新需求的關聯                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: 程式碼實作                                              │
│  - 根據需求實作功能                                              │
│  - 遵循專案現有程式碼風格（Swift 6, @MainActor, 層級結構）         │
│  - 複用既有組件和工具函數                                        │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: 編譯驗證                                                │
│  - swift build（於 [REPO_ROOT]）                                       │
│  - 確保無錯誤、無警告                                           │
│  - 如有錯誤立即修復                                              │
│  - 然後用 Xcode build 觸發 localized strings key 生成           │
│  - 完成 localization 之後再次重試編譯                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: 文件同步（並行執行）                                     │
│  ├─ DevPlans/Reqs4LLM 當中對應的 Phase 檔案: 添加 Phase 規格與實作備忘錄 │
│  ├─ DevPlans/LibVanguard-DevReqsHistory.md: 追加 Phase 歷史摘要      │
│  ├─ LibVanguard-KnowledgeMemo4LLM.md: 更新最後更新標記與近期摘要       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Step 5: 彙總報告                                                │
│  - 列出所有變更的檔案                                            │
│  - 說明核心實作邏輯                                              │
│  - 確認編譯狀態                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 12.2 文件更新規則

| 檔案 | 更新時機 | 內容格式 |
|------|---------|----------|
| **DevPlans/Reqs4LLM/Reqs_????-????.md** | 每個 Phase 必須 | `# Phase XX` 標題 + 規格說明 + `## Phase XX 實作結果` 小節 |
| **DevPlans/LibVanguard-DevReqsHistory.md** | 每個 Phase 必須 | 追加一行 `\| Phase XX \| 簡短描述 \|` |
| **LibVanguard-KnowledgeMemo4LLM.md** | 每個 Phase 必須 | 更新 `最後更新` 標記，並按需要補上近期 phase 摘要或修正過時指引 |
| **UserGuide (4語言)** | 影響使用者操作時 | 在對應章節添加功能說明（鍵盤熱鍵、滑鼠操作等） |

### 12.3 程式碼實作原則

1. **最小變更原則**：只做必要的修改，不改動無關程式碼
2. **風格一致性**：
   - 使用 `// Phase XX:` 註解標記新代碼
   - 遵循現有命名慣例（如 `handleXxx`, `onXxx`）
   - 保持縮排和空行風格
3. **平台相容性**：
   - (今後會按需求補充)
4. **狀態管理**：
   - (今後會按需求補充)

### 12.4 常見任務類型

| 任務類型 | 典型檔案 | 注意事項 |
|----------|----------|----------|

### 12.5 回報格式範本

```markdown
## 變更總結

### 程式碼變更

**檔案名稱.swift**:
- 變更項目 1
- 變更項目 2

### 文件更新

| 檔案 | 更新內容 | 注意事項 |
|------|---------|---------|
| DevPlans/Reqs4LLM/Reqs_????.md | 新增 Phase XX 規格與實作備忘錄 | 注意優先編輯以數字為 filename stem suffix 的分卷檔案，每個分卷最多 10 個 Phase，超出了就請新增分卷檔案。如果完成的任務剛好是當前分卷的最後一個任務（第十個任務）的話，請以新的分卷命名創建空白 markdown 檔案（除非該檔案已存在）。 |
| DevPlans/LibVanguard-KnowledgeMemo4LLM.md | 根據專案實際情況更新內容（如適用） | 參考既往記錄的文書風格。 |
| DevPlans/LibVanguard-DevReqsHistory.md | 新增 Phase XX 到開發階段歷史 | 參考既往記錄的文書風格。 |

### 編譯狀態

✅ 編譯通過 / ❌ 有錯誤（說明）
```

### 12.6 工作細節附註

- 交差前注意與 L10n 有關的內容是否全部補齊。
- 如果要使用 tmp 目錄的話，請使用 `[REPO_ROOT]/tmp` 目錄、而非 `/tmp/`。使用 `/tmp/` 這種 out-of-workspace 的路徑會迫使事主每次都得手動設定存取權限，非常麻煩。
- 如果事主丟給您 Xcode Instruments Profile 檔案的話，可能會是二進位檔案，請先嘗試用 `xctrace export` 解讀成 XML 再分析。
- 每次交差之前「一次性運行」 `make lintFormatUncommitted` 這一條命令。如果事主明確要你對某些工作單獨 commit 的話，每次 commit 之前也請運行 `make lintFormatUncommitted`。該命令僅對 tracked 且未 commit 的 Swift 檔案下動作，且依序執行 SwiftLint 自動修正與 SwiftFormat（先 lint 後 format 的順序已內建）。但注意在運行之後也請 build 一次，因為這些自動修正對 `count == 0` 的範式會頑固地轉換成 `.isEmpty` 而不會檢查相關 type 是否有 `isEmpty` 這個 read-only property。發現這種情況的話，可以用 `.count * 1 == 0` 取代之。commit 的 title 格式是 `ModuleName // WhatHasBeenFixed`、且在 commit 的 description 的末尾單獨一行寫明 `(Phase XXX - Task YYY)`。Commit Titles 始終以 ASCII Period `.` 結尾，除非是疑問句時才使用 `?` 結尾。
- 給 ChatGPT、Codex、Raptor 的特殊指令：Do not intentionally stop with tasks incompleted to ask me whether you do the next step I already told you. Just do it. I have backups, hence no worry. // Claude、Kimi、GLM、QwenCoder 從來不用問這些問題，就能把這些事情全部做好。唯獨 ChatGPT、Codex、Raptor 不這樣，就很奇怪很令人窩火。

---

> ⚠️ **注意**: 本文檔需要定期更新以反映最新程式碼狀態。如有發現過期內容，請及時修正。




